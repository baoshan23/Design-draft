import PageClient from './PageClient';

export const metadata = {
    title: 'Careers - GCSS',
    description: 'Join GCSS to build modern EV charging software: OCPP tooling, operations dashboards, payments integrations, and global deployments.',
};

export default function Page() {
    return <PageClient />;
}
