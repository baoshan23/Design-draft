import PageClient from './PageClient';

export const metadata = {
  title: 'Contact - GCSS | Get in Touch',
  description: 'Contact the GCSS team for demos, partnerships, or technical support. We respond within one business day.',
};

export default function Page() {
  return <PageClient />;
}
