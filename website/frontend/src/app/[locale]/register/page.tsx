import PageClient from './PageClient';

export const metadata = {
  title: 'Create Account - GCSS',
  description: 'Create a free GCSS account to deploy and manage your EV charging stations with OCPP-compatible hardware.',
};

export default function Page() {
  return <PageClient />;
}
