import PageClient from './PageClient';

export const metadata = {
  title: 'Community Forum - GCSS',
  description: 'Join the GCSS community forum to discuss EV charging operations, share setups, and get help from other operators and developers.',
};

export default function Page() {
  return <PageClient />;
}
