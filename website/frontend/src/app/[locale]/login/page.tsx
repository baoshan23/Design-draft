import PageClient from './PageClient';

export const metadata = {
  title: 'Log In - GCSS',
  description: 'Log in to your GCSS account to manage charging stations, monitor sessions, and configure your operator workspace.',
};

export default function Page() {
  return <PageClient />;
}
