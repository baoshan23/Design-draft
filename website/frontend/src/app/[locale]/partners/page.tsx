import PageClient from './PageClient';

export const metadata = {
    title: 'Partners - GCSS',
    description: 'Partner with GCSS: hardware OEMs, payment providers, network operators, and integrators building reliable EV charging deployments.',
};

export default function Page() {
    return <PageClient />;
}
