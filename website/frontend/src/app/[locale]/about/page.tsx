import PageClient from './PageClient';

export const metadata = {
    title: 'About - GCSS',
    description: 'Learn about GCSS (Global Charger System Service): our mission, what we build, and how we help operators scale EV charging networks globally.',
};

export default function Page() {
    return <PageClient />;
}
