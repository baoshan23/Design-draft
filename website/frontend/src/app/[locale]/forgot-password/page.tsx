import PageClient from './PageClient';

export const metadata = {
  title: 'Reset Password - GCSS',
  description: 'Reset your GCSS account password securely via email verification.',
};

export default function Page() {
  return <PageClient />;
}
