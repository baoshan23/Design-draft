import PageClient from './PageClient';

export const metadata = {
  title: 'FAQ - GCSS | Frequently Asked Questions',
  description: 'Answers to common questions about GCSS deployment, licensing, OCPP support, pricing, and supported charging hardware.',
};

export default function Page() {
  return <PageClient />;
}
