import PageClient from './PageClient';

export const metadata = {
  title: 'Blog - GCSS | EV Charging News & Insights',
  description: 'Latest articles on EV charging operations, OCPP protocol, station management, and industry trends from the GCSS team.',
};

export default function Page() {
  return <PageClient />;
}
