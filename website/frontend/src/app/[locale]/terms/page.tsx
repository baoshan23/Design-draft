import PageClient from './PageClient';

export const metadata = {
    title: 'Terms of Service - GCSS',
    description: 'Review the terms governing access to and use of GCSS, including acceptable use, accounts, licensing, and liability.',
};

export default function Page() {
    return <PageClient />;
}
