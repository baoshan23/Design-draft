# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-15 (home hero: re-add 3 stats counter row + push dashboard down + gradient-fade bottom): user asked "在仪表盘截图上方，按钮下方，插入之前的三个数据展示" then "仪表盘位置可以适当往下移动，底部可以用渐变消失的效果". Reinstated the `.hero-stats` block that was removed in commit `c4fe9db` (when the hero was first restructured for first-fold-fit). TWO files changed: (1) `src/app/[locale]/page.tsx` — re-added `import CounterAnimation from '@/components/effects/CounterAnimation'` and inserted the `<div className="hero-stats">...</div>` block BETWEEN `.hero-content` (which contains text + buttons) and `.hero-dashboard-mock`, making it a sibling of both inside `.hero-centered-inner`. Three `.hero-stat` items: `<CounterAnimation target={9999}/>` + `t('hero.stat1')` (Downloads / 下载量), `target={1069} suffix="+"` + `stat2` (Charge Points / 充电桩), `target={100} suffix="+"` + `stat3` (Countries / 国家). Icons from `/assets/icons/hero-stats/{downloads,chargers,countries}.svg`, all 16×16. Translation keys ALREADY exist in `messages/{en,zh}.json` under `hero.stat1/2/3` so no i18n changes needed. (2) `src/app/styles/pages-extracted.css` lines ~2417-2444: added override block `.hero.hero-centered .hero-stats { align-self: center; align-items: center; justify-content: center; gap: clamp(40px,5vw,72px); margin: clamp(8px,1.5vh,20px) 0 0; }` because the base `.hero-stats` rule in `sections.css:1788` left-aligns (`align-self/items/justify: flex-start`) — needed center alignment for the centered hero. Also `.hero.hero-centered .hero-stat { align-items: center; text-align: center; }` and `.hero.hero-centered .hero-stat-value { font-size: clamp(1.5rem, 2.4vw, 2.125rem) }` (base is 1.75-2.25rem, slightly shrunk so stats don't dominate the panel). Dashboard mock changes: `margin: 0 auto` → `margin: clamp(16px,3vh,40px) auto 0` (the 16-40px top margin nudges the dashboard further down below the stats row). Added gradient fade: `mask-image: linear-gradient(180deg, #000 0%, #000 65%, transparent 100%)` + `-webkit-mask-image` prefix — the dashboard image (frame + screenshot together since mask is on `.hero-dashboard-mock` wrapper) fades from solid at top to transparent at bottom 35%. Effect: the dashboard appears to dissolve into the dotted-grid panel rather than ending with a hard edge. NO mobile-media-query override on the mask — fade applies on mobile too. If undesirable on small screens, add `mask-image: none` inside `@media (max-width: 720px)` block at line 2466. Commit `7352711 feat(home): re-add 3 hero stats between buttons + dashboard`. Push STILL not attempted (env auth missing). PUSH STATUS: accumulated unpushed commits include the panel rework (likely bundled by parallel session into `7bb9b6b/5e791fb/805faa0`), prior hero work (`71ec5e1`, `c002947`, `6e9d788`, `d433dbc`), plus this commit `7352711` and any parallel-session demo work (`b4367d1`, `37e5c84`, `bc22efa`, `0a1cacc`, `2628273`, `dd9b5b3`). User must `git push fork main` from a shell with creds. NOTE TO FUTURE CLAUDE: if user wants the FADE START EARLIER (more aggressive), shift the `#000 65%` stop down to `#000 50%` or `40%` — at 40% the dashboard would be barely visible past mid-height. If user wants NO FADE, delete the two `mask-image` lines (the `-webkit-` prefix and the unprefixed one) from the `.hero-dashboard-mock` rule. If user wants the MASK on the FRAME instead of the mock wrapper (so the white frame + shadow also fade gracefully), move both `mask-image` lines from `.hero-dashboard-mock` to `.hero-dashboard-frame` — slight difference is the frame's box-shadow won't fade (shadow extends outside the masked region) which can look weird; mock-level masking includes shadows so cleaner. If user wants the STATS at LEFT-aligned text (in a row but each stat left-aligned), drop the `.hero.hero-centered .hero-stat { align-items: center; text-align: center }` rule but keep the row centering. Stats translation keys: change `messages/{en,zh}.json` keys `hero.stat1/2/3` — they're shared between the home hero and any other consumer (`hero.stat1` is also referenced in product-hero, so changing copy here may affect that). Counter animation: `CounterAnimation` is a client component with intersection-observer trigger — counters animate once when the hero scrolls into view. If user wants them to animate IMMEDIATELY on page load instead of on intersection, look in `src/components/effects/CounterAnimation.tsx`. PRIOR (different sessions): home hero → Arcadia-style rounded panel: white outer, 1440 panel w/ dotted-grid bg, 972 dashboard embedded inside; user ref `/tmp/claude-paste-1778836402666-db5c6342.png` (Arcadia screenshot — light grey rounded panel below header, dashboard mock sits flush at panel bottom). Restructured the hero from "full-bleed bg" to "white outer + rounded inner panel". SINGLE file changed: `pages-extracted.css` lines 2343-2398 (panel rule), 2433 (dashboard width), 2466-2509 (responsive overrides). **CHANGE 1** — `.hero.hero-centered` outer (lines 2347-2367): `background-color: #f5f6f8` → `#ffffff`, `background-image: url(...)` → `none`, kept the `:not()` specificity mirror selector so future bg work isn't wiped by polish.css. Padding: top `calc(72px + clamp(8px,2vh,24px))` (80-96px) → `calc(72px + clamp(24px,4.5vh,64px))` (96-136px) — pushes panel visually lower beneath the white header zone. Added horizontal padding `clamp(16px,3vw,32px)` (was `0`) so the panel has white edges on viewports > 1440. Added `justify-content: center` so panel centers horizontally; `align-items: center` → `align-items: stretch` so panel fills hero height. **CHANGE 2** — `.hero-centered-inner` (lines 2370-2391): now serves as the rounded panel. Added `max-width: 1440px` (was `width: 100%` only — `.container` already gives max 1440 via `--max-width` token, but I set explicit), `padding: clamp(48px,7vh,96px) clamp(24px,3vw,48px) 0` (overrides `.container`'s `0 24px` from `base.css:144` via (0,2,1) > (0,1,0) specificity), `background-color: #f5f6f8`, `background-image: url('/images/hero-grid-bg.png')`, `background-size: cover` (the pattern occupies most of panel; cover ensures full fill within the smaller panel dimensions — no big empty bands like contain on full hero), `background-position: center top`, `border-radius: clamp(16px,2vw,28px)`, `overflow: hidden` (clips dashboard at panel edges so rounded corners are clean), `flex: 1 1 auto` (panel fills hero vertical space inside outer padding). **CHANGE 3** — `.hero-dashboard-mock` (line 2433): `width: min(1080px, 92vw)` → `width: min(972px, calc(100% - 32px))` — switched from viewport-relative to panel-relative floor so on narrow screens the dashboard keeps 16px breathing room on each side INSIDE the panel. **MOBILE override** (max-width: 720px, lines 2466-2480): `padding: 56px 0 40px` → `80px 12px 32px` (gives panel 12px horizontal breathing room from screen edge); added `.hero-centered-inner { padding: 40px 20px 0; border-radius: 14px; }` so the panel has its own envelope on phones; dashboard `width: 100%` → `calc(100% - 8px)`. **SHORT-LAPTOP override** (max-height: 820 + min-width: 721, lines 2502-2515): `padding: calc(72+8px) 0 16px` → `calc(72+16px) clamp(16,3vw,32px) 16px`; added panel `padding: clamp(28px,4vh,56px) clamp(24px,3vw,48px) 0` so it stays inside the first fold on cramped vertical real estate. NOTHING removed from JSX — same `<section.hero.hero-centered> > .container.hero-centered-inner > .hero-content + .hero-dashboard-mock` tree. Background-image still references same `hero-grid-bg.png` (3840×1600 2.4:1 from prior session). Specificity selector mirror still load-bearing (without `:not(...)` polish.css `.hero { background: none }` rule would wipe the bg-color too — though we set `background-image: none` here, the outer's `#ffffff` bg-color also needs the protection). DIFFERENT PANEL vs PRIOR: prior layout the pattern bg spanned full viewport width; now contained to 1440 rounded box with white margins. The `clamp(16,2vw,28)` border-radius gives 16px at narrow viewports, 28px at 1440+. WORKING-TREE-CLEAN-AFTER-EDIT MYSTERY: when I went to commit, `git status` showed clean — the parallel session evidently bundled my Edit into one of their commits (`7bb9b6b`, `5e791fb`, or `805faa0`). I did NOT need to commit separately. Verified via `git diff HEAD -- pages-extracted.css` returns 0 lines and `git show HEAD:...` includes my `max-width: 1440px` and `width: min(972px, calc(100% - 32px))` lines. NOTE TO FUTURE CLAUDE: if user wants the dashboard FLUSH against the panel's bottom edge (currently flex-fills space with no padding-bottom but max-height clamps it), check that panel `padding-bottom: 0` is preserved. If user wants WHITE GAP between dashboard and panel bottom, add `padding-bottom: clamp(20px, 3vh, 40px)` to the panel rule. If user wants panel narrower (e.g., 1280 not 1440), change `max-width: 1440px` on `.hero-centered-inner`. If user wants the dashboard image to NOT be cropped at top (currently `object-position: top center` shows the chart header), change to `center center`. If user wants ROUNDED corners stronger, bump `clamp(16px, 2vw, 28px)` upper to e.g. `40px`. If user wants the bg-image SCALED so MORE pattern visible inside panel, switch panel `background-size: cover` → `contain` (but then bottom of panel will have empty band of bg-color). The dashboard mock has its own `border-radius: 16px` and shadow — these don't clash with panel's clip because the dashboard sits well inside panel padding. PRIOR (different sessions): hero bg cover→contain experiment (`71ec5e1`, now superseded — but commit kept in history); hero bg specificity fix (`6e9d788`); demo page card-shadow rework (`b4367d1`, `37e5c84`). user said "要看完整图，两边不裁切" — flipping the prior session's `cover` decision. SINGLE CSS keyword change in `pages-extracted.css:2352`: `background-size: cover` → `background-size: contain`. With the 3840×1600 (2.4:1) source image on typical hero containers: (a) 1440×860 (1.67:1): contain scales to width-fit → image renders 1440×600, leaving ~260px of bg-color #f5f6f8 below the pattern (image is anchored `center top` so the gap is at bottom). (b) 1080×860 (1.26:1): scales to width-fit → image 1080×450, ~410px gap below. (c) 2560×860 ULTRA-WIDE (2.98:1, wider than image ratio): contain switches to height-fit → image 2064×860, leaving ~250px gaps on EACH side filled with bg-color. So on standard monitors the image is full-width and gap-below; only at viewport widths > 2064px (≈2.4× the typical hero height) do side gaps appear. The empty bottom band is a feature not a bug — the dashboard mockup sits visually anchored below the pattern instead of layered on top of it. Background-color #f5f6f8 is the same as the image's near-white field so seam should be invisible. Commit `71ec5e1` style(home): hero bg → contain. PUSH STATUS: will fail again (env auth missing); user pushes manually — accumulated unpushed commits: `6e9d788`, `d433dbc`, `71ec5e1`, plus this CLAUDE.md commit. Specificity fix from `6e9d788` and parallel-session demo commits (`b4367d1`, `37e5c84`, `bc22efa`, `0a1cacc`) also unpushed. NOTE TO FUTURE CLAUDE: if user later says the empty bottom band looks weird (e.g., wants pattern to reach bottom of hero), options are (a) revert to `cover` and accept side crop, (b) use `100% auto` with `center top` (functionally identical to contain on standard widths but overflows + crops on ultra-wide), (c) increase the source image height by adding bottom padding to the PNG itself so the natural 2.4:1 becomes ~1.6:1 — then `cover` would barely crop. If user wants the pattern more visually present despite the gap, add a soft gradient overlay via `::before` to blend the pattern edge into the bg-color (avoid `::after` — it's killed by `display: none !important` on line 2338). If user wants the side gaps GONE on ultra-wide screens, add `background-size: 100% auto` instead — that forces width=100% and lets height overflow + clip via `overflow: hidden`. PRIOR (different sessions): demo page edition-card shadow rework (commits `b4367d1` + `37e5c84`); home hero dotted-grid bg specificity fix (commit `6e9d788`). user re-pasted a new variant of the dotted-grid bg image (`/tmp/claude-paste-1778835643295-ad255688.png`, md5 `d6b49455ad2f3fce56952f9b764f46df`, dimensions **3840×1600** vs prior `b65691740...` at 3840×2430) asking 等比例替换不要拉变形. NO files changed by me this turn — the file `public/images/hero-grid-bg.png` was ALREADY at the new content (committed by parallel session in `37e5c84 style(demo): drop drop-shadow on QR-code frames` along with the demo work — surprising bundling but confirmed via `git ls-tree HEAD` blob sha `1eacfedc...` matches `git hash-object` of the new disk content). After user picked "保持 cover (铺满，两侧轻微裁切)" in the AskUserQuestion flow, I confirmed `background-size: cover` in `pages-extracted.css:2352` is preserved — cover scales the image uniformly to fully cover the container, NEVER distorts, but DOES crop overflow. With new 2.4:1 source on typical 1.6:1 hero container (e.g. 1440×860), image gets scaled to height-fit then sides crop ~17% off each edge (270px on a 1620px scaled width). The dotted pattern in the image is concentrated in the horizontal center, so the cropped portions are mostly empty edges + outer ring of dots — center pattern + dashboard-icon-like dot clusters all survive. ALTERNATIVES OFFERED but DECLINED: (a) `contain` — full image visible, empty band below (bg-color #f5f6f8 fills); (b) `100% auto` + top-anchor — width snaps to hero, height auto-scales, pattern at top fades into empty color below. Either alternative is a 1-line CSS swap (`background-size: cover` → `contain` / `100% auto`). Background-position `center top` and background-repeat `no-repeat` unchanged. Specificity selector `html:not([data-theme="dark"]) .hero.hero-centered, html[data-theme="dark"] .hero.hero-centered` (the fix from commit `6e9d788`) still load-bearing — without it polish.css `background: none` wins and the image vanishes. Dev server on port 8000 still running, served chunk `src_app_0by61.r._.css` still has the image url. NOTE TO FUTURE CLAUDE: if user later complains about cropped edges and wants full image visible, change `pages-extracted.css:2352` `background-size: cover` → `background-size: contain` (single keyword swap). If user later wants pattern STRONGER, the image content itself is faint — boost contrast on the source PNG (no CSS change needed) OR add a `::before` overlay with `background-blend-mode: multiply` and a slightly darker tint. PRIOR (different sessions): demo page edition-card shadow rework (commits `b4367d1` + `37e5c84`); home hero dotted-grid bg specificity fix (commit `6e9d788`).
