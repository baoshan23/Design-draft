# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-15 (pricing comparison-table category-row: light gray bg → brand yellow, gold text → black): user attached `/tmp/claude-paste-1778832115024-fa4a7cf0.png` (the pricing page comparison matrix in Chinese locale, red boxes around two full-width section-divider rows showing "CPMS / 后台管理 / CPO 管理" and "充电 / 用户管理" — rendered in light gray `#F1F2F4` with gold text `#FEBF1D`) and said "这里的价格列表，把浅灰色改成黄色，然后上面的文字改成黑色" — invert the divider rows to brand yellow with black text. ROOT CAUSE: in `src/app/styles/sections.css:3684-3691` the `.comparison-table .category-row td` selector renders these rows with `background: var(--dark-surface)` (resolves to `#F1F2F4`) + `color: var(--primary)` (resolves to `#FEBF1D`). The JSX side (`src/app/[locale]/pricing/page.tsx:347-349`) emits `<tr className="category-row"><td colSpan={5}>{section.title}</td></tr>` — no JSX change needed, only the CSS. ONE FILE EDITED: `src/app/styles/sections.css:3685-3686` — two property values changed: (a) `background: var(--dark-surface)` → `background: #FEBF1D` (brand yellow, hardcoded — matches the `--primary` swatch but written as a literal because the change applies ONLY to this divider and shouldn't drift if --primary later moves). (b) `color: var(--primary)` → `color: #111418` (near-black, matching the design system's `--text-primary` for high-contrast AA on yellow). Other properties UNCHANGED: `font-weight: 700`, `font-size: 0.9rem`, `text-align: left`, `letter-spacing: 0.02em` — typography stays identical, only color contract flips. SCOPE DISCIPLINE: did NOT touch the CSS variables `--dark-surface` (`tokens.css:11 = #F1F2F4`) or `--primary` (`tokens.css:26 = #FEBF1D`) — they're shared tokens used across the entire app (cards, tab backgrounds, button fills, etc.). Touching them would have repainted half the site. The variable-→hardcoded swap deliberately ISOLATES the divider's new look. Verified `npx tsc --noEmit` clean. NOTE TO FUTURE CLAUDE: if user later says "黄色太亮了，换成稍微暗一点的金色" (yellow too bright, want a darker gold), swap `#FEBF1D` → `#E6A817` (the `--primary-dark` token's gold). If user wants the divider to use a SUBTLE yellow tint (still light, but yellow-tinted instead of gray), use a low-alpha brand-yellow overlay like `background: rgba(254,191,29,0.15); color: #8C5F00` (the `--primary-text` for AA contrast on light bg). If user wants BLACK BACKGROUND with yellow text (opposite of current), `background: #111418; color: #FEBF1D`. The matrix has ~4-5 category rows ("CPMS / 后台管理 / CPO 管理" + "充电 / 用户管理" visible in screenshot; full set comes from `pricing/page.tsx` config + i18n keys under `pricing.matrix.sections.*` — grep there for the full list). If user reports the divider looks too SOLID/harsh against the rest of the table, soften with `background: linear-gradient(180deg, #FEBF1D 0%, #FFD55A 100%)` (subtle gold gradient) or add `border-top/border-bottom: 1px solid #E6A817` for definition. Also: this is the FIRST row-styling change on the comparison table — the other selectors (`comparison-table td:nth-child(odd)`, `comparison-table thead th`, etc.) remain on the gray/dark-surface palette; if user later wants whole-table recolor, audit `.comparison-table` rules in sections.css around lines 3600-3760. PRIOR (different session): homepage hero rebuilt as Arcadia-style centered text + dashboard screenshot mock card below, with the b2b (B2B2C) tag set to solid yellow + blinking cursor — see commit log. user attached 3 images — `/tmp/claude-paste-1778831484308-9539eb7a.png` (current GCSS hero: left badge+title+stats, right 3D charger render), `/tmp/claude-paste-1778831508665-06c171ef.png` (Arcadia target layout: centered headline + paragraph + single primary CTA, BIG dashboard mockup card floating below, trusted-by logos at bottom), and `/tmp/claude-paste-1778831545283-5bab1973.jpg` (the GCSS Global-Charge admin dashboard at 3840x2160 — left sidebar with Home/Business/Devices/Operations/Other/Management/Learning-Hub nav, top "Device Status" cards (Unavailable=1, Offline=1, Occupied=2, Charging=3, Available=3, Idle=3, Total=10 donut), "Business Overview" tab w/ Today's Charging Amount=222.66 kWh, Today's Orders=32, Today's Order Total CNY=18888, Service/Electricity Fees, an October 25–November 4 multi-line chart with Nov 3 tooltip, right rail with To-Do/Platform Notifications/Quickly-configure CTA/APP-Download QR widgets). FILES CHANGED: (1) `public/images/dashboard-hero.jpg` — NEW asset, the dashboard screenshot copied from /tmp (1.4MB JPEG @ 3840x2160). (2) `src/app/[locale]/page.tsx` lines 100-173 — REWROTE the hero section: removed `<Image src="/images/hero-bg-charger.jpg" fill .../>` background entirely, changed wrapper class chain from `hero hero-with-video` → `hero hero-with-video hero-centered` (KEPT `hero-with-video` because `components.css:123-169` has `body:has(.hero-with-video) .header...` rules wiring the transparent header look — drop the class and the header breaks on homepage). Changed inner-grid class from `container hero-with-video-grid` → `container hero-centered-inner`. JSX flow now: `.hero-content > .hero-text` containing badge+H1+desc+buttons (stats REMOVED from inside `.hero-text`), then SIBLING `.hero-dashboard-mock > .hero-dashboard-frame > Image src=/images/dashboard-hero.jpg width=1920 height=1080 priority sizes="(max-width: 1200px) 92vw, 1120px" className=hero-dashboard-img`, then sibling `.hero-stats` (3 stats — Downloads 9999, Chargers 1069+, Countries 100+ — moved from inside hero-text to below the dashboard mock as a row). All same translation keys reused: `t('hero.badge')`, `t('hero.title1')`, `t('hero.desc')`, `t('hero.cta1')`, `t('hero.cta2')`, `t('hero.stat1/2/3')` — NO i18n changes needed. TypingText words array unchanged: `['CSMS Platform', '管理系统', 'Charge Hub', '充电平台']`. (3) `src/app/styles/pages-extracted.css` lines 2329+ — APPENDED a new CSS block (~120 lines) for the centered variant. Key rules: `.hero.hero-centered { background: cream gradient with top gold halo + bottom-right green tint, padding: clamp(80px,11vh,140px) 0 clamp(64px,10vh,120px), overflow: hidden }` (replaces the dark-charger bg — now reads as a clean light wash matching Arcadia). `.hero.hero-centered .hero-centered-inner { display: flex; flex-direction: column; align-items: center; gap: clamp(40px,6vh,72px) }`. `.hero.hero-centered .hero-content { align-items: center; text-align: center; max-width: 880px }` — OVERRIDES `sections.css:81-86` `.hero-with-video .hero-content { align-items: flex-start; text-align: left; max-width: min(640px, 100%) }` (specificity 0,3,0 > 0,2,0; also later in cascade since pages-extracted.css loads after sections.css). `.hero.hero-centered .hero-title { font-size: clamp(2.25rem,5vw,3.75rem); line-height:1.08; letter-spacing:-0.02em; max-width: 900px; text-align: center }` (bumped from default ~3rem to a hero-scale 60px max, matching Arcadia's "Solutions to power the new energy era" size). `.hero.hero-centered .hero-desc { max-width: 640px; margin: 0 auto 32px; text-align: center; color: #5e6470 }`. `.hero.hero-centered .hero-buttons { justify-content: center }` — overrides `sections.css:200-202` `.hero-with-video .hero-buttons { justify-content: flex-start }`. `.hero.hero-centered .hero-stats { align-self: center; justify-content: center; margin: 0; gap: clamp(40px,5vw,72px) }` — DEFEATS THREE conflicting rules: `sections.css:1788-1797` `.hero-stats { align-self: flex-start; align-items: flex-start; justify-content: flex-start; margin: clamp(28px,4vh,44px) 0 0 0 }` and `sections.css:1799-1801` `.hero-with-video .hero-stats { align-self: flex-start }` — mine wins on (0,3,0) > (0,2,0) AND later source order. `.hero.hero-centered .hero-stat { align-items: center; text-align: center }` overrides `sections.css:1803-1808` `.hero-stat { align-items: flex-start }`. THE DASHBOARD FRAME: `.hero-dashboard-mock { width: min(1120px, 92vw); margin: 0 auto }`. `.hero-dashboard-frame { border-radius: 18px; background: #fff; border: 1px solid rgba(0,0,0,0.06); box-shadow: 0 30px 60px -20px rgba(20,20,30,0.18), 0 12px 28px -12px rgba(20,20,30,0.10), inset 0 0 0 1px rgba(255,255,255,0.6); overflow: hidden }` + `::after` adds a subtle top-fade highlight `linear-gradient(180deg, rgba(255,255,255,0.25) 0%, transparent 30%)` to read as a glass-on-card. `.hero-dashboard-img { display: block; width: 100%; height: auto; border-radius: 16px }`. Mobile @720px: hero padding shrinks to `72px 0 56px`, dashboard goes full-width with `border-radius: 12px`, stats gap collapses to `28px` with `row-gap: 20px` so 3 stats wrap to 2 rows neatly on phones. ONE CRITICAL OVERRIDE: `sections.css:1249-1262` `.hero-with-video::after { content: ''; ...; background: linear-gradient(to bottom, transparent 0%, var(--dark-bg, #0a0a0a) 100%); ...; display: block !important }` baked a fade-to-#0a0a0a designed for the OLD DARK hero. With my new cream bg this would paint a 96px-tall BLACK BAND at the bottom of the hero. KILLED it with `.hero.hero-centered::after { display: none !important; content: none !important }` (specificity 0,3,1 + `!important`) — must be the very first rule in my new block so the override resolves before the rest. CASCADE/SPECIFICITY VERIFIED: every overriding selector either has higher specificity OR same specificity with later source order. `hero-bg-charger.jpg` is no longer referenced from page.tsx (file still exists in public/images/ — leave it, it's used by hero-bg-charger.* fallback paths if anywhere else references it; quick grep showed no remaining users but didn't audit exhaustively). `hero-with-video-grid` class is no longer rendered (renamed to `hero-centered-inner`) — the `sections.css:90-103` grid rules `.hero-with-video > .hero-with-video-grid { display: grid; grid-template-columns: minmax(0,1fr) minmax(0,auto); ... }` are now dead-CSS but harmless (no DOM matches them). EFFECT: homepage hero now reads top-to-bottom as `[gold badge pill] / [H1 with typing accent] / [centered narrow paragraph] / [Primary "Get started" + Secondary "Pricing" CTA pair, centered] / [BIG dashboard screenshot inside a white rounded card with subtle shadow, 1120px max wide] / [Downloads 9999 · Chargers 1069+ · Countries 100+ stats row, centered]` — then the EV-charger manufacturers marquee section (unchanged) right below. CONFIRMED via `curl -sL http://localhost:8000/zh | grep -o 'hero-centered\|hero-dashboard-mock\|hero-with-video-grid\|hero-bg-charger\|dashboard-hero.jpg'`: 4x hero-centered, 2x hero-dashboard-mock, 2x dashboard-hero.jpg, ZERO hero-with-video-grid, ZERO hero-bg-charger. NOTE TO FUTURE CLAUDE: if user reports the dashboard image looks too tall, the JPEG is 3840x2160 (16:9) — the frame respects aspect-ratio via Next/Image width=1920 height=1080. If they want it cropped to a wider/shorter aspect, either crop the source or wrap it in a container with explicit `aspect-ratio: 21/9` + `object-fit: cover` on the img. If they want the dashboard to extend BEYOND the container (full-bleed Arcadia-style), drop the `.container` parent for `.hero-dashboard-mock` or add `width: 100vw; margin-left: calc(50% - 50vw)` to break out of the container. If they want the hero stats GONE entirely (Arcadia has no stats — just logos), delete the `.hero-stats` block from page.tsx; the CSS rules for it become dead but harmless. If they want a single CTA only (Arcadia has just "Request demo"), remove the `btn-secondary` Link. The TypingText accent still runs inside the H1 — if they want a static title instead, swap `<TypingText words={...} />` for a plain `<span>` with the desired phrase. The header transparency on home is preserved because the `hero-with-video` class is kept on the section element — DO NOT drop that class without also rewriting the `body:has(.hero-with-video) .header...` selectors in components.css:123-169. PRIOR (different session): product pages b2c + b2b feature-card shadows removed via `body:has(.product-hero) .card/.glass-card/.feature-card { box-shadow: none }` rule in pages-extracted.css — see commit log.
