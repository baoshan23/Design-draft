# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session


- 2026-05-19 — **Homepage (`/[locale]`) first-screen (hero) background → soft orange/white gradient art** (commit `afced90`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "将这个背景，替换到首页首屏的背景" + a 5333×3000 baseline JPG (smooth white→amber→orange diagonal gradient, lighter top-left, deep orange bottom-right). Saved → **`website/frontend/public/images/hero-bg.jpg`** (1.55 MB; kept the old `hero-grid-bg.png` on disk, NOT `git rm`'d, for easy revert; no ImageMagick/cwebp/sharp available to downscale — gradient JPG compresses fine, on par with prior 1.26 MB b2b PNG). The homepage hero bg lives in **ONE place**: `src/app/styles/pages-extracted.css` ~2440-2446 — selector `html:not([data-theme="dark"]) .hero.hero-centered, html[data-theme="dark"] .hero.hero-centered` (the explicit `:not()` mirror already out-specifies & post-orders polish.css's `html:not([data-theme='dark']) .hero{background:none}` reset, per the in-file comment — so NO polish.css twin needed here, unlike the b2c/b2b `.product-hero` cases). FIX = 2 lines in that block: `background-image: url('/images/hero-grid-bg.png')` → `url('/images/hero-bg.jpg')`; `background-color` fallback `#f5f6f8` → `#FFF7EC` (warm cream matching the new art). `background-size:cover` + `background-position:center top` + `background-attachment:local` KEPT (cover preserves aspect ratio — crops, never distorts; the hero's `overflow:hidden`/`max-height:calc(100svh-180px)` compression still pins the image via `local`). Grepped: `hero-grid-bg` now has ZERO refs anywhere; polish.css:1009 is the unrelated b2c hero, untouched. CSS-only, no TSX/i18n → no tsc. NOTE: unrelated ` M b2c/page.tsx` showed in `git status` with an EMPTY `git diff` (no content change, linter/mtime touch) — deliberately NOT staged/committed (committed only `hero-bg.jpg` + `pages-extracted.css` by explicit path). To revert: in that pages-extracted block restore `url('/images/hero-grid-bg.png')` + `background-color:#f5f6f8`; optionally `git rm` hero-bg.jpg. **NOT VISUALLY VERIFIED in-browser** (autodebug needs user preview+debug) — correct by construction (single-source bg rule, already-proven `:not()` selector); if the deep-orange bottom-right clashes with hero content, switch `background-position` to `left top` or add a translucent white overlay on `.hero.hero-centered::before`. ───── PRIOR: **b2b (`/[locale]/b2b`) "7×24 小时专属技术支持" pricing screen (`#pricing`, Section 13) — its 2 cards rebuilt to use the pricing page's exact card/text layout** (commit `90e5416`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "把这一屏的两个卡片风格和排版，完全参考价格方案中的卡片和文字布局排版" + screenshot (red box = the whole `#pricing` section: 标题/desc + Web APP 平台版 $21,800 card + APP 平台版 $68,000 featured card). The pricing page (`pricing/page.tsx` `renderCard`) wraps its 2 b2c/b2b plan cards in `<div className="pricing-cards pricing-cards--2col">` with inner classes `.plan-name` / `.plan-price>span` / `.plan-price-note` / `.plan-actions`(buttons) / `.plan-features li>.check` — all styled by `.pricing-cards--2col .pricing-card .plan-*` (`sections.css` ~3704-3754: left-aligned, big 3.25rem price, top-bordered feature list, actions-above-features). b2b's section reused the same `.pricing-card.glass-card` wrapper but with DIVERGENT inner markup (`grid-2` + inline `maxWidth:900` wrapper; `<h3>` / `.pricing-amount` / `.pricing-features` w/ `<CheckIcon/>`; single bottom CTA) so it picked up only the base `.pricing-card` look, not the `--2col` text/layout system. FIX = **`src/app/[locale]/b2b/page.tsx` only** (~line 500-535), no CSS/i18n: grid wrapper `div.grid-2 style{maxWidth900,gap32}` → `div.pricing-cards.pricing-cards--2col` (the modifier already supplies `grid 2col + max-width:860 + margin:auto`, so inline style dropped — collapses to 1-col @≤640px via existing media query). Each card's inner DOM reordered to mirror `renderCard` exactly: `<h3>`→`<div className="plan-name">`; `.pricing-amount`→`<div className="plan-price"><span>{price}</span></div>`; added `<div className="plan-price-note">&nbsp;</div>` (b2b i18n has no note/unit — empty `&nbsp;` reserves the same vertical rhythm as pricing's filled notes; `.plan-price-note` has min-height:1.2em + mb:22px); single CTA wrapped in `<div className="plan-actions">` with `btn-full` added (standard=`btn-secondary btn-full`, featured=`btn-primary btn-full` — kept the existing single /contact "立即联系" CTA, did NOT fabricate pricing's 2nd buynow button); `.pricing-features`→`.plan-features`, `<CheckIcon/>`→`<span className="check">&#10003;</span>` (the round gold check badge `.plan-features li .check`). DOM order now name→price→price-note→plan-actions→plan-features = pricing's order (actions render above the top-bordered feature list). `.featured` class kept → its `.pricing-card.featured::before{content:'Popular'}` pill now also shows (consistent w/ pricing). Old `.pricing-amount`/`.pricing-features` CSS now unused by b2b (harmless, may be used elsewhere — not removed). `CheckIcon` import still used at b2b/page.tsx:555 (kept). `npx tsc --noEmit` CLEAN. To revert: restore the prior `grid-2` wrapper + `<h3>`/`.pricing-amount`/`.pricing-features`+`<CheckIcon/>`/single-bottom-`<Link>` block (`git show 90e5416^:website/frontend/src/app/[locale]/b2b/page.tsx`). **NOT VISUALLY VERIFIED in-browser** (autodebug needs user preview+debug) — correct by construction (reuses the proven, already-live `.pricing-cards--2col` system verbatim). ───── PRIOR: **b2b (`/[locale]/b2b`) architecture screen (`#architecture`) background → white** (commit `a8f5d1f`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "将这一屏的背景改成白色" + screenshot (the whole "OCPP CPMS 系统平台版 — B2B" `.b2b-arch-diagram` screen, which sat on the pale page bg showing through the transparent `<section className="section" id="architecture">` at `b2b/page.tsx:126`). FIX = **`src/app/styles/pages.css` only**, 1 new rule inserted right before `.b2b-arch-diagram` (~line 1458): `#architecture{background:#ffffff}`. Scoped by the section id (b2b-only; `.section` itself has no own bg, no other `#architecture` in any css). Specificity (1,0,0) beats any `.section*` rule; verified no later polish.css/`#architecture` override. CSS-only, no TSX → no tsc. To revert: delete that one `#architecture` block. **NOT VISUALLY VERIFIED** — robust-by-construction. ───── PRIOR: **b2b (`/[locale]/b2b`) first-screen (hero) background → soft orange gradient art** (commit `52f51a9`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "帮我将这张背景图，插入到 这里，大小你帮我适配一下，背景图不要变形" + a 3360×1536 orange-orb gradient JPG + a red-boxed screenshot of the whole b2b hero (SaaS平台版运营模式 (B2B2C) headline + video card). Mirrors the b2c blue-hero approach exactly. Saved JPG → **`website/frontend/public/images/b2b-hero-bg.jpg`** (450 KB). FIX = TWO files (same dual-rule pattern the b2c fix taught — pages-extracted alone is overridden by polish.css's `.hero{background:none}`): (1) **`pages-extracted.css`** new block right after `.product-hero.hero-with-shot` (~line 47): `.product-hero.particles-bg{background-color:#FCEFE7; background-image:url('/images/b2b-hero-bg.jpg'); background-size:cover; background-position:center; background-repeat:no-repeat}` — covers dark mode. (2) **`polish.css`** new block right after the b2c `html:not([data-theme='dark']) .product-hero.hero-with-shot` rule (~line 1014, before `.hero::before`): `html:not([data-theme='dark']) .product-hero.particles-bg{…same…}` — the decisive light-mode rule, specificity (0,3,1) > the `html:not([data-theme='dark']) .hero{background:none}` reset (0,2,1) AND after it in source order. `background-size:cover` preserves the image's aspect ratio (no distortion — crops overflow, never stretches), satisfying "不要变形". **Scope b2b-only by construction:** b2b hero = `.product-hero.particles-bg` (`b2b/page.tsx:90`); b2c = `.product-hero.hero-with-shot` (`b2c/page.tsx:53`) keeps its blue art, untouched. /zh/b2b HTTP 200; `/images/b2b-hero-bg.jpg` 200 image/jpeg; both selectors confirmed in served `_next` bundle (`.product-hero.particles-bg` @ ~17425, `html:not([data-theme="dark"]) .product-hero.particles-bg` @ ~20749). No JSX/i18n → no tsc. To revert: delete BOTH new blocks; optionally `git rm` the image. **NOT VISUALLY VERIFIED in-browser** (autodebug needs user preview+debug) — correct by cascade analysis + bundle check; if the orange orb (upper-left of the art) clashes with the hero text, switch `background-position` to `right center` or add a translucent white overlay. **FOLLOW-UP (commit `12bc18d`, pushed fork):** user asked to try `right center` — both rules' `background-position: center` → `right center` (so the orange orb shifts off the left text area toward the right edge / video card side). Confirmed 2× `right center` in served bundle. **FOLLOW-UP 2 (commit `7ce852a`, pushed fork):** user gave a new art ("更换成这个背景试试") — a 3840×2197 RGBA PNG (left-orange → center-blue blob gradient w/ small scattered dots). Saved → **`public/images/b2b-hero-bg.png`** (818 KB); both rules now `url('/images/b2b-hero-bg.png')` (was `.jpg`) + `background-color` fallback `#FCEFE7`→`#E3EBFB` (matches the new bluish art); `background-position:right center` kept. `git rm`'d the orphaned old `b2b-hero-bg.jpg`. Served bundle now 2× `.png`, 0× `.jpg`; `/images/b2b-hero-bg.png` 200 image/png. **FOLLOW-UP 3 (commit `c842ff9`, pushed fork):** user gave another art ("更换成这张试试") — a 2880×1036 RGBA PNG, soft light-blue/lavender gradient (very uniform, faint diagonal light streaks). Overwrote `public/images/b2b-hero-bg.png` **in place** (same path) — NO CSS change; existing `#E3EBFB` fallback + `cover`/`right center` still suit it. Served 200 image/png (1.26 MB). Revert: `git show c842ff9^:website/frontend/public/images/b2b-hero-bg.png` to restore the prior blue/orange-dotted art. Revert whole feature: delete both `.product-hero.particles-bg` blocks (pages-extracted ~line 47 + polish.css ~line 1014). ───── PRIOR: **b2b (`/[locale]/b2b`) "OCPP CPMS 系统平台版 — B2B" arch diagram: card yellow edge-border removed + all yellow gradients flattened to pure `#FEBF1D`** (commit `5cee9fe`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "这里的卡片边缘黄色线去掉，把所有的渐变黄改成纯黄色" + screenshot (red box = the whole `.b2b-arch-diagram`). FIX = **`src/app/styles/pages.css` only**, 6 edits, no TSX: (1) `.b2b-arch-box` border `1.5px solid var(--accent-strong)` → `1px solid var(--border-subtle)` (kills static gold card edge); (2) `.b2b-arch-box::before` hover edge `linear-gradient(135deg,var(--accent),transparent 70%)` → `none`; (3) `.b2b-arch-box:hover` box-shadow dropped the `0 0 0 1px var(--accent-strong)` gold ring; (4) `.b2b-arch-instance-dot` A/B/N circle `radial-gradient(...var(--accent)→var(--accent-dark))` → flat `var(--accent)`; (5) `.b2b-arch-pill:hover` `linear-gradient(135deg,var(--accent),var(--accent-dark))` → flat `var(--accent)` (line 1721 ONLY — an identical gradient at pages.css:2430 is a DIFFERENT out-of-scope element, untouched); (6) `.b2b-arch-center-ring` 3-layer radial/radial/linear yellow gradient → flat `rgba(254,191,29,0.12)`. `.b2b-arch-box-header` dashed divider & ring `border`/connectors are solid (not gradients, not card edge) — left intact per scope. `--accent` = `#FEBF1D` for all 3 role boxes (gold-mono since `f0c0299`). CSS-only → no tsc. To revert: restore the 6 originals (`git show 5cee9fe^`). **NOT VISUALLY VERIFIED** — robust-by-construction. ───── PRIOR: **b2c (`/[locale]/b2c`) hero stats bar (`.hero-footer-bar`): pulsing gold glow removed, replaced with flat #e5e5e5 hairline** (commit `b5c737e`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "这里的投影去掉，改成线条#e5e5e5" + screenshot (red box = the 3-stat footer strip 管理充电桩/支持运营商/支持货币 at the b2c hero fold). The "投影" was NOT a static box-shadow — it's the **`heroFooterGlow` keyframe** (`sections.css:1875`, an infinite 4s pulse of `box-shadow: 0 0 20-30px rgba(254,191,29,0.05-0.1)`) applied via the `animation:` shorthand on `.hero-footer-bar` (`sections.css:1911`). FIX = **`src/app/styles/sections.css` only**, 1 new rule added right after `html:not([data-theme="dark"]) .hero .hero-footer-divider` (~line 1921): `html:not([data-theme="dark"]) .product-hero.hero-with-shot .hero-footer-bar{animation:heroFooterSlideUp 0.8s ease-out; box-shadow:none; border-top:1px solid #e5e5e5}`. The `animation` override is the key — the shorthand fully replaces the base's two-animation list, dropping `heroFooterGlow` (keeps the one-shot slide-up entrance); `box-shadow:none` is belt-and-suspenders. Also swaps the near-invisible light-mode `border-top:rgba(255,255,255,0.5)` for a solid `#e5e5e5` line. **Scope b2c-only:** selector includes `.product-hero.hero-with-shot` (b2c hero); b2b's identical bar (`.particles-bg`) keeps the original glow — intentional. Specificity (0,4,1) > the `html:not([data-theme="dark"]) .hero-footer-bar` light rule (0,2,1) and the base `.hero-footer-bar{animation}` (0,1,0). Verified no later css (pages-extracted/pages/polish) re-adds box-shadow/animation to `.hero-footer-bar`. Confirmed compiled into served `_next` bundle (`box-shadow:none; border-top:1px solid #e5e5e5; animation:.8s ease-out heroFooterSlideUp`). CSS-only, no JSX → no tsc. To revert: delete that one rule (base `heroFooterGlow` resumes). **NOT VISUALLY VERIFIED in-browser** (autodebug session needs user preview+debug) — correct by cascade analysis + bundle check. **NOTE:** the prior blue-gradient hero bg fix (`b9bec80`) IS now visibly working per the user's screenshot. ───── PRIOR: **b2c (`/[locale]/b2c`) first-screen (hero) background → soft blue gradient art** (commit `25ae3d1`, fork `design-draft-local` per PUSH POLICY, no force). User: "帮我将这张图，作为这里的首屏背景" + a 3840×1440 blue-orb gradient JPG + a red-boxed screenshot of the whole b2c hero (OCPP CPMS / 独立运营商模式 (B2C) headline, monitor shot, stats bar). Saved the JPG → **`website/frontend/public/images/b2c-hero-bg.jpg`** (98 KB, progressive). FIX = **`src/app/styles/pages-extracted.css` only**, 1 new rule right after the base `.product-hero{…background:#F1F2F4}` block (~line 36): `.product-hero.hero-with-shot{background-color:#F4F7FE; background-image:url('/images/b2c-hero-bg.jpg'); background-size:cover; background-position:center; background-repeat:no-repeat}`. **Scope is b2c-only by construction:** b2c hero = `.product-hero.hero-with-shot` (`b2c/page.tsx:53`), b2b hero = `.product-hero.particles-bg` (`b2b/page.tsx:90`) — b2b intentionally keeps flat #F1F2F4, unaffected. The flat-#F1F2F4 first screen note in `polish.css` ~1023-1026 still holds for b2b; `.product-hero::before` dot overlay stays `display:none` in light mode (untouched). No JSX/i18n change → no tsc. /zh/b2c HTTP 200; `/images/b2c-hero-bg.jpg` serves 200 image/jpeg. **FOLLOW-UP — user reported "没有改到" (no visible change). ROOT CAUSE:** `polish.css:1000` `html:not([data-theme='dark']) .hero{background:none}` (specificity 0,2,1) BOTH out-specifies the pages-extracted `.product-hero.hero-with-shot` rule (0,2,0) AND loads later (polish.css is last `@import` in globals.css) — so in light mode the hero bg was reset to none. **FIX (commit `b9bec80`, pushed fork):** added `html:not([data-theme='dark']) .product-hero.hero-with-shot{background-color:#F4F7FE; background-image:url('/images/b2c-hero-bg.jpg'); background-size:cover; background-position:center; background-repeat:no-repeat}` in **`polish.css` only**, inserted right AFTER the `.hero{background:none}` reset (~line 1002) so it wins by both specificity (0,3,1 > 0,2,1) and source order. The pages-extracted.css rule is KEPT (covers dark mode, where the light-only reset doesn't apply → image shows in both themes). Verified both rules compiled into the served `_next` CSS bundle (selectors `\.product-hero\.hero-with-shot` @ ~17411 and `html:not([data-theme="dark"]) .product-hero.hero-with-shot` @ ~20727, the latter immediately after the `.hero{background:none}` @ ~20723). LESSON: any future `.product-hero`/`.hero` visual override MUST also clear the `html:not([data-theme='dark']) .hero{background:none}` (and sibling `.mesh-bg/.particles-bg/::before` resets) in that polish.css ~1000-1027 light-mode block — pages-extracted alone is not enough. To revert fully: delete BOTH the pages-extracted block and the polish.css block; optionally `git rm` the image. **STILL NOT VISUALLY VERIFIED in-browser** (autodebug session needs user to open preview + enable debug; navigate returned "no active autodebug session") — but now correct by cascade analysis + confirmed in the compiled bundle. If the blue orb mass clashes with the right-side monitor shot, switch `background-position` to `left center` or add a translucent white overlay. ───── PRIOR: **Pricing (`/[locale]/pricing`) "完整功能对比" yellow header row shortened again** (commit `b2b2f43`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "这里'功能'这一栏，高度缩小一点" + screenshot (red box = the sticky yellow `<thead>` of `.comparison-table`). FIX = same 1 rule, **`src/app/styles/sections.css` only** (~3871-3872): `padding-top: 20px → 16px`, `padding-bottom: 56px → 36px` (≈24px shorter, on top of prior `1205e35`'s 24→20 / 80→56). `.comparison-table tbody tr:first-child td{padding-top:32px}` (~3879) left intact. CSS-only, no TSX → no tsc. To revert: restore `padding-top:20px; padding-bottom:56px` (or fully to `24px/80px`). **NOT VISUALLY VERIFIED** — robust-by-construction. ───── PRIOR: **/demo (`/[locale]/demo`) "多租户平台版" edition bullet dots → flat pure yellow** (commit `1a83089`, pushed fork `design-draft-local` per PUSH POLICY, no force). User: "把这里的小圆点，改成纯黄色，不要渐变，不要投影" + screenshot (red boxes = the `<li>` bullet markers in the `Web APP 平台版` / `APP 平台版` demo-edition cards). The dots = `.demo-edition-bullets li::before` in **`src/app/[locale]/demo/demo-page.css` only** (~443-444). FIX = 1 declaration block: removed `background: linear-gradient(135deg,#FFD95A 0%,#C07F00 100%)` + `box-shadow: 0 0 8px rgba(255,217,90,.5)` → `background: #FEBF1D` (site brand gold, flat). Same selector/size (8×8 circle) otherwise untouched. CSS-only, no TSX → no tsc. To revert: restore the gradient+box-shadow pair. **NOT VISUALLY VERIFIED** (autodebug needs user preview) — robust-by-construction. ───── PRIOR: **b2c (`/[locale]/b2c`) "强大功能" (`#features`) feature cells: illustration tile removed, icon moved left of the title, body left-aligned** (commit `4928f3a`, fork `design-draft-local` per PUSH POLICY, no force). User: "把这里黄框的插图容器去掉，图标放到小标题的左边，内容与小标题左对齐" + screenshot (yellow boxes = the bottom `.feature-card-icon` Moss "product visual" tiles under each of the 6 cells). FIX = **`src/app/styles/sections.css` only**, no JSX (DOM order stays icon, h3, p): `.b2c-feature-cell` flex-column → **CSS grid** `grid-template-columns:auto minmax(0,1fr); grid-template-rows:auto auto; column-gap:16px; row-gap:10px; align-items:start` (padding 48px 40px kept). `.feature-card-icon` → `grid-column:1; grid-row:1`, **tile stripped** (`background:none; border:0; border-radius:0; min-height:0; width/height:36px`; inner svg 64→32px) — now a plain icon left of the title. `h3` → `grid-column:2; grid-row:1`; `p` → `grid-column:2; grid-row:2; margin:0` (was `order`-based flow w/ `10px 0 28px`) so the description is left-aligned under the title (col 2), not under the icon. Dark mirror `[data-theme="dark"] .feature-card-icon` gradient/border → `background:none; border:0`. All scoped `.b2c-feature-*` (b2c-only, no cross-route bleed); 600px single-col + 32/24 pad rules still apply. /zh/b2c HTTP 200, class served; CSS-only → no tsc. NOTE: cell SVG strokes use `#181818` (was OK inside the old light tile) — on dark mode the mono strokes are now low-contrast on the page bg (gold `#FEBF1D` accent paths still show); pre-existing-ish, left as-is (out of scope). To revert: restore the prior `.b2c-feature-cell` flex-column + `.feature-card-icon` 168px tile block + `p{order:2;margin:10px 0 28px}` + the dark gradient mirror (see commit `4928f3a^`). **NOT VISUALLY VERIFIED** (autodebug needs user preview) — robust-by-construction. ───── PRIOR: **b2c (`/[locale]/b2c`) 智能运营中心 (`product.ops`) yellow-box illustration replaced** (commit `a5e7fdc`, pushed `e93be43..a5e7fdc` fork `design-draft-local` per PUSH POLICY, no force). User gave a new Order-Total/Business-dashboard art + a screenshot of the yellow-bordered box as the target. The yellow box = the b2c Smart Operations Center `feature-row reverse` whose `<Image fill>` at `b2c/page.tsx:327` points to `/images/smart-ops-dashboard-2.png` (container `aspectRatio:16/10`, `objectFit:cover`, `objectPosition:center`, gradient `#F1F2F4→#FFF` bg, `borderRadius var(--radius-md,14px)`). Replaced the file in place (`cp` over `public/images/smart-ops-dashboard-2.png`, 176996→189411 B; new PNG 1332×1082) — NO code/JSX/CSS change. No tsc (no TSX edit). **NOT VISUALLY VERIFIED** (autodebug needs user preview). To revert: `git show e93be43:website/frontend/public/images/smart-ops-dashboard-2.png` (or any pre-`a5e7fdc` rev) and restore the blob. ALSO this session (b2c hero, separate commits, all on fork): `37ae708` — `.product-hero` `min-height` `calc(100svh - 42px)` → `80svh` → finally **`calc(72svh - 42px)`** (`pages-extracted.css` ~line 29; user "缩小展示高度" then "再缩小一点点"; shared by b2b; revert by restoring `100svh`). ───── PRIOR: **Pricing (`/[locale]/pricing`) "完整功能对比" yellow comparison-table header row shortened** (commit `1205e35`, in sync on fork `design-draft-local` per PUSH POLICY — push initially rejected because the ref had concurrently advanced to `37ae708` [a separate b2c `product-hero` min-height 80→72svh tweak not made this session]; `git fetch` confirmed `fork/design-draft-local == local main`, `1205e35` already on remote, no force). User: "缩小一点点功能一栏的高度" + screenshot with the yellow-bordered table header (the `<thead>` of `.comparison-table` on the pricing page, `pricing/page.tsx:324-342`). The row's tallness is entirely from `.comparison-table th` padding. FIX = 1 rule, **`src/app/styles/sections.css` only** (~3863-3864): `padding-top: 24px → 20px`, `padding-bottom: 80px → 56px` (≈28px shorter row). Left `.comparison-table tbody tr:first-child td{padding-top:32px}` (~3872) intact so the first data row keeps its breathing room below the yellow band. CSS-only, no TSX → no tsc. To revert: restore `padding-top:24px; padding-bottom:80px`. **NOT VISUALLY VERIFIED** (autodebug needs user preview) — robust-by-construction. ───── PRIOR: **b2c (`/[locale]/b2c`) hero ("首屏") vertically centered to kill the top-heavy blank band** (commit `088868f`, pushed `f47d5de..088868f` fork `design-draft-local` per PUSH POLICY, no force). User: "首屏的高度适配调节下，现在太空白了" + screenshot showing content stuck at top under SubNav and a huge void above the fold-pinned stats bar. ROOT CAUSE: `.product-hero` is `min-height:calc(100svh - 42px)` flex-column; `.container` (60px top pad) sat flush at top while `.hero-footer-bar` is `margin-top:auto` pinned to the fold, so ALL the 100svh slack collapsed into one band between content and stats. FIX = 1 rule, **`src/app/styles/pages-extracted.css` only** (added right after the `.product-hero>.hero-footer-bar` rule, ~line 53): `.product-hero>.container{flex:1 1 auto; display:flex; flex-direction:column; justify-content:center}` — container grows to eat the slack and centers `.hero-content` in it, splitting whitespace evenly above/below; footer stays exactly at the fold (margin-top:auto now redundant-but-harmless). CSS-only, no TSX → no tsc. **SHARED:** `.product-hero` is also the b2b hero (same `container + hero-footer-bar` structure) — b2b gets the same balanced centering, consistent & intended. Short-vh safe (section is min-height/height:auto, no clip). To revert: delete that one `.product-hero>.container` block. **NOT VISUALLY VERIFIED** (autodebug needs user preview) — robust-by-construction. ───── PRIOR: **b2c (`/[locale]/b2c`) "您的品牌，您做主" (`product.brand`) yellow-box illustration replaced** (commit `2ba9637`, pushed `f389a17..2ba9637` fork `design-draft-local` per PUSH POLICY, no force). User gave a new dual-phone CORE/IZI-charge art + a screenshot of the yellow-bordered editor box as the target. The yellow box = the b2c brand `feature-row reverse` whose `<Image>` at `b2c/page.tsx:250` points to `/images/OEM.png`. Replaced the file in place (`cp` over `public/images/OEM.png`, 192603→179011 B) — NO code/JSX change, same path/dims (`width=1920 height=1080`, `objectFit:cover`, `borderRadius:14`). No tsc needed (no TSX edit). **NOT VISUALLY VERIFIED** (autodebug needs user preview). To revert: `git show f389a17:website/frontend/public/images/OEM.png` (or any pre-`2ba9637` rev) and restore the blob. ───── PRIOR: **b2b page (`/[locale]/b2b`, "SaaS平台版运营模式 B2B2C") gold-mono palette unification with the homepage** (commit `f0c0299`, pushed to fork `design-draft-local` per PUSH POLICY — clean fast-forward `2ccd94e..f0c0299`, no force). User: "把产品页里的'SaaS平台版运营模式(B2B2C)'整一页的风格按照首页的样式风格去统一". Explored both pages (Explore agent) → b2b diverged via a multi-accent palette (blue #0369A1 / teal #0F766E / green #10B981 / amber #F59E0B in arch diagram, support matrix, demo card, payment-flow gradient) vs the gold-mono homepage. AskUserQuestion (3 tiers: palette-only / +hero / full-restructure) → user chose **"Palette + surfaces (CSS-only)"** — recolor to gold, KEEP every section, order, the split video hero, the SubNav and all bespoke diagrams; NO JSX restructuring. FIX = 1 file, **`src/app/styles/pages.css` only**, 5 edits converting `--accent`/color tokens to homepage gold (`#FEBF1D` / `#E6A817` / `rgba(254,191,29,0.1-0.12)` / `rgba(254,191,29,0.28-0.3)`), matching the already-gold `[data-role="platform"]` & `[data-accent="gold"]` blocks so the `var(--accent)` cascade propagates: (1) `.b2b-arch-box[data-role="operator"]` green→gold + `[data-role="user"]` amber→gold (grid pos untouched); (2) `.b2b-arch-center-inner span:nth-child(1|2|3)` hub labels → all `#FEBF1D`; (3) `.support-col[data-accent="rose"]` blue→gold + `[data-accent="emerald"]` teal→gold (JSX `data-accent="rose|emerald"` LEFT AS-IS — now just resolve to gold, preserves CSS-only contract); (4) `.b2b-demo-card[data-accent="blue"]` #0369A1→#FEBF1D; (5) `.b2b-payment-flow-wrap` gradient green end-stop `rgba(16,185,129,0.05)` → `rgba(254,191,29,0.06)` (symmetric gold→white→gold). Verified NO other non-gold accents in the b2b CSS range 1451-2800 — the only leftover (pages.css ~2795-2812 `.b2c-demo-pill[data-tone="success"|"teal"]`) is the **b2c** page's status pills (OUT OF SCOPE, intentionally left; green = semantic "online" status). `b2b/page.tsx` inline styles already used `var(--primary|--text-*|--border-*)` only — no inline edits. b2b hero already gold (`.text-gradient`); `.mesh-bg`/`.particles-bg` (polish.css) no non-gold tint. /en/b2b + /zh/b2b HTTP 200 on dev :8000 w/ classes in served HTML; no TSX change so no tsc. **NOT VISUALLY VERIFIED** (autodebug needs user preview) — robust-by-construction (token-cascade recolor, reuses proven homepage gold). NOT-chosen tiers (for later): "+hero" = center the b2b hero like homepage; "full-restructure" = glass-cards→image-cards, drop SubNav, flatten section rhythm. Some role/accent selectors now value-identical (harmless dup, kept for clarity / easy future re-divergence). ───── PRIOR: **b2c "强大功能" (`#features`) restyled to mirror getmoss.com's feature grid** (user gave a Moss screenshot + the yellow-box screenshot as the target for "这一屏的样式 / 黄色框的布局内容和样式"). Two files, CSS-only + one className: (1) `b2c/page.tsx` — the features `<div className="section-header">` got the extra class `b2c-feature-head` (no other JSX change; the 6 cells keep DOM order `icon, h3, p`). (2) `sections.css` `.b2c-feature-cell` block rewritten: cell is now `display:flex; flex-direction:column`; the visual order is reflowed via flex `order` WITHOUT touching JSX — `h3{order:1}` (Moss title: 1.05rem/600/#181818, **green `+` `::before` removed**), `p{order:2}` (Moss body: 0.9rem/400, normal-case, line-1.6, #6c717a — was tiny uppercase stat caption), `.feature-card-icon{order:3}` turned into the bottom "product visual" tile (`margin-top:28px; min-height:168px; flex-center; border-radius:14px; bg linear-gradient(150deg, rgba(254,191,29,.10), rgba(0,0,0,.03)); 1px rgba(0,0,0,.05) border; inner svg 64×64`). Cell pad 52→48. Added dark mirrors for h3/p/visual + new `.b2c-feature-head` rule (left-aligns the header — `text-align:left; max-width:820px; margin:0 0 44px`; h2 `clamp(2rem,3.4vw,2.9rem)/700/lh1.15`; p 1.05rem/max-560; label mb 18). All scoped `.b2c-feature-*`/`.b2c-feature-head` — b2c-only, no cross-route bleed. `npx tsc --noEmit` CLEAN. **NOT VISUALLY VERIFIED** (autodebug needs user preview). To revert the Moss look: restore the prior `.b2c-feature-cell` block (stat-band: `.feature-card-icon` was a 14px-margin top icon, h3 had green `+` `::before`, p was 0.72rem uppercase) and drop the `b2c-feature-head` class+rule. NOTE: the cell SVGs are now decorative tiles, not topic icons — if real product screenshots are wanted later, swap the `<svg>` inside `.feature-card-icon` for `<Image>`. FOLLOW-UP (same session): tiles now **bottom-align across each row** — `.feature-card-icon` margin `28px 0 0` → `auto 0 0` (margin-top:auto pins to cell bottom; cells are equal-height grid items), and the 28px min text↔tile gap moved onto `.b2c-feature-cell p` `margin: 10px 0 28px`. FOLLOW-UP 2: **removed the hero 6-card feature grid** in Section 1 — the `{/* Feature Grid */}` `<ScrollAnimation><div className="grid grid-3" style={{marginTop:56}}>` block (6 `card glass-card tilt-card` cells: `product.f1..f6.*`) that sat between the hero CTA buttons and the stats bar, per user ("去掉这里的内容"). `product.f1..f6.*` i18n left intact (unused now); `ScrollAnimation` import still used by `#features`. `npx tsc --noEmit` CLEAN. To restore: `git show` the pre-removal commit. ───── PRIOR: **REVERTED** the sticky stacking-card experiment (`git revert` of `735c46b`; working tree now identical to `0ea06c7`). User asked to undo it. Removed: `b2c/StackCards.tsx` (deleted), the `<StackCards>`/`<article className="b2c-stack-card">` wrapper in `b2c/page.tsx` (four `product.ui|brand|app|ops.*` highlights are back to their original `<ScrollAnimation>`-wrapped `feature-row`/`feature-row reverse` rows ~260-408), and the `.b2c-stack*` CSS block in `sections.css`. `npx tsc --noEmit` CLEAN. DO NOT reapply the stacking deck unless asked again — if revisited, the approach was: pure-CSS `position:sticky` pile + a tiny rAF JS shrink/dim of the pinned card as the next rises (progressive enhancement, ≤900px/reduced-motion no-op) — see `git show 735c46b` for the full impl. PRIOR `0ea06c7`: removed the b2c "强大功能" band top/bottom gradient bands (user read them as a shadow) — `.b2c-feature-lined` now plain `border-top`/`border-bottom: 1px solid rgba(0,0,0,0.09)` (dark mirror .1), no `::before/::after` bands. PRIOR `4b099b4`: b2c "强大功能" cells → Moss stat-band look (CSS-only in `sections.css` `.b2c-feature-cell`: `.feature-card-icon{display:none}` — SVG kept in `b2c/page.tsx` for revert; `h3` big `clamp(1.9rem,2.6vw,2.5rem)` weight-500 w/ green `::before "+"` #3fae59; `p` tiny uppercase 0.72rem/600/0.09em `--text-tertiary`; cell pad 44→52). PRIOR `91f0b4e`/`8abb264`/`d045cde`: b2c "强大功能" made full-bleed lined-grid (100vw escape + 2×`.b2c-feature-row`, padding-inline content re-align; KNOWN RISK: 100vw may add a horizontal scrollbar if body lacks overflow-x:clip — unverified) / earlier restyled `grid grid-3` cards → hairline lined grid / removed the UI-custom + zero-config two-card block (`product.uicustom.*`/`product.zeroconfig.*` i18n left intact, unused). `.b2c-feature-*` is b2c-only. PRIOR (context): b2c "系统演示" section restyled to mirror the /demo platform-editions shell (`b2c/DemoTabs.tsx` rewritten + `.b2c-demo-*`/`.b2c-qr` replica in `sections.css`, `.b2c-demo-stage` max-width 800→1100 in `pages.css`; reused existing i18n only, EN/ZH in sync; b2c-only, no cross-route bleed). NOT VISUALLY VERIFIED across these (autodebug needs user preview+debug); robust-by-construction.

  **PUSH POLICY (settled):** push every commit with `git push fork main:design-draft-local`. Local `main` → fork branch `design-draft-local`. NEVER force-push or touch fork `main` (it holds 54 security-pin/contact-page commits that diverged from local at `8a4d15c` on 2026-05-11; local-only design work is the 400+ commits). `origin` = `anindya127/Design-draft` also diverged — don't push there.

  **Current b2c `/[locale]/b2c` state (3 same-day changes, all live):**
  1. `a1d0d59` — moved the `{/* APP / H5 */}` ScrollAnimation block to sit right after the "Your Brand"/`product.brand` `feature-row reverse` block and before `{/* Exclusive UI Customization & Zero-Touch Config */}`; its wrapper `marginTop` is `40`. Section order in Section 4 `.container`: ui → brand(reverse) → APP/H5 → uicustom/zeroconfig grid-2 → Payment Matrix → 20+ Languages(#multilingual) → Smart Ops.
  2. `385ca3a` — sized the slideshow box to match the brand image above: `src/app/styles/sections.css` `.feature-image-placeholder:has(.app-slideshow)` now `aspect-ratio:4/3` (was `aspect-ratio:auto; min-height:520px`), plus phone-fit sub-rules `.app-slideshow{container-type:size; padding:8px 0}`, `.app-slideshow-track{width:auto; height:calc(100cqh - 36px)}` (cqh-responsive, derives width from base `aspect-ratio:9/19.5`), `.app-slideshow-dots{margin-top:10px}`. If phone too small → bump `100cqh - 36px` toward `- 20px` (don't change the 4/3 — it intentionally matches the row above).
  3. `28f1535` — (a) same `:has(.app-slideshow)` rule `background:transparent` → `#F1F2F4` (rounded card matching the OEM "Your Brand" row above). (b) Slide mechanic changed from cross-fade to a real horizontal carousel: added `.app-slideshow-screen{position:absolute;inset:10px;border-radius:40px;overflow:hidden;z-index:1}` (clip box inside the phone bezel — the `.app-slideshow-track` bezel keeps `overflow:visible` so its `::after` dynamic-island notch at z-index:3 stays over the screen) + `.app-slideshow-rail{display:flex;width/height:100%;transition:transform 600ms cubic-bezier(.16,1,.3,1)}`; `.app-slideshow-slide` is now `flex:0 0 100%` (no more absolute/opacity); reduced-motion now kills `.app-slideshow-rail` transition. `AppSlideshow.tsx`: slides wrapped in `<div class="app-slideshow-screen"><div class="app-slideshow-rail" style={{transform:translateX(-index*100%)}}>…</div></div>`, no per-slide `is-active` class. Auto-advance 2000ms + dots unchanged. `npx tsc --noEmit` PASSES.
  4. `035c84c` — fixed the wrap quirk: `AppSlideshow.tsx` now appends a clone of `SLIDES[0]` after the 9 real slides (`CLONE_INDEX = SLIDES.length`); `index` increments without modulo (`advance` = `i+1`), and an `onTransitionEnd` on the rail detects `index === CLONE_INDEX` → sets `animate=false` + `index=0` (rail jumps back to slide 0 with `transition:'none'` so it's invisible), then a double-`requestAnimationFrame` effect re-enables `animate` for the next move. Dots use `realIndex = index % SLIDES.length`. Result: loop slides forward seamlessly, no backward sweep. `npx tsc --noEmit` PASSES.
  5. `f00dc5e` — scaled the iPhone frame to the shrunken phone (fixed px bezel/radius/island looked oversized in the 4:3 box). Added scoped overrides in the `:has(.app-slideshow)` block using `cqh` (1% of `.app-slideshow` box height ≈ phone height): `.app-slideshow-track{border:0.3cqh solid #2a2a2a;border-radius:7.5cqh;padding:1.3cqh}`, `.app-slideshow-screen{inset:1.3cqh;border-radius:6.2cqh}` (inset must equal track padding so screen sits flush in bezel), `.app-slideshow-track::after{top:2.2cqh;width:13cqh;height:4cqh}` (island; border-radius stays 999px pill). Base px rules at sections.css ~486-518 untouched (fallback). To tune: thinner bezel→lower the two `1.3cqh`; less round→lower `7.5cqh`/`6.2cqh`; smaller island→lower `13cqh`/`4cqh`. Pure CSS.

  6. `4ed922d` — (HOMEPAGE `/[locale]`, not b2c) user asked to move the hero text block (label/title/desc/buttons/stats) up a bit. `pages-extracted.css`: `.hero.hero-centered` top padding extra `clamp(6px,1.2vh,20px)` → `clamp(2px,0.5vh,8px)` (the `72px +` fixed-header buffer KEPT), and `.hero-centered-inner` top padding `clamp(14px,2.4vh,36px)` → `clamp(4px,0.8vh,12px)`. Net ≈ 20–36px higher. Mobile/short-vh overrides at ~2565/2589 unchanged. Pure CSS; if user wants it higher still, trim those two clamps further (don't touch the `72px`).

  7. `004c425` — (HOMEPAGE) widened the gap between the 3 hero stats and the dashboard illustration below: `pages-extracted.css` `.hero-dashboard-mock` top margin `clamp(4px,0.8vh,14px)` → `clamp(12px,2vh,26px)` (~+8–12px). The total stats↔mock spacing also includes `.hero-centered-inner{gap:clamp(10px,1.6vh,20px)}` — bump that too if more is wanted. Pure CSS.

  8. `75eae7c` — (b2c) moved the `{/* Smart Operations Center */}` block (`product.ops.*`, `feature-row reverse`, `smart-ops-dashboard.png`, `marginTop:40`) from the END of Section 4 (it was after the `20+ Global Languages`/#multilingual block, last before `</div></section>`) to right AFTER the `{/* APP / H5 */}` block and before `{/* Global Payment Matrix */}`. New Section-4 order: ui → brand(reverse) → APP/H5(feature-row) → Smart Ops(feature-row reverse) → Payment Matrix → 20+ Languages(#multilingual). Pure JSX reorder, no i18n/CSS change; `npx tsc --noEmit` PASSES. To undo: move the same block back to just before the Section-4 closing `</div>`.

  **NOT VISUALLY VERIFIED** (autodebug needs user to open preview + enable debug) — all changes are robust-by-construction but unconfirmed in-browser.

  **Older still-relevant flags (do NOT reapply unless asked):** homepage hero `.hero.hero-centered .hero-centered-inner` uses `url('/images/hero-grid-bg.png') cover` — a CSS dot-grid was tried then REVERTED (`6ed2f2b`). b2c `.product-hero::before` light-mode dot field IS live (`d692ec4`, gold `rgba(214,158,0,0.55)` 2.5px/24px; pale-yellow on `#F1F2F4` ≈ invisible — use darker gold ≥0.4 alpha for texture on that bg); same selector also styles the b2b hero. Homepage yellow nav bar was reverted (`bcbd0aa`); `logo-globe.png` recoverable via `git show 8fef934`. b2c `.product-hero` is `min-height:calc(100svh - 42px)` flex-column with `.hero-footer-bar{margin-top:auto}` pinning the 3 stats to the fold (b2b shares `.product-hero` — verify if touching).
