# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-15 (homepage hero: Arcadia-style centered layout, locked to first-fold fit): two-iteration rebuild of the home hero. ITERATION 1 swapped layout from "left text + right 3D charger" to "centered text + dashboard mock below". ITERATION 2 (this turn) tightened it so title + dashboard + manufacturer marquee fit within one viewport (no scroll required to see logos), matching reference `/tmp/claude-paste-1778832795237-b9d5aacc.png` (Arcadia full-page). FILES: (1) `public/images/dashboard-hero.jpg` — NEW asset, the Global-Charge admin dashboard screenshot (3840x2160 JPEG, ~1.4MB, with left sidebar nav, Device Status pill cards (Unavailable/Offline/Occupied/Charging/Available/Idle/Total 10), Business Overview tab w/ Today's Charging Amount 222.66kWh + Orders 32 + chart Oct 25-Nov 4, right rail with To-Do/Notifications/Quick-Config/APP-Download QR). (2) `src/app/[locale]/page.tsx` — REMOVED the `<Image hero-bg-charger.jpg fill>` background entirely. Class chain on `<section>`: `hero hero-with-video hero-centered` (KEPT `hero-with-video` because `components.css:123-169` `body:has(.hero-with-video) .header...` rules wire the transparent header look on home — drop the class and the header breaks). Inner wrapper renamed `hero-with-video-grid` → `hero-centered-inner`. JSX flow: `.hero-content > .hero-text` (badge + H1 + desc + buttons), then SIBLING `.hero-dashboard-mock > .hero-dashboard-frame > <Image src=/images/dashboard-hero.jpg width=1920 height=1080 priority sizes="(max-width: 1200px) 92vw, 1120px" className=hero-dashboard-img>`. The `.hero-stats` block (Downloads 9999 / Chargers 1069+ / Countries 100+) was REMOVED entirely — it pushed the dashboard below the fold and the manufacturer marquee already serves as a trust signal. Also removed the now-unused `import CounterAnimation` (its only consumer was the stats block). All translation keys reused (`t('hero.badge|title1|desc|cta1|cta2')`) — NO i18n changes. TypingText words unchanged. (3) `src/app/styles/pages-extracted.css` lines 2330-2484 — APPENDED a `~150-line` centered-hero block. KEY RULES: `.hero.hero-centered::after { display: none !important; content: none !important }` — must be first to kill `sections.css:1249` `.hero-with-video::after` which baked a fade-to-#0a0a0a designed for the old dark hero (would paint a 96px-tall BLACK BAND over our cream bg). `.hero.hero-centered { background: cream gradient with top gold halo + bottom-right green tint; min-height: calc(100svh - 72px - 221px); padding: clamp(24px,3.5vh,48px) 0 clamp(20px,2.5vh,36px); display: flex; align-items: center }` — the `min-height` calc is THE LOAD-BEARING TRICK: 100vh minus header (72) minus trusted-bar `min-height: 221px` from `pages.css:2856` = exactly the room the hero needs to fill so all three together = 100vh on any screen. Used `svh` to handle mobile URL-bar shift. `.hero-centered-inner { display: flex; flex-direction: column; align-items: center; gap: clamp(16px,2.4vh,28px); min-height: 0 }` — tight gap (was 40-72px in iteration 1). `.hero-content { align-items: center; text-align: center; max-width: 880px }` — overrides `sections.css:81-86` `.hero-with-video .hero-content { align-items: flex-start; text-align: left; max-width: min(640px, 100%) }` (specificity 0,3,0 > 0,2,0 + later in cascade order). Title: `font-size: clamp(1.875rem,4vw,3rem)`, `line-height: 1.1`, `letter-spacing: -0.02em`, `margin-bottom: 12px !important` (the `!important` is REQUIRED because `sections.css:1596-1599` `.hero-title { margin-bottom: 20px }` is same-specificity AND the JSX has inline `style={{marginBottom: 16}}` — without `!important` the spacing won't budge). Desc: `max-width: 640px; margin: 0 auto 20px; font-size: clamp(0.95rem,1.05vw,1.0625rem); line-height: 1.55`. Buttons centered, no bottom margin. THE KEY DASHBOARD-SIZING TRICK: `.hero-dashboard-mock { width: min(1080px,92vw); flex: 1 1 auto; min-height: 0; display: flex; max-height: clamp(280px,50vh,540px) }`. The `flex: 1 1 auto` makes the mock consume all remaining hero vertical space after text/buttons; `max-height: clamp(280,50vh,540)` caps it so tall screens don't yawn into a 700px+ banner-of-dashboard-top. `.hero-dashboard-frame { width: 100%; height: 100%; border-radius: 16px; bg #fff; subtle border + drop-shadow + inner highlight; overflow: hidden }` — no aspect-ratio (height comes from the flex parent). `.hero-dashboard-img { width: 100%; height: 100%; object-fit: cover; object-position: top center; border-radius: 14px }` — the natural 16:9 source image fills the frame, cropped from the bottom so only the top half (sidebar + Device Status cards + Business Overview chart top) is visible. KEY VIEWPORT MATH: header 72px + hero `calc(100vh - 72px - 221px)` + trusted-bar 221px = 100vh on ANY height. 900px screen: hero=607px, text content ~282 (badge+H1+desc+buttons+gaps), leaves ~325 for dashboard → flex:1 fills it → renders 1080×325 frame, image cropped top-half. 1080px screen: hero=787, leaves ~505 → 50vh = 540 close to cap → ~505px tall. 1440px screen: hero=1147, leaves ~865 → max-height: 540 wins → dashboard locked at 540px, 327px of hero whitespace centered via flex align-items:center. MEDIA QUERIES: `@media (max-height: 820px) and (min-width: 721px)` for short-laptop windows: hero padding `16px 0 16px`, title `clamp(1.5rem,3.2vw,2.25rem)`, desc margin-bottom 14px, inner gap `clamp(10px,1.4vh,18px)` — squeezes text content so dashboard still gets room. `@media (max-width: 720px)` mobile: `min-height: auto` (release fold-fit since mobile is expected to scroll), padding `56px 0 40px`, `.hero-dashboard-mock { width: 100%; max-height: 320px; flex: 0 0 auto }` (override flex-fill so it has a fixed-ish size), `.hero-dashboard-frame { aspect-ratio: 16/11; height: auto }` (back to fixed aspect since we're not flex-filling). Also tightened `.trusted-bar { padding: 32px 0; .trusted-label { margin-bottom: 18px; font-size: 15px } }` — but note: `.trusted-bar` still has `min-height: 221px` from `pages.css:2856` and `display: flex; align-items: center`, so padding alone won't reduce its envelope; the override is purely cosmetic to balance the marquee within that fixed 221px box. CASCADE/SPECIFICITY VERIFIED: every overriding selector either has higher specificity OR same specificity with later source order (`pages-extracted.css` loads after `sections.css` and `polish.css` per the global CSS import order in `app/globals.css`). DEAD CSS LEFT: the now-unused `.hero.hero-centered .hero-stats / .hero-stat / .hero-stat-value` rules from iteration 1 remain but no DOM matches — harmless. `hero-bg-charger.jpg` still exists in `public/images/` (other pages may reference it; quick grep didn't find any but didn't audit exhaustively). `hero-with-video-grid` class no longer rendered (renamed to `hero-centered-inner`) so `sections.css:90-103` grid rules are dead. CONFIRMED via curl: `hero-centered` 4x, `hero-dashboard-mock` 2x, `dashboard-hero.jpg` 2x, ZERO `hero-stats`, ZERO `hero-with-video-grid`. CSS compiled clean (`max-height: clamp` + `.hero-dashboard-mock` rules present in built chunk). NOTE TO FUTURE CLAUDE: if user wants the dashboard TALLER (more of the chart visible), bump `max-height: clamp(280px, 50vh, 540px)` upper bound 540 → 640 (and/or middle 50vh → 58vh) — but verify it still fits the first fold at 900-1080px screen heights. If user wants MORE of the dashboard visible (not just top half), change `object-position: top center` → `center center` (crops top+bottom equally) or supply a different source image at a wider native ratio (24:9 dashboard crop). If user wants the trusted-bar SHORTER (so hero gets more vertical room), edit `pages.css:2856` `.trusted-bar { min-height: 221px }` down (e.g. 160px) AND update my hero `min-height: calc(100svh - 72px - 160px)` to match — they MUST track together or the math breaks. If user re-adds stats, they'll push the dashboard out of the fold again — either move stats below the manufacturer marquee in a NEW section, or make them inline next to the buttons (less elegant). If user wants the TypingText accent gone (Arcadia just has static text), swap `<TypingText words={[...]}/>` for a plain `<span>` with the desired phrase. Header transparency on home is preserved by keeping `hero-with-video` class on `<section>` — DO NOT drop that class without also rewriting `components.css:123-169` `body:has(.hero-with-video) .header...` selectors. PRIOR (different session): b2c features icons redrawn — viewfinder + bolt for 扫码即充, terminal window + cursor for OCPP 可视化命令 — see commit log.
