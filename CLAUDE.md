# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-14 (matrix CPMS image shrink): user attached `/tmp/claude-paste-1778729577452-58c30869.png` with a red box around just the CPMS card's image and said "红框的图片再小一点" — scoped the size reduction to the CPMS card only. Added a CPMS-specific override at `pages.css` ~line 1305: `.matrix-column-cpms .matrix-column-figure { flex: 0 1 360px; max-width: 360px }`. The shared `.matrix-column-app .matrix-column-figure { flex: 0 1 440px; max-width: 440px }` rule still governs the APP/H5 card. Net effect: APP/H5 image stays at 440px wide, CPMS image now 360px (80px narrower). With `object-fit: cover` still active, the CPMS image now shows even less of the leftmost portion of its 1253×651 composition; right side (Fault Alerts / OTA Firmware Updates / Analytics Dashboard labels) remains the visible part. NOTE TO FUTURE CLAUDE: if user wants APP/H5 also shrunk, change the shared rule's 440 → smaller (or add `.matrix-column-app:not(.matrix-column-cpms)` specificity). If user wants CPMS smaller still, drop the override to 320/300. The mobile breakpoint `.matrix-column-app .matrix-column-figure { flex: 1 1 auto; margin: 0; height: 200px }` still applies on both cards on narrow viewports (the CPMS-only override sits in the desktop scope, not in the @media block). PRIOR (same session):

- 2026-05-14 (payment matrix FOLLOW-UP: home-screen phone + bottom-fade mask + white frosted-glass bubbles + bigger logos): user attached `/tmp/claude-paste-1778729296605-c39cd3b3.png` (AstroPay screenshot annotated with a red box around the phone's bottom-fade zone) and said "中间手机界面显示首页，然后底部做渐变消失的效果，参考刚刚发的参考网站...红框的图，然后logo的色块用白色磨砂玻璃效果展示，logo也放大一点" — four coordinated changes to the existing orbit layout from earlier this session. (1) PHONE IMAGE: `page.tsx` ~line 460 swapped `src="/images/App_login_iphone.png" width={293} height={604}` → `src="/images/Mobile_home.png" width={1500} height={3248}` — Mobile_home.png is the GCSS app home screen (Scan & Charge banner, Order CTA, Charger View / Charging Package / My Car / Profile cards) sized 1500×3248. Aspect ratio ~0.462:1; at 280px wide renders ~606px tall. The image also includes the iOS status bar at top (9:41, signal/wifi/battery). Sizes hint `(max-width: 768px) 180px, 280px` updated to match new desktop width. (2) BOTTOM FADE: added `-webkit-mask-image: linear-gradient(to bottom, #000 0%, #000 60%, transparent 95%)` + unprefixed `mask-image` to `.payment-orbit-phone` (`sections.css:2604-2605`). The mask is on the WRAPPER not the img so it composes correctly with the `filter: drop-shadow(...)` rules already on the wrapper. Top 60% of the phone is fully opaque, fades to transparent between 60-95%, fully transparent in the bottom 5%. The mask is applied to the WRAPPER which means the drop-shadow filter still draws around the visible (non-faded) portion — gives a clean "phone emerging from below" feel matching the AstroPay reference where the phone bottom dissolves into the background. (3) WHITE FROSTED GLASS BUBBLES: `sections.css:2611-2660` rewrote `.payment-bubble` from cream gradient (`linear-gradient(155deg, #FFFBF0 0%, #F5EBC8 100%)`) → translucent white + backdrop blur: `background: rgba(255, 255, 255, 0.55); backdrop-filter: blur(12px) saturate(140%); -webkit-backdrop-filter: blur(12px) saturate(140%); border: 1px solid rgba(255, 255, 255, 0.6); box-shadow: 0 8px 20px rgba(140,95,0,0.08), 0 2px 6px rgba(0,0,0,0.04), inset 0 1px 0 rgba(255,255,255,0.9)`. The frosted-glass effect blends the section background through the bubble for that "magnified glass" look. Hover state bumped to `background: rgba(255, 255, 255, 0.75)` for a slightly more opaque hover. Dark-mode override: `background: rgba(40,32,22,0.55)` (translucent dark warm-brown). Added `background 380ms ease` to the transition list. (4) LARGER LOGOS: `.payment-bubble-inner { width: 62%; height: 62% }` → `width: 76%; height: 76%` (`sections.css:2670-2671`). Bumps logo content area from ~62% to ~76% of the bubble diameter (e.g. on an 80px bubble, inner goes from 50px → 61px). The icons (PaymentLogo SVGs + next/image PNGs) auto-scale to fill the inner box thanks to the existing `.payment-bubble-inner > *` and `.payment-bubble-inner svg/img` rules at 100% width/height + object-fit:contain. PHONE SIZE: desktop `.payment-orbit-phone { width: 260px }` → `280px` (+20px to give the home-screen UI more readable size at the centerpiece). Mobile breakpoints bumped to match: 900px breakpoint phone width 180px → 200px, 640px breakpoint 140px → 160px. Orbit container heights (720/560/460) unchanged. NOTE TO FUTURE CLAUDE: if user wants the phone fade to start LATER (more of the phone visible), change `60%` stop to e.g. `70%`. If user wants fade to be MORE GRADUAL, widen the band (e.g. `40% → transparent 100%`). If user wants NO fade, remove the mask-image rules entirely. If user wants the bubbles MORE TRANSPARENT (glassier), drop `rgba(255,255,255,0.55)` to `0.4`; if user wants OPAQUE WHITE bubbles, change to `rgba(255,255,255,0.92)` and drop the backdrop-filter (cheaper render). If user wants the logos BIGGER STILL, push `.payment-bubble-inner` to `82%` or `86%` — but at >82% the larger logos (Mastercard / Discover at native 40×24 ratio) start touching the bubble border. If user wants the home screen REPLACED with something else, drop-in options: `Mobile_payment.png` (charging-orders list, 1186×2473) or `App.png` (general app shot). Avoid `App_login_iphone.png` since the user explicitly switched away from login. The `backdrop-filter: blur` requires browser support — Safari 9+, Chrome 76+, Firefox 103+; older browsers will fall back to the flat `rgba(255,255,255,0.55)` solid background which still reads as soft-white, just without the blur halo. PRIOR (same session, INITIAL): user attached `/tmp/claude-paste-1778728456453-5663c67b.png` (the current dense 8-col grid of 46 payment logos on a flat panel) and `/tmp/claude-paste-1778728482706-4844a45b.png` (AstroPay.com/payments hero — central iPhone surrounded by ~7 round bubble icons in a soft sage tint) and said "这一屏的支付方式布局，改成这种，https://www.astropay.com/payments这是参考图的别人的网站链接，这一屏的动态效果我也要学这个展示方式" — replaced the entire payment-logos grid with an orbital layout: a centered iPhone mockup with 46 circular floating bubbles scattered in left/right bands around it. WebFetch to astropay.com failed (socket closed) so the AstroPay layout/animations were inferred from the screenshot only (round soft-tinted bubbles, gentle continuous float). FOUR coordinated edits: (1) JSX (`src/app/[locale]/page.tsx` ~lines 440-485): replaced the `<div className="payment-logos">{PAYMENT_METHODS_FLAT.map(...)}</div>` block with a `<div className="payment-orbit" role="list">` containing a `<div className="payment-orbit-phone"><Image src="/images/App_login_iphone.png" width={293} height={604} sizes="(max-width: 768px) 160px, 260px" /></div>` centerpiece + the 46-bubble map. Each bubble is `<div className="payment-bubble payment-bubble-v{1,2,3}" style={{ left:%, top:%, width:px, height:px, animationDelay, animationDuration }}><div className="payment-bubble-inner">{PAYMENT_ICONS[name]}</div></div>`. The v1/v2/v3 modifier rotates through 3 float animation variants (i % 3). Positions are pulled from a NEW const `PAYMENT_BUBBLE_POSITIONS` array (added in `page.tsx` ~lines 37-86, just below the `StarIcon` const) — 46 entries split into LEFT BAND (x: 2-32%) and RIGHT BAND (x: 68-96%) to avoid the centered phone (which occupies x: 38-62%). Each entry: `{ x, y, size, delay, duration }` — sizes vary 56/60/64/72/76/80/84px for organic non-uniform feel; delays -0.2 to -2.8s and durations 5.4-7.2s for staggered motion. (2) CSS (`sections.css` lines 2582-2710): completely replaced the old `.payment-logos` 8-col-grid styles with new `.payment-orbit` + `.payment-bubble` rules. Key rules: `.payment-orbit { position: relative; width: 100%; height: 720px }` (desktop), responsive `height: 560px` at 900px breakpoint, `height: 460px` at 640px. `.payment-orbit-phone` centered absolutely at left/top 50% with `transform: translate(-50%, -50%)`, `width: 260px` (180/140 on smaller breakpoints), with a warm `drop-shadow(0 24px 48px rgba(140,95,0,0.18))` to ground it. `.payment-bubble { position: absolute; border-radius: 50%; background: linear-gradient(155deg, #FFFBF0 0%, #F5EBC8 100%); border: 1px solid rgba(192,127,0,0.10); box-shadow: 0 6px 16px rgba(140,95,0,0.10), 0 2px 4px rgba(140,95,0,0.06), inset 0 1px 0 rgba(255,255,255,0.8); transform: translate(-50%, -50%) }` — cream-gradient circular pill matching the rest of the site's light-cream tokens (same `#FFFBF0`/`#F5EBC8` family used in the GlobeVisualization wrapper). Dark-mode override flips the bubble bg to dark warm-brown gradient. (3) THREE float keyframe variants for organic motion: `paymentBubbleFloatA` (simple Y bob -10px), `paymentBubbleFloatB` (3-stop diagonal drift 4/-8 → -3/-4), `paymentBubbleFloatC` (Y bob -6px + 2deg rotate). All wrapped in `translate(-50%, -50%)` to preserve the bubble's absolute-positioning anchor. Hover state: `transform: translate(-50%, calc(-50% - 4px)) scale(1.08)` + lifted shadow + `animation-play-state: paused` (so hover reads cleanly without competing motion). Reduced-motion media-query disables all animation. (4) Removed orphaned mobile rules at `pages.css:4266-4273` (`.payment-logos { gap: 8px }` and `.payment-logos > div { font-size: 0.75rem; padding: 6px 12px }`) — no longer apply since the grid is gone. ICONS RENDER inside `.payment-bubble-inner` (62% × 62% of the bubble), which is a flex centerer with a universal `> *` rule forcing direct children (whether `<Image className="payment-logo-img">`, `<span className="payment-logo-svg">`, or `<svg>` fallback) to fill 100% × 100% with `object-fit: contain`. NESTED rule `.payment-bubble-inner .payment-logo-svg svg { width: 100% !important; height: 100% !important }` forces inline-SVG payment icons (Visa/Mastercard/etc) to scale to their span container. PHONE ASSET: used existing `/public/images/App_login_iphone.png` (293×604, clean iPhone showing the GCSS login screen) — chosen over `Mobile_payment.png` because the latter is a long screenshot (1186×2473) that would render too tall and dominate the section; the login-screen iPhone has the correct aspect for a centered phone mockup. NOTE TO FUTURE CLAUDE: if user wants MORE bubbles visible (e.g. add new payment methods to PAYMENT_METHODS_FLAT), append entries to PAYMENT_BUBBLE_POSITIONS — `i % .length` wrap handles the case where methods > positions, but bubbles will overlap if you exceed 46 without adding positions. If user wants the phone DIFFERENT (e.g. show payment UI rather than login), swap `src` to `/images/Mobile_payment.png` and bump width to 320+ to handle the taller aspect — but be aware that asset is much taller (~5x phone-frame aspect) and will need a clipping/masking wrapper. If user wants the bubbles BIGGER/SMALLER globally, tune the per-entry `size` values in PAYMENT_BUBBLE_POSITIONS, OR add a CSS `transform: scale(1.2)` on `.payment-bubble` (but this conflicts with the existing translate(-50%) anchor — would need to compound into each keyframe instead, mirroring the existing 640px mobile-breakpoint scale(0.7) pattern). If user wants the float MORE/LESS pronounced, tune the keyframe Y/X deltas at `sections.css` ~lines 2670-2685 (A: -10px, B: 4/-8 + -3/-4, C: -6px). If user wants STAGGER tighter/looser, the inline `animationDelay`/`animationDuration` come from the positions array — edit those entries. If user wants the bubbles SAGE-GREEN like the AstroPay reference (instead of cream), change the gradient to `linear-gradient(155deg, #E8EEE5 0%, #D8E0D4 100%)` and the border to `rgba(80,100,80,0.10)`. Mobile breakpoint at 640px scales all bubbles to 0.7 via override keyframes (since `translate(-50%, -50%)` is in the transform, scale must be appended to all three keyframe variants, not added as a separate transform — done correctly). The 46 positions are HAND-PLACED to avoid visual collisions; if you add more methods, eyeball-check overlap with the new entries (no algorithmic packing). DARK-MODE colors are inferred but unverified — the project's dark theme is rarely exercised, so test if user reports issues. The `<PaymentRequestForm />` below the orbit and the section header above are untouched. PUSH TO FORK: same `https://github.com` credential issue persists from prior sessions; commits land locally but `git push fork main` requires user-side credential helper / SSH remote / PAT-in-URL fix. ORPHANED but harmless: previous CSS rules for `.payment-logo` 3D-tilt effect, `.payment-logo::after` floor-shadow, and the old `paymentFloat` keyframe — all replaced (not re-added). Image asset `/public/images/Mobile_payment.png` (not used in this redesign) still on disk — kept for potential future use.
