# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-15 (`/demo` page hero — bg from warm yellow gradient → FLAT pure brand yellow `#FEBF1D`, with all decorative orbs/grid removed): user follow-up to the prior turn (where I'd set the `.demo-hero` bg to a `#FFF1A8 → #FFD95A` gradient + white radial overlay + retuned orbs/grid) said "颜色改的不对，改成纯黄色" — gradient is wrong, want pure flat yellow. Same phrasing pattern as the prior contact-icon "颜色是纯黄色，不要有渐变" turn — "纯黄色" on this site means flat `#FEBF1D` brand yellow with NO gradient and NO decorative tint layers. ONE FILE EDITED: `src/app/[locale]/demo/demo-page.css`. TWO CHANGES: (1) `.demo-hero` background: was `radial-gradient(ellipse at top, rgba(255,255,255,0.5) 0%, transparent 60%), linear-gradient(180deg, #FFF1A8 0%, #FFD95A 100%)` (prior turn's warm yellow gradient + white radial overlay) → `background: #FEBF1D` (flat brand-yellow). Picked `#FEBF1D` rather than `#FFD95A` or `#FFFF00` because `#FEBF1D` is the canonical brand bright yellow on this site (matches CTA buttons, active nav, the b2c/b2b "(B2C)"/"(B2B2C)" tag colors from prior sessions, and the contact icon override — keeps the demo hero in visual sync with everything else "yellow" on the site). (2) `.demo-hero-orb`, `.demo-hero-orb-1`, `.demo-hero-orb-2`, `.demo-hero-grid`: COLLAPSED all four rule blocks (44 lines combined) into a single `.demo-hero-orb, .demo-hero-grid { display: none }`. The decorative orbs (white highlight bloom + dark amber shadow bloom) and the gridline texture were all subtle gradients/tints layered on the bg — they'd defeat the "纯黄色 (flat)" intent. The `<div className="demo-hero-bg">` wrapper and its three child divs still exist in the JSX (`demo/page.tsx:160-163`) — they're now visually inert. Could remove the JSX too for cleanliness, but `display: none` is reversible (one CSS rule) and keeps the option open if user later wants the orbs back. The previous turn's foreground retunes (eyebrow pill bg → white-translucent, title → dark gradient, desc → dark brown, stats → dark) ALL STAY — they were tuned for AA contrast on a yellow bg and remain fine on flat `#FEBF1D` (which has very high luminance, dark text reads cleanly). Verified `npx tsc --noEmit` clean. COMMITTED locally as `983c9c4` (`style(demo): hero bg → flat pure brand yellow (no gradient, no orbs/grid)`). PRIOR commits this session: `96c6e0a` (the gradient version that user rejected) + `abd225e` (CLAUDE.md log of the same). Push to fork still NOT done (no credentials configured in this env — `git push fork main` returns `fatal: could not read Username`). User will need to push manually: 3 commits ahead now on local main (`96c6e0a` → `abd225e` → `983c9c4` + this CLAUDE.md update). NOTE TO FUTURE CLAUDE: if user wants a DIFFERENT shade of yellow, edit `.demo-hero { background: #FEBF1D }` — lighter pastel try `#FFE066`, deeper try `#E6A817`, but `#FEBF1D` is brand-canonical. If user wants the orbs/grid BACK (maybe just dialed-way-down), delete the `.demo-hero-orb, .demo-hero-grid { display: none }` rule and the original rules return — they're still in git history at `96c6e0a`. If user wants the bg to MATCH the homepage's golden orbs R3F canvas instead of being flat yellow, that's a larger refactor — homepage uses `<R3FCanvas>` + frosted-glass overlay (referenced in CLAUDE.md "Visual" line), not a CSS bg color. If user wants OTHER `/demo` sections (`#demo-b2c`, `#demo-b2b`) also yellow, those still use their own tokens — see the CSS file's section blocks after line ~263. KEY INSIGHT: the user's "纯黄色" vocabulary is consistent across sessions — every time they've used it, the correct response was flat `#FEBF1D` with no gradient and no tint layers. Don't try to be clever with subtle gradients when they ask for "纯" (pure). PRIOR (different session): contact page icon color blocks gradient → solid pure yellow: user follow-up to the prior unification said "颜色是纯黄色，不要有渐变" — pure yellow, no gradient. ROOT CAUSE: `.icon-gradient--gold` in `components.css:739` is defined as `background: linear-gradient(135deg, #E6A817, #D4890A)` (a darker amber-to-darker-amber gradient — not the brand bright `#FEBF1D` either, and visually a gradient even when uniform-ish). The prior turn had unified the 6 contact icons to all use `icon-gradient--gold` but the user wants flat color, not a gradient. ONE FILE EDITED: `src/app/styles/pages-extracted.css`. ONE new rule block inserted directly after the existing `.method-icon` definition (~line 1640): `.method-icon.icon-gradient--gold, .info-icon.icon-gradient--gold { background: #FEBF1D }`. SCOPE: `.method-icon` and `.info-icon` are ONLY used on the contact page (verified via grep across src/ — both classes have ZERO usages outside `contact/PageClient.tsx`), so an override on these is safe and won't leak to other pages. The compound selector `.method-icon.icon-gradient--gold` has specificity (0,2,0) which beats both the gradient's `.icon-gradient--gold` (0,1,0) and the base `.method-icon` (0,1,0) — no `!important` needed. The override uses `#FEBF1D` (the brand bright yellow, matches CTA buttons, active nav, hero accents) rather than retaining the darker `#E6A817/D4890A` amber, because pure brand yellow is the canonical "yellow" on this site. EFFECT: all 6 icon squares on the contact page (商务沟通/技术支持/快速联系 method icons + Phone/Email/Office info icons) now render as flat `#FEBF1D` — no gradient at all, just solid brand yellow. The shared `.icon-gradient--gold` class definition (still a gradient) is INTACT for all other pages that use it (matrix CTA, home hero badges, b2c/b2b sections, nav dropdown — though nav dropdown overrides to white/yellow on hover anyway). NOTE TO FUTURE CLAUDE: if user later wants the SAME flat-color treatment site-wide (replace ALL `--gold` gradients with flat #FEBF1D), the one-line fix is at `components.css:739`: change `background: linear-gradient(135deg, #E6A817, #D4890A)` → `background: #FEBF1D`. But verify other usages first because some places (e.g. the matrix card CTAs, the homepage's icon-gradient-decorated tag pills) rely on the gradient's depth/dimensionality. If user wants the contact icons to use a DIFFERENT solid yellow (e.g. lighter `#FFCC33`, deeper `#E6A817`), change the `#FEBF1D` in the new rule. If user adds a new icon section to the contact page later, applying `icon-gradient--gold` to it will continue working as gold-gradient UNLESS that element also has `.method-icon` or `.info-icon` (the override is scoped to those two classes). PRIOR (different session): \\
- 2026-05-15 (`/demo` page hero — background flipped from dark brown gradient → warm brand-yellow gradient, with all foreground text retuned for AA contrast on yellow): user attached `/tmp/claude-paste-1778827849592-6e64736f.png` (screenshot of the `/demo` hero showing "一站式体验 / GCSS 两大版本" eyebrow+title with "5 | 版本" and "<60s | 上线时间" stats below) and said "这一屏的背景颜色改成黄色". SCOPE: ONLY the `.demo-hero` section (top of `/demo`). Downstream sections (`#demo-b2c`, `#demo-b2b`, etc.) were untouched — if user later wants the WHOLE `/demo` page yellow, those need separate edits. ONE FILE EDITED: `src/app/[locale]/demo/demo-page.css` (the `/* ---------- Hero ---------- */` region, roughly lines 102–261). ELEVEN RULE BLOCKS CHANGED: (1) `.demo-hero` background: was `radial-gradient(ellipse at top, rgba(192,127,0,0.12) 0%, transparent 60%), linear-gradient(180deg, #1a1210 0%, #241a1a 100%)` (dark brown two-layer) → `radial-gradient(ellipse at top, rgba(255,255,255,0.5) 0%, transparent 60%), linear-gradient(180deg, #FFF1A8 0%, #FFD95A 100%)` (warm yellow gradient: cream-yellow `#FFF1A8` at top → brand-gold `#FFD95A` at bottom; white-tinted radial overlay near top center keeps a soft "light source" feel). Stayed in the brand-gold family (`#FFD95A` is already used elsewhere in this same CSS for orb gradients) rather than a pure `#FFFF00` saturated yellow. (2) `.demo-hero-orb-1` bg: `#FFD95A → #FFFAE0` + `opacity: 0.55` (was inheriting 0.35) — a saturated-yellow orb disappears on yellow bg; switched to near-white pale orb so it reads as a soft highlight bloom. (3) `.demo-hero-orb-2`: kept `#C07F00` (dark amber, still readable on yellow as a shadowy bloom) but `opacity: 0.28 → 0.18`. (4) `.demo-hero-grid` lines: `rgba(255,217,90,0.04) → rgba(140,95,0,0.08)` (was faint gold on dark, now dark gold on yellow — visible as a subtle texture; drop to `0.05` if too busy). (5) `.demo-hero-eyebrow` (the "在线沙箱 · 免注册体验" pill): border `rgba(255,217,90,0.32) → rgba(140,95,0,0.35)`, bg `rgba(192,127,0,0.08) → rgba(255,255,255,0.45)` (semi-translucent white frosted pill for separation against yellow), color `var(--primary) → #5C3F00` (dark brown for AA on the white-translucent pill). The pulsing green `.demo-hero-dot` was LEFT UNTOUCHED — green-on-yellow still reads and the "live" semantic is preserved. (6) `.demo-hero-title-script` (the "一站式体验" italic kicker): `color: var(--primary) → #6E4A00`. (7) `.demo-hero-title-main` (the big "GCSS 两大版本"): was a light-on-dark gradient `linear-gradient(180deg, #FFF7D4 0%, #FFD95A 100%)` clipped to text → swapped to a dark-on-light gradient `linear-gradient(180deg, #2A1D05 0%, #5C3F00 100%)` (espresso fading to dark gold) + solid-color fallback `color: #1F1505` for non-`background-clip` UAs. The gradient direction (top→bottom, darker→lighter) is INVERTED from the prior light-on-dark — gives the title a subtle dimensional shading that complements the bg gradient. (8) `.demo-hero-desc`: `color: var(--text-secondary) → #5C3F00` (uniform dark brown body, ≥4.5:1 on the mid-bg). (9) `.demo-hero-stats` border-top: `rgba(255,217,90,0.15) → rgba(140,95,0,0.25)` so the divider survives. (10) `.demo-hero-stat-num` (the "5" and "<60s"): `var(--primary) → #2A1D05` (near-black — brand-gold on yellow disappears; made dark so numbers pop). (11) `.demo-hero-stat-label`: `var(--text-tertiary) → #7A5400` (warm dark gold uppercase caption). WHAT WAS NOT TOUCHED: (a) The two `<Link className="btn btn-primary">` and `btn-secondary` CTA buttons in the hero — they pull global button styles; primary (yellow) still works on yellow bg because it has its own border/shadow; secondary (outline) reads fine. If user reports buttons looking off, fix lives in global `.btn-secondary` rule (likely `src/app/styles/components.css`). (b) All sections below the hero — they still use original tokens, so the page transitions from yellow hero → standard editorial sections below. (c) Reduced-motion / dark-mode media queries — existing media blocks weren't modified. Verified `npx tsc --noEmit` clean. COMMITTED locally as `96c6e0a` (`style(demo): hero background → warm yellow gradient`). PUSH TO FORK FAILED — `git push fork main` returned `fatal: could not read Username for 'https://github.com': No such device or address` (no credentials configured for `fork` remote in this environment). User will need to push manually or set up credentials for `https://github.com/baoshan23/Design-draft.git`. NOTE TO FUTURE CLAUDE: if user wants the YELLOW more pastel/desaturated, tone the bottom stop `#FFD95A → #FFE7A0`; bolder/more saturated → `#FFC72C` or `#FFB800`. If they want FLAT (no gradient), set `background: #FFD95A` and drop the radial overlay. If user says "改回原来的深色" (revert), easiest is `git revert 96c6e0a` — touches only this one CSS file. If user wants the SAME yellow treatment on `/b2c` or `/b2b` hero sections, those use DIFFERENT class chains (`.hero-with-video .hero-title` in `sections.css`) — would need a parallel edit there; do NOT try to share `.demo-hero` rules across pages (they're scoped to the demo page intentionally). If user wants the WHOLE `/demo` page yellow (not just the hero), grep `\.demo-section\|\.demo-section-alt\|\.demo-card` in the same CSS file and apply matching token swaps; the section heads, plan cards, and CTA blocks all currently assume a dark-or-neutral bg, so each would need its own foreground retune. The orphaned `b2cShimmer` keyframe from a prior session still lives in `sections.css` — unrelated to this change but a cleanup opportunity if a future task touches that file. PRIOR (sibling-agent session): b2b hero "(B2B2C)" tag swapped from 4-stop gradient sweep to solid yellow + blinking cursor to match the homepage main title's visual: user said "参考首页首屏的主标题黄色的动态效果，（B2B2C）也要改成那种动态效果" then immediately followed with "颜色是纯黄色，不要渐变" — so the requested effect is the same one already on the homepage hero (.typing-text override in `sections.css:123-131` = solid `#FEBF1D` + gold-glow text-shadow + blinking `|` cursor via `::after`), explicitly NOT a multi-stop gradient sweep. CONTEXT: prior session had set the (B2B2C) tag to use `.hero-title-tag.gradient-text-animated` (the 4-stop `#E6A817 → #D4890A → #F5C946 → #E6A817` `gradientShift 4s` sweep) — that was visually a GRADIENT, which the user just rejected. Meanwhile a sibling-agent session ALSO updated the b2c page to drop its shimmer and use solid yellow + blinking cursor via `.hero-title-b2c` — so this change brings b2b into visual parity with b2c. TWO FILES EDITED: (1) `src/app/[locale]/b2b/page.tsx:101` — span class chain `hero-title-tag gradient-text-animated` → `hero-title-tag` (dropped `.gradient-text-animated`). The (B2B2C) span no longer references the homepage's gradient utility. (2) `src/app/styles/sections.css` `.hero-title-tag` rule REPLACED: removed the prior `display: inline-block; margin-left: 0.35em;` + reduced-motion gradient fallback block, replaced with `.hero-title-tag { display: inline-block; margin-left: 0.35em; color: #FEBF1D; -webkit-text-fill-color: #FEBF1D; text-shadow: 0 2px 18px rgba(254, 191, 29, 0.35) }` + `.hero-title-tag::after { content: '|'; margin-left: 4px; color: #FEBF1D; -webkit-text-fill-color: #FEBF1D; animation: blink 0.7s step-end infinite }` + reduced-motion block stripping only the cursor blink. REUSES the EXISTING `@keyframes blink` from `enhancements.css:157-159` (no new keyframe). MARGIN PRESERVED: kept `margin-left: 0.35em` because the b2b hero (UNLIKE the b2c page after its own redo) does NOT have a `<br />` between the phrase and the bracket tag — "(B2B2C)" sits inline AFTER "SaaS平台版运营模式" on the same line, so it needs horizontal spacing. If the user later wants a line break before "(B2B2C)" to match the b2c page exactly, add `<br />` before the span in `b2b/page.tsx:101` AND drop the `margin-left: 0.35em` from `.hero-title-tag`. EFFECT: b2b hero now reads "SaaS平台版运营模式 (B2B2C)|" — the phrase in default dark hero color, "(B2B2C)" in solid `#FEBF1D` brand yellow with a gold-glow halo, and a blinking yellow `|` cursor after it. Visually matches the homepage main title (solid yellow + glow + blink) AND visually matches the b2c page's redo. Verified `npx tsc --noEmit` clean. NOTE TO FUTURE CLAUDE: `.hero-title-tag` and `.hero-title-b2c` now have IDENTICAL styles (solid yellow + glow + blink cursor) — they're effectively duplicates with different class names. If user later asks for a refactor / DRY-up, consolidate them: rename `.hero-title-b2c` callers in b2c/page.tsx to use `.hero-title-tag` and delete the `.hero-title-b2c` rule block. Kept separate for now because: (a) the b2c page has `<br />` before its tag (no horizontal margin needed) while b2b is inline (needs `margin-left`) — merging would force one to override the other; (b) prior session's note explicitly anticipated this divergence ("If user wants UNIFIED animation across both, the simplest fix is to swap b2b's class chain to `<span className="hero-title-b2c">(B2B2C)</span>` matching this page's pattern") — so the current setup is the documented choice. If user wants the b2b cursor REMOVED (just solid glowing yellow, no blinking), delete the `.hero-title-tag::after` block. If user wants the glow STRONGER, bump `text-shadow: 0 2px 18px rgba(254,191,29,0.35)` → `0 0 32px rgba(254,191,29,0.55)`. The orphaned `gradient-text-animated` class on the homepage's `<TypingText>` is now unused for VISUAL effect (the `.typing-text` override blanks its gradient) but still exists as a utility class — it's referenced from `contact/PageClient.tsx:142` (contact-page title2 — still uses the multi-stop sweep, presumably intentionally) so DO NOT delete that class definition. PRIOR (sibling-agent session): contact page unified all 6 icon color-block backgrounds to brand-yellow `--gold`; b2c hero REDO switched (B2C) tag from shimmer to solid-yellow + blinking cursor — see commit log.
