# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-06-09 — **b2b hero illustration — enlarged as much as possible without touching the left title/buttons** (`7b3de21`; user red-boxed the right-side illustration `把红框的贴图尽可能的放大，但是不要影响到左边标题的所有内容`). The illustration is `<Image src=/images/b2b-hero-illustration.webp>` in `.hero-video-wrap.hero-media-bare` (b2b/page.tsx ~170), right grid column of `.product-hero.particles-bg .hero-split:has(.hero-video-wrap)` (sections.css ~1892). **Constraint**: the EN buttons ("Become a Platform Operator" + "Contact Sales", `.hero-buttons{flex-wrap:wrap}`) need ~500px, so the text col can't shrink past that without wrapping → kept it at **520px** (was 560), gap **40→24**. **Main lever = right-bleed**: `.product-hero.particles-bg .hero-media-bare{width:calc(100% + max(24px,(100vw - var(--max-width) + 48px)/2))}` extends the image's RIGHT edge to the viewport edge (X=24px on ≤1440 = the `.container` padding; grows by the gutter on wider). `.hero` has `overflow:hidden` at the viewport edge so it clips exactly there. Width (not transform) on the wrapper — its heroEntrance animation only touches transform, so no conflict; image is `width:100%` → fills the new width. ≤900px resets `width:100%`. So the image is much bigger on wide screens (+~50% @1920), modestly bigger on laptops (gutter is only 24px ≤1440). tsc 0. **Followup `太大了，缩小一点` (`c6b7dd9`)**: the full-bleed over-enlarged wide screens → **capped the bleed at +64px** (`min(64px, max(24px, …))`) + text col `520→530` / gap `24→28`. Wide screens now bleed ≤64px (was up to 264); laptops ~unchanged (24px). Revert: grid back to `minmax(0,560px)`/gap 40 + drop the `.hero-media-bare` width rule.

- 2026-06-09 — **blog page → Arcadia `/blog` layout + content-page hero title/subtitle unification + small fixes** (final standalone commit `2fa0b82`, pushed fork `design-draft-local`; earlier parts of this session were swept into concurrent commits). (1) **Blog redesign** (`blog/PageClient.tsx` + `pages-extracted.css` ~1343 block): replaced the old featured-split/two-section grid with the Arcadia `/blog` "最近发布" listing — left-aligned `.blog-section-title` (uses `blog.recent`), a `.blog-toolbar` (filter pills LEFT + `.blog-search` box RIGHT, live filters title/excerpt via a `query` state), and ONE 3-col `.blog-grid`. Cards = **bordered white box** (`.blog-card` flat `<Link>`, 1px border + 16px radius + `overflow:hidden`) with a **cover image on top** (`.blog-card-img`, `ImagePlaceholder` 16/10) + `.blog-card-body` (plain-gold uppercase `.post-category` → title → 4-line excerpt → gold `.blog-card-date` pinned via `margin-top:auto`). Added i18n `blog.featuredStories`/`blog.recent`/`blog.searchPlaceholder` (en+zh). User flow: Arcadia-clone → "按这个布局"(text-card listing screenshot) → "保留封面图"(covers added back). (2) **Article back button** (`pages.css` `.vg-article-back`): was `position:fixed; left:24px` → overlapped the centered title; made in-flow (dropped fixed/left/top/z-index/box-shadow, added `margin-bottom:24px`) so it sits above the category/title at all widths; deleted the redundant `@media(max-width:900px)` override. (3) **Content-page hero title+subtitle unified** (block appended at END of `polish.css`, loads last so it beats per-page sizes + mobile @media): `.blog-hero h1, .forum-hero h1, .contact-hero h1, .faq-hero .section-header h1, .hero.mesh-bg .hero-title` → `clamp(2.625rem,5.5vw,3.75rem)/500/1.1/-0.03em` (= base h1; only forum [was 1.6rem/800] + faq-mobile actually changed); subtitles `…p, .hero.mesh-bg .hero-desc` → `1.1rem/1.7/--text-secondary`. Scope = content pages only; marketing heroes (home `.hero-centered`, b2b/b2c `.product-hero`) EXCLUDED (they don't use `.hero.mesh-bg`). Added a `faq-hero` class to faq's hero `<section>` for a clean target (no global `.section-header` change). User picked "仅内容页" via AskUserQuestion. (4) **demo edition titles un-bolded** (`demo/demo-page.css` `.demo-section-title` `font-weight:800→500`). (5) **blog hero title CJK space** (`blog/PageClient.tsx`): `{title} {title2}` → `{title}{locale==='zh'?'':' '}{title2}` → zh 「洞察与最新动态」(no space), en "Insights & Updates" keeps it. tsc 0 throughout; SSR verified. Revert: blog block → prior featured/two-section markup + glass `.blog-card`; back button → `position:fixed;left:24px;top:…`+900px override; drop the polish.css unified-hero block + `faq-hero` class; demo weight→800; title→`{title} {title2}`.

- 2026-06-09 — **blog cards — fully static on hover, only the cover image zooms** (`4a2593b`→`5b9cd5a`; user `这里卡片不要做交互投影` then `做成完全静态，只有图片稍微动一下的交互效果`). `.blog-card:hover` (pages-extracted.css ~1443): `box-shadow→none`, `transform→none`, `border-color→var(--border-subtle)` (same as rest); `.blog-card:hover h3` gold `--primary-text→--text-primary` (no title colour change). KEPT only `.blog-card:hover .blog-card-img img{transform:scale(1.05)}` (the slight image zoom). Revert: hover box-shadow `0 14px 30px rgba(0,0,0,.07)` + `translateY(-3px)` + border `rgba(254,191,29,.55)` + h3 `--primary-text`.

- 2026-06-09 — **site-wide — Arcadia-style page-transition WIPE (切屏效果) — the thing the user ACTUALLY wanted** (user `没改对，我要的是 arcadia.com 刷新的切屏效果` — the prior GSAP element-fade entrance was the wrong read; the user means Arcadia's two-panel curtain WIPE). Re-analysed arcadia.com source: its `PageTransition` renders two stacked full-screen panels `<div class="PageTransition_wipe wipe1">`(springGreen, z1000) + `wipe2`(cream, z1001), CSS `position:fixed; inset:0; width/height:100%`. From `_app.js` the GSAP timeline (`ease:"ArcadiaEase"`): **onExit (cover)** `set([wipe1,wipe2],{scaleY:0,transformOrigin:"center bottom"})` then `fromTo(wipe1,{scaleY:0},{scaleY:1,duration:.7},"start")` + `fromTo(wipe2,{scaleY:0},{scaleY:1,duration:.7},"start+=0.2")` (panels grow up from the bottom to cover, staggered 0.2s); **onEnter (reveal)** `scaleX→0, transformOrigin:"right center"` + springGreen `autoAlpha:0`. **Replicated** in GCSS brand colours (gold `var(--primary)` + cream `#fff2d7`): new `components/effects/PageWipe.tsx` (client) renders `.page-wipe-root > .page-wipe-gold + .page-wipe-cream`; an isomorphic-layoutEffect sets both to `scaleY:1, transformOrigin:'center top'` then a GSAP timeline collapses them upward to `scaleY:0` (cream at t0, gold at t+0.15, `duration:.7, ease:'ArcadiaEase'`), `onComplete` → `display:none`. **Mounted in `[locale]/template.tsx`** (re-mounts on every route change) so the wipe replays on a hard refresh AND every client-side navigation = the page-to-page 切屏. CSS in polish.css (appended ~1340): `.page-wipe-root{position:fixed;inset:0;z-index:100000;pointer-events:none}` + `.page-wipe{position:fixed;inset:0;transform-origin:center top;transform:scaleY(1);animation:pageWipeReveal .7s cubic-bezier(.16,1,.3,1) forwards}` (gold delay .15s, cream delay 0) + `@keyframes pageWipeReveal{to{transform:scaleY(0);visibility:hidden}}` — **the CSS animation is a no-JS FALLBACK** (panels wipe away even if GSAP never runs → page never stays covered); PageWipe sets `style.animation='none'` to cancel it and take over. `prefers-reduced-motion` → `.page-wipe-root{display:none}`. tsc 0; dev :8000 `/en` `/en/b2b` `/zh` `/en/pricing` 200, all render 2 wipe panels (gold+cream), no errors. NOT visually verified (autodebug down). **Kept the prior `<ArcadiaReveal/>` element-fade** (Arcadia has BOTH the wipe and the element entrance) — offered to remove it if the user wants the wipe alone. Tuning: colours in polish.css `.page-wipe-gold/.page-wipe-cream` + the cream `#fff2d7`; timing (`.7`, stagger `.15`) + direction (`transformOrigin`) in PageWipe.tsx. Revert: drop `<PageWipe/>` + import from template.tsx, delete PageWipe.tsx + the `.page-wipe*` CSS block.

  - 2026-06-09 followup — **made it the FULL two-phase wipe + gold/black** (user `也可改成 Arcadia 那种「从底部展开盖上」` + `「从右侧收起」的两段式，或者换成纯黄+黑`). PageWipe.tsx rewritten: panels are now **gold `var(--primary)` + black `#181818`** (was gold+cream `#fff2d7`; class `.page-wipe-cream`→`.page-wipe-black`). Two phases: **REVEAL** on mount (refresh/after-nav) = panels cover, retract to the right `scaleX 1→0, transformOrigin:'right center'` (black @0, gold @0.15); **COVER** on internal-link click = grow up from the bottom `scaleY 0→1, transformOrigin:'center bottom'` (gold @0, black @0.12) THEN navigate. App Router has no before-navigate hook, so COVER intercepts link clicks in the **capture phase** (`document.addEventListener('click', fn, true)` + `preventDefault`+`stopPropagation` to beat next/link), computes `dest = url.pathname+search+hash`, and `router.push(dest)` (uses `next/navigation` useRouter, NOT next-intl's — the href already carries the locale, so next-intl's would double-prefix). **Navigation safety**: a 1.5s `setTimeout` hard-navigates (`location.href=dest`) if the cover timeline's `onComplete` never fires, cleared on success — so an animation hiccup can never strand the user behind the panels. Skips: external origin, `target=_blank`, `download`, `rel*=external`, modified/middle clicks, and same-path/in-page-anchor (left to native / the Lenis anchor handler). CSS fallback updated to `transform:scaleX(1)`/`scaleX(0)` `transform-origin:right center` (matches the reveal direction). tsc 0; `/en` `/en/b2b` `/zh` 200, render gold+black panels, no errors. NOT visually verified (autodebug down) — **test link clicks in a real browser to confirm navigation still works** (the capture-phase intercept is the main risk). Revert this followup: see the base entry above (PageWipe.tsx + `.page-wipe*` CSS).

  - 2026-06-09 followup — **too fast + "跟 arcadia 不一样" → faithful 3-LAYER replica** (user `切换的速度太快了，而且跟 arcadia.com 都不一样`). Re-extracted the FULL `_app.js` PageTransition timelines (was working off partial grabs): Arcadia uses **THREE** layers — `overlay` + `wipe1`(springGreen) + `wipe2`(cream) — and `timeout:900` per phase. **onExit (cover)**: `overlay autoAlpha 0→0.5` (dims the page) **+** `set([w1,w2],{scaleX:1,scaleY:0,transformOrigin:"center bottom"})` then `w1 scaleY 0→1 @start`, `w2 scaleY 0→1 @start+=0.2` (both grow up from the bottom, staggered). **onEnter (reveal)**: `set(overlay,{autoAlpha:0}) .set(w1,{autoAlpha:0})` (springGreen HIDDEN) `.set(w2,{transformOrigin:"right center"}).to(w2,{scaleX:0,duration:.7})` — i.e. **only ONE panel (cream) slides off to the right**; the prior 2-panel reveal was wrong. **Rebuilt PageWipe to match**: 3 layers `.page-wipe-overlay`(#181818 scrim) + `.page-wipe-gold` + `.page-wipe-black`. REVEAL = black only, `scaleX 1→0` from the right (gold + overlay hidden). COVER = overlay fades to 0.5 + gold then black (0.2s stagger) `scaleY 0→1` from the bottom, then navigate. **Slowed** `DUR 0.6/0.7→0.9s` + `STAGGER→0.2` to match Arcadia's pace (the "太快" fix). Safety timer 1.5→2.0s. CSS: added `.page-wipe-overlay` (opacity:0/hidden), `.page-wipe-gold` now default-hidden (opacity:0/visibility:hidden, only shown during COVER), `.page-wipe-black` keeps the `scaleX` fallback at 0.9s. tsc 0; `/en` `/en/b2b` `/zh` 200, render all 3 layers, no errors. NOT visually verified (autodebug down). Tuning: `DUR`/`STAGGER` consts + overlay `autoAlpha:0.5` in PageWipe.tsx. Revert: see the base entry (PageWipe.tsx + `.page-wipe*` CSS).

- 2026-06-09 — **homepage hero — GSAP-driven Arcadia-style entrance animation (full replica of arcadia.com's refresh interaction)** (user analysed arcadia.com [`分析...刷新的交互方式`] then chose `第二种` = full GSAP replica, not the lightweight CSS-blur option). Curl'd arcadia.com's source: Next.js + **GSAP (ScrollTrigger + CustomEase)**, NO Lenis / framer-motion / preloader. Its refresh reveal (chunk `9292`): text `[data-animate="true"]` → `gsap.fromTo({autoAlpha:0,y:10},{autoAlpha:1,y:0,duration:0.6,stagger:0.08,ease:"power2.out"})`; media/containers add `filter:blur(6px)→0` + `scale`, 0.75s; brand `CustomEase` = `ArcadiaEase`; first-screen plays immediately, below-fold via ScrollTrigger. **Replicated**: `npm i gsap` (v3.15). New `lib/gsap.ts` `setupGsap()` registers ScrollTrigger+CustomEase once + `CustomEase.create('ArcadiaEase','M0,0 C0.16,1 0.3,1 1,1')` (= the site's `cubic-bezier(.16,1,.3,1)`). New `components/effects/ArcadiaReveal.tsx` (client, isomorphic-layoutEffect sets the hidden state before paint → no FOUC; `prefers-reduced-motion`→skip): scans `[data-animate]`, `="media"`→blur+scale (y24/0.75s), `="text"`/default→fade+slide (y10/0.6s), all `ease:'ArcadiaEase'`; **above-fold (rect.top<0.9vh) plays immediately staggered 0.08s, below-fold gets a per-element ScrollTrigger** (`start:'top 88%', once`); cleanup `gsap.context().revert()`. **Opt-in via `data-animate`** to coexist with the existing CSS system: homepage hero `h1.hero-title`/`p.hero-desc`/`.hero-buttons`/`.hero-stats` = `data-animate="text"`, `.hero-dashboard-mock` = `data-animate="media"`, `<ArcadiaReveal/>` mounted in `page.tsx`. **Coexistence**: polish.css `heroEntrance` selectors (~1230) got `:not([data-animate])` so GSAP-owned elements don't double-animate, while b2b/b2c heros (no `data-animate`) keep the CSS entrance (verified: b2b/b2c/pricing render 0 `data-animate`). tsc 0; dev :8000 `/en` `/zh` 200 (5 `data-animate` each: 4 text + 1 media), no GSAP/module errors. NOT visually verified (autodebug down) — motion only shows in a real browser. Tuning: opt any element in with the `data-animate="text"|"media"` attr; `ArcadiaEase` path in lib/gsap.ts; stagger 0.08 / durations / `start:'top 88%'` in ArcadiaReveal.tsx. Extend site-wide: add `data-animate` attrs on other pages + move `<ArcadiaReveal/>` into layout.tsx. Revert: drop `<ArcadiaReveal/>` + import + the 5 `data-animate` attrs from page.tsx, restore polish.css hero selectors (drop `:not([data-animate])`), delete `ArcadiaReveal.tsx` + `lib/gsap.ts`, `npm rm gsap`.

- 2026-06-09 followup — **payment marquee — tightened the two-row gap 28→12px** (`f1453e7`; user `两行logo展示的间距小一点`). `.b2c-pay-marquee` `gap:28px→12px`. (Note: each pill is still 132px tall vs ~114px logos, so ~9px pill padding remains top/bottom — for even tighter, reduce `.b2c-pay-pill` height but keep ≥122 to clear the Mastercard/Swish 122px overrides.) Revert: gap back to 28.

- 2026-06-09 followup — **payment marquee — enlarged the wide-wordmark base tier 104→114px** (`7ae7a23`; user red-boxed the Samsung/amazon/Cash App/Revolut/Alipay/WeChat/GrabPay/GCash row `再放大一点点`). Those AR≥1.7 wordmarks live in the BASE rule (the square/medium logos are pinned separately by the 104 low-AR override), so bumping base `104→114px` (max-width 200→216; mobile 76→82/162) enlarges exactly that wordmark group (+ PayPal/PromptPay/Afterpay/Scalapay/Discover/Klarna, same category) without touching the square logos. Revert: base back to 104/200, mobile 76/150.

- 2026-06-09 followup — **payment marquee — shrank MoMo/Toss/Tabby/Sezzle tier 64→56px + REMOVED the grey band** (`1209a3c`; user red-boxed MoMo/Toss `稍微缩小一点点` + `灰色背景去掉`). Very-wide tier `64→56px` (mobile 46→40). `.b2c-pay-screen` stripped back to just `margin-top:80px` (dropped the full-bleed `100vw`/`background:var(--dark)`/`padding:80px` band) → payment screen is plain on white again; the marquee keeps its own `100vw` full-bleed breakout. Restored the `#multilingual` `marginTop:96→128` (the band's padding that justified the earlier trim is gone). tsc 0. Revert: re-add the grey band to `.b2c-pay-screen` + tier back to 64/46.

- 2026-06-09 followup — **payment marquee — enlarged Swish + MobilePay** (`9eb2c86`; user red-boxed them row 2 `红框的logo放大一点`). Dedicated override `[alt="Swish"],[alt="MobilePay"]{height:122px; max-width:208px}` (mobile 88/150) — their marks sit small in padded PNGs. Revert: drop the override.

- 2026-06-09 followup — **payment marquee — enlarged Samsung Pay a touch** (`32cdb89`; user red-boxed Samsung Pay `再放大一点点`). Added a dedicated override `[alt="Samsung Pay"]{height:118px; max-width:222px}` (mobile 86/164), like the Mastercard one. Revert: drop the Samsung Pay override.

- 2026-06-09 followup — **payment marquee → full-bleed + TWO staggered scrolling rows** (`c3d2e05`; user `展开区域横向铺满，然后做两行滚动展示`). `sections.css` `.b2c-pay-marquee`: dropped `max-width:1360px; margin:auto` → full-bleed `width:100vw; margin-left:calc(-50vw+50%)` (breaks out of the grey band's padding to span the whole viewport); added `flex-direction:column; gap:28px` for two rows; mask fade tightened 6→5%. New `.b2c-pay-track--2` (dur 94s / delay -41s / `padding-left:200px`) so row 2 staggers vs row 1. `b2c/page.tsx`: split `PAYMENT_METHODS_FLAT` into 2 halves, render 2 `.b2c-pay-track` rows (each still ×2 copies for seamless loop). tsc 0. Revert: marquee back to `max-width:1360; margin:auto` single row + drop `.b2c-pay-track--2` + the page back to one track over the full list.

- 2026-06-09 followup — **payment screen → full-bleed GREY band + enlarged Mastercard logo** (`16912d9`; user red-boxed Mastercard `这个logo放大一点` + `支付这一屏的白色背景改成灰色`). (1) **Grey band**: the payment block (`b2c/page.tsx`, was `<div style={marginTop:80}>`) → `<div className="b2c-pay-screen">`; new `sections.css` rule `.b2c-pay-screen{margin-top:88px; width:100vw; margin-left:calc(-50vw+50%); background:var(--dark)(#F1F2F4); padding:80px clamp(16px,4vw,24px)}` — full-bleed grey band wrapping the header + marquee, breaking out of the white `#features` section (body `overflow-x:clip` makes the 100vw trick safe). (2) **Mastercard**: its PNG has heavy internal padding → read small at the 104 base tier; dedicated override `[alt="Mastercard"]{height:122px; max-width:230px}` (mobile 90/170) → renders ~223×122. (3) Trimmed the languages `#multilingual` `marginTop:128→96` since the band now adds its own 80px grey bottom padding (total marquee→languages ≈176px). tsc 0. Revert: payment block back to `<div style={marginTop:80}>` + drop `.b2c-pay-screen` + the Mastercard override; languages marginTop back to 128. user clarified `我的意思是，调整支付这一屏和下面多语言的间距` → AskUserQuestion: 加大). Reverted the `29f532f` header-side change (marquee `marginTop:24` dropped → header→marquee back to the standard 72px); set the `#multilingual` languages block `marginTop:96→128` (only the payment→languages gap is enlarged). tsc 0. Revert: languages block `marginTop` back to 80.

- 2026-06-09 followup — **payment logo marquee — base tier 88→104px (wide wordmarks too small)** (`a17edce`; user red-boxed Samsung Pay/Amazon Pay/Cash App Pay/Revolut Pay `这几个logo也偏小`). Those AR~1.8–1.9 wordmarks sat in the base-88 tier, smaller than the 104 neighbours. Raised `.b2c-pay-pill .payment-logo-img` base `88→104px`, max-width `180→200` (so wide ones grow before capping; Cash App 1.92→200 wide caps at ~104 tall). Net: EVERYTHING except the very-wide MoMo/Toss/Tabby/Sezzle pills (still 64px) is now 104px — the per-AR low-tier overrides are now redundant-but-harmless. Mobile base `64→76`, pill `96→110`, max-width `132→150`. CSS-only. Revert: base back to 88/180, mobile 64/132.

- 2026-06-09 followup — **payment logo marquee — full COLOUR at rest (dropped grayscale) + bumped AR~1.6 logos up to the 104px tier** (`50f50ae`; user red-boxed Swish+MobilePay `红框的logo也大一点` + `然后静态展示时，就已经是彩色的`). (1) Removed `filter:grayscale(1)`/`opacity:.88`/`transition` from `.b2c-pay-pill .payment-logo-img` + deleted the `:hover` colorize rule → logos show full brand colour statically. (2) Swish(1.62)/MobilePay(1.6) had landed in the base-88 tier (smaller than the 104 neighbours Bancontact/MB WAY/Twint); added Swish/MobilePay/SPEI/LINE Pay/American Express/SEPA (AR 1.6–1.67) to the **104px** low-AR tier (mobile 76px) so the base-88 tier now holds only the genuinely-wide AR≥1.7 wordmarks. CSS-only. Revert: re-add the grayscale filter + hover rule, drop the 6 alts from the 104/76 tier.

- 2026-06-09 followup — **payment logo marquee — re-tiered to match the MoMo/Toss Pay VISUAL WIDTH (user marked those as "normal")** (`e55ac60`; user red-boxed MoMo+Toss Pay `红框的logo大小为正常`). Insight: MoMo/Toss read big because they're WIDE (~165px, colored pills), not tall — they're the SHORTEST tier (64px). Plain wordmarks looked small because they rendered narrower. Re-sized by target visual WIDTH ~150–170px: merged the old square(100)+medium(88) tiers into ONE **low-AR tier 104px** (square logos fill ~104–163px wide, e.g. Visa 1.57→163), **base 76→88px** (the 1.6–1.95 AR wordmarks → ~155px wide), very-wide MoMo tier 64px kept (165 wide); `max-width:240→180` (so tall wordmarks cap near MoMo's width). Pill stays 132. Mobile: base 64 / low-AR 76 / very-wide 46 / pill 96 / max-width 132. CSS-only. Revert: split the low-AR tier back to square 100 / medium 88, base→76, max-width→240.

- 2026-06-09 followup — **payment logo marquee — scaled ALL tiers up (still too small)** (`0b4eaa3`; user screenshot + `这样已经是偏小了`). Pill `height:96→132`, padding `36→32`; logo base `58→76`, square tier `78→100`, medium tier `66→88`, very-wide tier `48→64`, max-width `200→240`, opacity `.78→.88` (less faint). Mobile pill `78→100`, base `44→56`, square `58→74`, med `50→66`, wide `38→48`. CSS-only. Revert: halve back to the `79e6dc4` values.

- 2026-06-09 followup — **payment logo marquee — per-logo height tiers so every brand reads at a similar VISUAL size** (`79e6dc4`; user `logo的大小太小了，看不清，帮我把过于小的logo放大一点，视觉上全部的logo大小都差不多`). Root cause: a uniform `height:52px` makes square/icon logos (AR ~0.9–1.3) render ~50px wide = tiny, while wide wordmarks (AR ~2.7) render ~140px = big. Measured all 48 displayed PNGs' aspect ratios (python struct PNG header) and bucketed them in `sections.css`: base `52→58px`; **square tier 78px** (`[alt=]` for Multibanco/Interac/PayPay/JCB/Apple Pay/Touch'n Go/UnionPay/Bancontact/Google Pay, AR<1.35); **medium tier 66px** (BLIK/MB WAY/OXXO/Boleto/Affirm/Twint/Zip/iDEAL/KakaoPay/Pix/UPI/Visa/Przelewy24/FPX, AR 1.35–1.6); **very-wide tier 48px** (MoMo/Sezzle/Tabby/Toss Pay, AR 2.67). Now square ~78px-wide ≈ wordmarks ~100px-wide — balanced. Attribute-selector overrides use `!important` (base also `!important`; higher specificity wins). Mirrored scaled tiers inside the `@media(max-width:700px)` block (base 44 / square 58 / med 50 / wide 38) since the !important desktop tiers would otherwise beat the mobile base. CSS-only. Revert: drop the 3 tier blocks (desktop + mobile) + base back to 52.

- 2026-06-09 followup — **payment logo marquee — bare grayscale logos (no pill), contained to 1360px, slower scroll** (`3b847cb`; user `两个方向都做，然后仅在1360的宽度内做滚动展示，滚动速度慢一点`). `sections.css`: (1) **bare logos** — `.b2c-pay-pill` dropped `background:#fff`/`border`/`border-radius:9999px` → transparent, `height:116→96`, `padding:52→36`, no min-width; removed the `[data-theme=dark]` white-pill rule. (2) **grayscale** — logos `filter:grayscale(1); opacity:.78` at rest → `grayscale(0); opacity:1` on hover (go-electra desaturated strip; whole track also pauses on hover). (3) **contained** — `.b2c-pay-marquee` dropped the full-bleed `width:100vw; margin-left:calc(-50vw+50%)` → `width:100%; max-width:1360px; margin:0 auto` (scroll lane capped at 1360, centred). (4) **slower** — track `gap:24→32`, anim `60s→80s`. ≤700px pill 76/logo 40. CSS-only. Revert: re-add the white pill bg/border/radius + full-bleed 100vw + drop the grayscale filter + anim back to 60s.

- 2026-06-09 followup — **payment logo marquee — re-tuned to go-electra landowner partner-strip proportions (compact, more pills in view, smoother scroll)** (`2d74c84`; user `参考 https://www.go-electra.com/en/landowner/ 红框的样式，重新调整下大小和滚动展示的方式`). WebFetch'd the go-electra landowner page (reachable this session) — its partner strip = single compact row, modest grayscale logos, evenly spaced, many visible at once, smooth continuous glide (the SAME strip as the user's original red-boxed ref image). The 2× pass (88px logos / 168px pills) was too big (~4 pills visible). Dialed back in `sections.css`: `.b2c-pay-pill` logo `88→52px` (max-width 280→180), pill `height:168→116`, `min-width:340→260`, `padding:72→52`; track `gap:32→24`, anim `90s→60s` (smoother moderate pace); ≤700px pill 88/logo 40. Now ~5-6 pills visible on full-bleed, restrained go-electra look. CSS-only, no tsc impact. Revert: logo 88/pill 168/min-width 340/pad 72/gap 32/anim 90s.

- 2026-06-09 followup — **payment logo marquee — logos 2× (44→88px), slower scroll, section gap unified** (`fb4a668`; user `支付方式的logo放大到现在的2倍，展示的滚动速度慢一点，参考首页的合作商展示方式` + `这一屏和上下屏的高度间隙，与其他的保持一致`). `sections.css`: `.b2c-pay-pill` logo `height:44→88px` (max-width 150→280), pill `height:104→168`, `min-width:240→340`, `padding:0 52→72`; track `gap:24→32`, anim `50s→90s` (slower, homepage 合作商 = 50s but this track is longer so 90s reads similarly unhurried); ≤700px pill 120/logo 60. **Spacing fix**: the marquee had an inline `marginTop:32` on top of `.section-header`'s standard `margin:0 auto 72px` → 104px header→content gap vs the site-standard 72px; removed the inline `marginTop` (`b2c/page.tsx`) so header→marquee = 72px, matching every other section. Block-to-block gaps were already 80px (`marginTop:80` on each block). tsc 0. Revert: restore `style={{marginTop:32}}` on `.b2c-pay-marquee` + logo 44/pill 104/min-width 240/pad 52/gap 24/anim 50s.

- 2026-06-09 followup — **payment logo marquee — 2 rows → single full-bleed row, border-only (no shadow), bigger logos** (`4e4a2af`; user `不要投影，只保留边线，仅有一行展示，横向铺满一屏，logo放大一点`). `b2c/page.tsx`: dropped the 2-row split — now ONE `.b2c-pay-track` over the whole `PAYMENT_METHODS_FLAT` (48) ×2 copies. `sections.css`: `.b2c-pay-marquee` lost `flex-direction:column`/gap, added full-bleed break-out `width:100vw; margin-left:calc(-50vw + 50%)` (works because body `overflow-x:clip`); dropped the `.b2c-pay-track--2` rule + the `:hover` shadow; `.b2c-pay-pill` `box-shadow:var(--shadow-sm)→none` (border line only), `height:84→104`, `min-width:200→240`, `padding:0 44→52`, logo `height:30→44px` (max-width 120→150); track gap 20→24, anim 40→50s; ≤700px pill 84/logo 34. tsc 0. Revert: restore the 2-row PAY_ROWS markup + the `.b2c-pay-track--2`/shadow CSS from `git show 4e4a2af^:`.

- 2026-06-09 — **b2c `#payments` "全球支付矩阵" — region-tag grid → horizontal logo-pill marquee w/ REAL payment logos** (`94a7121`; user pasted the current 2-row region grid [Oceania/Africa/… + payment-method text tags] + a pill-marquee ref `将这里的布局排版，改成这种，调用实际支付方式对应的logo`). Replaced the `PAY_REGIONS` region-card markup (`b2c/page.tsx` ~346: each `.b2c-pay-card` = region `<h4>` + `.tag` method chips) with logo pills: imported `PAYMENT_ICONS` + `PAYMENT_METHODS_FLAT` from `components/sections/home/paymentIcons.tsx`, split the 48-method flat list into 2 rows, each marquee item is now a `.b2c-pay-pill` holding `PAYMENT_ICONS[name]` (renders the real brand-logo PNG via `<PaymentLogo>`). Kept the existing 2-track staggered `.b2c-pay-marquee` (track--1 40s / track--2 47s rotated + 160px pad + pause-on-hover + edge-mask). **CSS** (`sections.css` ~2883, replaced the `.b2c-pay-card`/`.tag` block): `.b2c-pay-pill` = white rounded-9999px capsule, `height:84px; min-width:200px; padding:0 44px`, `border:1px solid var(--border-subtle)` + `--shadow-sm`, centered logo; `.b2c-pay-pill .payment-logo-img/.payment-logo-svg{height:30px!important;width:auto;max-width:120px;object-fit:contain}` (scoped so it doesn't fight the homepage `.payment-fisheye-inner img` rule); dark-theme keeps the pill near-white so coloured logos stay legible; ≤700px → height 70 / logo 26. tsc 0; dev :8000 SSR renders 192 pills (48×2 copies×2 rows). NOT visually verified (autodebug down). Revert: restore the `PAY_REGIONS` region-card markup + the `.b2c-pay-card`/`.b2c-pay-card .tag` CSS from `git show 94a7121^:`, drop the paymentIcons import + the `.b2c-pay-pill` CSS.

- 2026-06-09 — **pricing `#addons` "可选扩展" cards — vertical/centered editorial-stat layout → horizontal icon-left layout (reference: icon tile left + bold black title + grey desc right); line icons → filled (面性) glyphs** (`0d58b7c`; user pasted the current pricing screenshot + a ToDesktop-style 3×2 ref `将这里的卡片布局，改成这种，图标是面性图标，小标题黑色加粗`). The cards are `.addon-card` (`pricing/page.tsx` ~280: `.addon-card-icon` + `.addon-card-body` > `.addon-label`(desc) + `.addon-price`(price)). **Layout** (`sections.css` ~4765 override block): `.addon-card` `flex-direction:column;align-items:center;text-align:center` → `row;flex-start;left`, dropped the border/radius (ref has no card chrome), `gap:18px`; `.addon-card-body` stays `column-reverse` (DOM is label-then-price → reverse puts price on TOP as the title, label below as desc). **Title black bold**: `.addon-price` `1.6rem/800/var(--primary)`(gold) → `1.15rem/700/var(--text-primary)`(black) — the price is now the bold-black 小标题. `.addon-label` desc → `0.88rem/400/var(--text-secondary)` grey. **Icon tile**: `transparent`+black line icon → soft-gold `rgba(254,191,29,.12)` rounded tile + gold filled glyph (`color:var(--primary)`). **Filled icons**: rewrote all 6 `addonIcons` SVGs from two-tone stroke (`fill:none;stroke;.ic-accent`) to single-tone Material-style `fill="currentColor"` filled glyphs (smartphone/translate/credit-card/POS-terminal/code-`</>`/storefront) — dropped the now-dead `.ic-accent` stroke rule. **Grid**: `.addons-grid` 3-col override got `gap:36px 40px` for ref-like air. tsc 0. NOT visually verified (no dev server up this session). Revert: `.addon-card` back to `column/center/center` + `1px solid var(--border-subtle)`/`--radius-xl`/`padding:28px 22px`; `.addon-price` back to `1.6rem/800/--primary`, `.addon-label` to `.92rem/500/--text-secondary`; icon tile back to `transparent`+`color:#1a1a1a`+30px svg + re-add `.ic-accent{stroke}`; restore the 6 stroke SVGs from `git show 0d58b7c^:`; drop the `.addons-grid` gap.

- 2026-06-09 — **homepage — introduced Lenis smooth-scroll for a go-electra-style "丝滑" wheel feel** (user `导入Lenis让首页滚动更丝滑`; analysed go-electra first by curl'ing its source — its footer reveal is the SAME native `position:sticky;bottom:0` giant-wordmark wipe this site already replicates, and the buttery feel comes from **Lenis** [`lenis` lib] + native sticky, NOT scroll-jacking). `npm i lenis` (v1.3.23 — registry IS reachable in this session, ~4s; the "zero outbound network" note is stale for npm here). New client effect `components/effects/SmoothScroll.tsx` = `<ReactLenis root options={{ lerp:0.09, smoothWheel:true, wheelMultiplier:1, syncTouch:false }} />` from `lenis/react`. **`root` mode smooths the NATIVE window scroll (no wrapper transform)** — critical: the footer-reveal `.footer-reveal{position:sticky;bottom:0}` wipe + the fixed sub-navs keep working, and the existing rAF scroll-linked effects (`SectionReveal`/`CtaScrollExpand`) still get native `scroll` events, so nothing else changed. Mounted **site-wide** in `[locale]/layout.tsx` (next to `<GlobalEffects/>`) — covers every route (marketing + dashboard/admin/auth). Guarded by `prefers-reduced-motion` (returns null → no Lenis). **`allowNestedScroll: true`** so the wheel scrolls genuinely-scrollable nested `overflow-y:auto` containers natively (`.payment-request-modal`, `.admin-modal-body`, `.docs-sidebar`/`.docs-toc`, `.forum-sidebar`, `.dash-sidebar`, `.blog-editor-preview`, `.buy-summary`, `.nav.mobile-open`) instead of hijacking the page — Lenis auto-detects them (scrollHeight > clientHeight). The MapLibre map is NOT an overflow-scroll container (wheel = JS zoom, scrollHeight == clientHeight) so auto-detect can't catch it → it keeps the explicit `data-lenis-prevent` on `CoverageMap`'s `.coverage-map` div. tsc 0; dev :8000 — `/en` `/en/b2b` `/en/pricing` `/en/docs` `/en/login` `/en/dashboard` `/zh` `/zh/admin` all 200, no module errors (forced fresh recompile → 0 new errors); `data-lenis-prevent` present in homepage SSR. NOT visually verified (autodebug needs the user to open the preview) — smoothness only shows in a real browser. Tuning: `lerp` in SmoothScroll.tsx (lower=floatier, higher=snappier); enable smooth anchor-link jumps with `options.anchors:true`. Revert to homepage-only: move `<SmoothScroll/>` from `layout.tsx` back to `page.tsx` + drop `allowNestedScroll`. Full revert: drop `<SmoothScroll/>` + its import from `layout.tsx`, delete `SmoothScroll.tsx`, remove `data-lenis-prevent` from CoverageMap, `npm rm lenis`.

  - 2026-06-09 followup — **推广到全站** (user `推广到全站`): moved the homepage-only `<SmoothScroll/>` into `[locale]/layout.tsx` + added `allowNestedScroll:true` (so modals/sidebars/admin-tables keep native wheel scroll). See the consolidated entry above.

  - 2026-06-09 followup — **锚点跳转也变平滑** (user `需要`): added an `<AnchorScrollHandler/>` child inside `<ReactLenis>` (uses `useLenis()` to grab the instance) that intercepts left-clicks on same-page anchor links and routes them through `lenis.scrollTo` for a smooth glide. **Lenis's built-in `anchors` option does NOT read `scroll-margin-top`** (only a fixed `offset`), which wouldn't match this site's per-id offsets (80/88/96px) or sub-nav pages — so instead the handler reads the target's own computed `scroll-margin-top` (getComputedStyle resolves the `calc()` → px) and passes `offset: -margin`, keeping the landing position IDENTICAL to the native jump, just smoothed. Same-page detection = normalized-pathname match AND `getElementById(hash)` exists in the current DOM (so cross-page `/en#models` links from b2b still let the router navigate). Skips: external origins, `target=_blank`, `download`, modified clicks (cmd/ctrl/shift/alt/middle). Updates the address bar via `history.pushState(null,'',hash)` (no second jump). tsc 0; `/en` `/en/b2b` `/en/pricing` `/zh` 200, no errors. NOT visually verified (autodebug down) — anchor smoothness only shows in a real browser. Revert: remove the `<AnchorScrollHandler/>` child + its function from SmoothScroll.tsx.

## Persistent reference (not session history — keep across sessions)

- **PUSH POLICY (settled):** push every commit with `git push fork main:design-draft-local`. Local `main` → fork branch `design-draft-local`. NEVER force-push or touch fork `main` (it holds 54 security-pin/contact-page commits that diverged from local at `8a4d15c` on 2026-05-11; local-only design work is the 400+ commits). `origin` = `anindya127/Design-draft` also diverged — don't push there.

- **Older still-relevant flags (do NOT reapply unless asked):** b2c `#features` section 4 has an `AppSlideshow` carousel — a separate phone slideshow in a `4/3` `.feature-image-placeholder:has(.app-slideshow)` card (do not confuse with the `Gallery3D` `#gallery` cluster). homepage hero `.hero.hero-centered .hero-centered-inner` uses `url('/images/hero-grid-bg.png') cover` — a CSS dot-grid was tried then REVERTED (`6ed2f2b`). b2c `.product-hero::before` light-mode dot field IS live (`d692ec4`, gold `rgba(214,158,0,0.55)` 2.5px/24px; pale-yellow on `#F1F2F4` ≈ invisible — use darker gold ≥0.4 alpha for texture on that bg); same selector also styles the b2b hero. Homepage yellow nav bar was reverted (`bcbd0aa`); `logo-globe.png` recoverable via `git show 8fef934`. b2c `.product-hero` is `min-height:calc(100svh - 42px)` flex-column with `.hero-footer-bar{margin-top:auto}` pinning the 3 stats to the fold (b2b shares `.product-hero` — verify if touching). The running `next dev` on :8000 can develop a dead file-watcher (container inotify limit) and serve a stale CSS bundle — if an edit doesn't show, kill it, `rm -rf .next`, restart `npm run dev`.
