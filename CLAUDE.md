# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-05-15 (diagram modal: more breathing room between title and illustration): user attached `/tmp/claude-paste-1778825310011-8c02a57d.png` (the B2C modal open showing "B2C 模式 — 直营运营生态" in a red box at the top, with the diagram illustration directly underneath; visual gap between title and diagram looks ~24-30px) and said "这里标题和插图的间隙再大一点" — increase the gap. ONE FILE EDITED: `src/components/sections/home/DiagramModal.css`. ONE rule changed: `.diagram-modal-header { top: -56px }` → `top: -100px`. The header is `position: absolute` (set in the prior session) and pinned at `top` measured against modal-content's top edge — moving it from -56 to -100 pushes the title 44px FURTHER above the modal frame, which translates to ~44px MORE visual gap between the title's bottom and the diagram's top (since the diagram's top edge stays anchored to modal-content's top + 4px body padding). Title font-size/weight/color/text-align UNCHANGED. NOTE TO FUTURE CLAUDE: if user wants EVEN MORE gap, push `top: -100px` → -130 or -150 (each -10px adds 10px more gap below the title). But on shorter viewports the title can clip ABOVE the visible viewport — the modal is centered vertically, so the available space ABOVE the modal frame is (viewportH - modalH) / 2. With modal width 1280px at 16:9 → modal height 720px. On a 900px viewport: space above = (900-720)/2 = 90px. Title at top:-100 would sit at y=-10 (CLIPPED 10px above viewport). On a 800px viewport (modal clamps to 752 via max-height): space above = (800-752)/2 = 24px. Title at top:-100 = y=-76 (heavily clipped). So `top: -100px` is borderline-safe for 900-1000px viewports and CLIPS on shorter ones. If user reports the title is cut off on a laptop, swap to `position: fixed; top: 110px; left: 0; right: 0` — pins the title to viewport regardless of modal centering, no clip risk. To DECREASE the gap (less space) push back toward -60 to -80. The 4px body padding on .diagram-modal-body could ALSO be tweaked if user wants more gap between modal-frame top and diagram (i.e. inside the frame) — bump 4px → 16-24px. Currently kept tight so the diagram fills the frame. PRIOR (different session): \\
- 2026-05-15 (pricing page cards: re-arrange the b2c/b2b plan cards into the modern stacked layout — title-left, oversized price, primary CTA stack BEFORE the feature checklist, divider above the checklist; keep all colors): user attached `/tmp/claude-paste-1778824444672-837ec629.png` (Chinese-locale `/pricing` page, "单运营商部署" section: two centered cards "定制品牌 Web APP" $300 and "APP 企业版" $16,900 — both with text-align:center, plan-name → price → period → (deposit-offer) → features-list → button-stack at the bottom in `.plan-actions` flex column) and `/tmp/claude-paste-1778824521980-8b09e1ff.png` (reference: "Business" / "Enterprise" cards left-aligned, with title at top → description → giant `$48/mo` price → full-width CTA button → thin divider → "Everything in Starter, plus:" heading → green-check feature list) and said "帮我把这里的卡片内容，布局排版改成这种 只更改布局排版，不改变颜色" — change the layout/composition only, keep all colors. SCOPE DECISION: the screenshot is the `/pricing` page (NOT `/b2c` — confused at first because /b2c has a similar Section 8 license block with the same `.pricing-card` markup, but the buttons "立即购买"/"联系销售" + "最受欢迎" ribbon + i18n key path `pricing.b2c.title` confirmed it's the pricing page). The shared `renderCard` in `pricing/page.tsx` builds BOTH the b2c (`b2cPlans`) and b2b (`b2bPlans`) sections, so the change applies to all 4 cards on the page. I did NOT touch the `/b2c` page's Section 8 cards (different file, different markup — those use `t('product.plan2.btn')` etc, no `.plan-actions` block, no popular ribbon). TWO FILES EDITED: (1) `src/app/[locale]/pricing/page.tsx` (renderCard at lines 35-75): moved `<div className="plan-actions">` block (the buynow + contactsales button stack) from BELOW `<ul className="plan-features">` to ABOVE it. The deposit-offer block (only rendered when `PLANS_WITH_DEPOSIT_OFFER.has(plan.keyBase)` for appent/webplat/appplat) stays where it was — between price-note and the new actions position. New DOM order is: ribbon → plan-name → plan-price → plan-price-unit → plan-price-note → (plan-deposit-offer) → plan-actions → plan-features. (2) `src/app/styles/sections.css`: NEW block "Pricing page (2-col) layout" inserted right before the existing "Add-on cards — icon + body row layout" section at ~line 3363. SEVEN new rules, ALL scoped to `.pricing-cards--2col .pricing-card` so the change ONLY affects the pricing page (the `.pricing-cards` wrapper used on the `/b2c` Section 8 license block has NO `--2col` modifier so it's untouched, and any other `.pricing-card` usage elsewhere — search confirmed only the pricing page uses `.pricing-cards--2col` as confirmed by grep). The seven rules: (a) base `.pricing-cards--2col .pricing-card` sets `text-align: left` and `padding: 36px 32px` (overrides the default `text-align: center; padding: 32px 24px`). (b) `.pricing-cards--2col .pricing-card.popular, ...featured` bumps `padding-top: 44px` to give the absolute-positioned ribbon (top: -12px) more breathing room from the now-larger plan-name underneath. (c) `.plan-name` gets `font-size: 1.35rem; margin-bottom: 10px` (was 1.1rem/8px) — bigger title to match the reference's prominent "Business"/"Enterprise" weight. (d) `.plan-price` gets `justify-content: flex-start; align-items: baseline; font-size: 3.25rem; line-height: 1.05; margin-top: 6px; margin-bottom: 4px; flex-wrap: nowrap` — flex-start aligns price to the LEFT (was justify-content: center), 3.25rem is significantly bigger than the default 2.25rem to match the reference's huge `$48` style; nowrap keeps "$300" and "/setup" together on one line. (e) `.plan-price-unit` (the "/起" or "/setup" suffix) bumped from 0.85rem → 0.95rem so it's readable next to the bigger price. (f) `.plan-price-note` gets `text-align: left; margin-bottom: 22px` — explicit left-align (the parent's text-align:left already covers it but being explicit is safer if cascade changes), and tighter bottom margin since the actions are now right below. (g) `.plan-deposit-offer` gets `margin-bottom: 22px` to match the price-note rhythm. (h) `.plan-actions` gets `margin-top: 0; margin-bottom: 24px; gap: 10px` — overrides the default `margin-top: auto` (which previously pushed the actions to the BOTTOM of the flex card; now they should sit naturally between the price block and the features list). (i) `.plan-features` gets `margin-top: 0; margin-bottom: 0; padding-top: 20px; border-top: 1px solid var(--border-subtle); flex-grow: 1` — the border-top is the visual divider matching the reference's thin separator above "Everything in Starter, plus:"; flex-grow:1 lets the features list fill the remaining card height so cards in a row stay equal-height. EFFECT: each card on `/pricing` now reads top-to-bottom as: ribbon (if popular/featured) → bold left-aligned plan name → giant left-aligned price with small unit → small left-aligned price note → (gold deposit-offer banner if deposit-eligible) → 22px gap → full-width "立即购买" primary CTA + "联系销售" secondary CTA stacked → 24px gap → 1px divider line → 20px gap → checklist of features with gold check marks. All colors UNCHANGED — primary `#FEBF1D` still on CTAs, ribbons, deposit-offer accent; check marks still `var(--primary)`; deposit-offer still has its gold gradient bg + border. `.btn-full` (the existing class on both CTA buttons) already gives them width:100% so they fill the card correctly. NOTE TO FUTURE CLAUDE: if user wants the divider COLOR changed, swap `var(--border-subtle)` for any other border token in the CSS rule (g). If user wants to drop the secondary "联系销售" CTA on the smaller cards (it visually doubles the CTA stack height), edit `renderCard` in pricing/page.tsx to conditionally render only the primary Link. If user wants the SAME stacked layout applied to the `/b2c` page's Section 8 license cards (the section that was confused as the target initially), edit `b2c/page.tsx:601` to add a `pricing-cards--2col` modifier class to the `<div className="pricing-cards">` wrapper AND move the single Link button to before the `<ul className="plan-features">` in BOTH cards (the manual JSX, not a renderCard helper). If user wants the price even BIGGER (matching the reference's `$48` prominence more closely), bump `.plan-price font-size: 3.25rem` → 3.75-4rem in rule (d). If user wants RIGHT-aligned price (some pricing designs put the price in the top-right corner), change `justify-content: flex-start` → `flex-end` in rule (d) and consider wrapping plan-name + plan-price in a flex row container. The `.pricing-cards--2col` selector specificity (0,2,0) is enough to win against the base `.pricing-card` rules (0,1,0) without any !important, so all overrides cascade cleanly. PRIOR (different session): 2026-05-15 (product nav: ALWAYS land at "系统概览"/Overview when entering /b2c or /b2b from anywhere): user said "不管从那一页跳转到产品页时，都默认从'系统概览'开始" — every entry into the product page should reset to the Overview hero, not preserve scroll. PRIOR ATTEMPT this same session added `<ScrollResetOnLoad />` to b2c/b2b pages, but user re-asked because edge cases remained: (a) the header dropdown link `<Link href={{ pathname: '/b2c', hash: 'overview' }}>` set URL to `/b2c#overview`, which made `window.location.hash === '#overview'` cause `ScrollResetOnLoad` to bail (the `if (window.location.hash) return;` guard); browser would then anchor-scroll to the `#overview` element, but since `.sub-nav { position: fixed; top: var(--header-height) }` plus the `--header-height` (~72px) and `.sub-nav-inner` (~42px) overlay the top of the hero, the anchor-scroll positioned the overview section's top edge BEHIND the fixed bars — visually looked like the page was scrolled slightly down. (b) Next.js App Router treats clicking a `<Link>` to the same pathname (`/b2c` when already on `/b2c`) as a no-op — no re-mount, no useEffect re-fire, so scroll stays wherever the user left it. THREE FILES EDITED: (1) `src/components/layout/Header.tsx`: dropped the `hash: 'overview'` from the b2c dropdown link (now plain `<Link href="/b2c">`). ADDED a new memoized handler `goToProductTop(target)`: a curried `useCallback` that returns an onClick which calls `closeMenu()` first, then checks `pathname.endsWith(target)` — if true (user is ALREADY on the page), it calls `window.scrollTo({ top: 0, behavior: 'smooth' })`. Used the `endsWith` check because next/navigation's `usePathname()` returns the FULL path INCLUDING the locale prefix (e.g. `/en/b2c` or `/zh/b2c`), and the targets are bare `/b2c` / `/b2b`. The `pathname` is from `usePathname()` already in the Header file at line 16, no new imports. Both dropdown links now use `onClick={goToProductTop('/b2c')}` and `onClick={goToProductTop('/b2b')}` instead of the bare `closeMenu`. (2) `src/components/effects/ScrollResetOnLoad.tsx`: tightened the hash bail-out — was `if (window.location.hash) return;`, now `if (hash && hash !== '#overview') return;`. So back-compat with any older cached link `/b2c#overview` (e.g. someone has it bookmarked or it's in their history) still lands at scrollTo(0,0) instead of the broken anchor-scroll-behind-fixed-subnav state. Deep links to OTHER anchors (e.g. `/b2c#features`, `/b2c#gallery`) still respect the hash and let browser scroll to that section. (3) NO change to b2c/b2b page.tsx files — they already had `<ScrollResetOnLoad />` from earlier in this session at the top of their return blocks. FLOW MATRIX (after fix): (A) Other page → click "Product" dropdown → goes to `/b2c` (no hash). Page mounts, ScrollResetOnLoad runs, no hash → scrollTo(0,0). Lands at Overview. ✓ (B) Already on `/b2c` (mid-scroll) → click "Product" dropdown → Next.js skips re-mount (same pathname, no hash diff), but the `goToProductTop` onClick fires, sees `pathname.endsWith('/b2c')`, scrolls window to top smoothly. ✓ (C) External link to `/b2c#overview` (old bookmark/cache) → page mounts, ScrollResetOnLoad sees `#overview`, bails old logic — now treats as no-hash and scrollTo(0,0). ✓ (D) Deep link `/b2c#features` (legitimate share to a specific section) → ScrollResetOnLoad sees `#features` ≠ `#overview`, bails, browser anchor-scrolls to features. ✓ NOTE TO FUTURE CLAUDE: if the smooth-scroll feels janky on the same-page click, change `behavior: 'smooth'` → `'auto'` (instant). If user later wants /pricing or /buy to also reset on entry, add `<ScrollResetOnLoad />` to those pages — the component now also handles the `#overview` no-op edge case so it's safe to copy to any page that has an overview-style top section. If user adds MORE product variants (e.g. /b2g, /enterprise), extend `goToProductTop`'s type union `'/b2c' | '/b2b'` and add new dropdown links. The mobile menu accordion uses the same `<Link>` components so the fix also applies to mobile nav. PUSH STATUS: prior sessions' commits remain unpushed to fork ("could not read Username for https://github.com" — git credential helper unconfigured). This session's commits share that fate until credentials are added. PRIOR (different session): \\
- 2026-05-15 (trusted-bar marquee: balance EVCity logo size — bump it bigger, shrink the others slightly): user attached `/tmp/claude-paste-1778824672583-dbf45d97.png` (the homepage trusted-bar manufacturer logos marquee scrolling left-to-right: DELIXI / LiFud / 驴充充 / WILLDIGITS / wolun / TOPSTAR / Fullwatt / XUPERNOVA / UNIT / [-EVo- in a red box, visibly smaller and more cramped than the others] / DELIXI / LiFud — all logos rendering at the same 52px height with `width: auto`, but EVCity's source PNG is roughly SQUARE so at 52×52px its footprint is small while the wider logos render ~52×120-180px and dominate visually) and said "这里红框的logo图标放大一点，其他的稍晚缩小一点，视觉上要差不多大小" — boost EVCity, shrink others slightly, equalize visual weight. ROOT CAUSE: in `src/app/styles/pages.css:2933-2939` `.marquee-item img { height: 52px; width: auto; max-width: 200px; object-fit: contain; display: block }` applied a uniform height to ALL marquee images. Since `width: auto` lets each image keep its aspect ratio, a square source (evcity.png ~1:1 aspect) renders 52×52 = small while wide horizontal logos (delixi.png, lifud.png, etc — most are ~3:1 to 4:1 aspect) render 52×150-200px. The visual weight is dominated by WIDTH, not height — EVCity at 52×52 looks tiny compared to a 52×180 DELIXI even though their heights are identical. Solution: use attribute selector `img[alt="..."]` to override JUST the EVCity logo, AND drop the default height so the wide logos render slightly smaller. ONE FILE EDITED: `src/app/styles/pages.css`. TWO rule changes: (1) `.marquee-item img { height: 52px }` → `height: 44px` (15% smaller across the board — shrinks all 10 logos uniformly). The default height drop alone won't equalize EVCity since the source is square; this just shrinks the wide logos enough to make the gap smaller. `max-width: 200px` UNCHANGED — most logos hit width-based clamp on their own well below 200px anyway since 44px height × 3-4 aspect ratio = 132-176px wide. (2) NEW RULE `.marquee-item img[alt="Evcity"] { height: 72px; max-width: 110px }` — pinpoint override for the EVCity logo. The `alt="Evcity"` selector matches the `<Image alt={name}>` in page.tsx where the MANUFACTURER_LOGOS array has `{ name: 'Evcity', src: '/images/manufacturers/evcity.png' }` at line 181. Height 72px is 64% TALLER than the new 44px default — picked to roughly equalize the visible glyph size since EVCity has more whitespace baked in than other logos. `max-width: 110px` caps the resulting box width (at 72px tall × ~1:1 aspect, the natural width is also ~72px, but adding a cap prevents the image from getting compressed if the source aspect is slightly off). The 72px tall image fits inside the marquee container which is also 72px tall (`.trusted-bar .marquee { height: 72px }` at pages.css:2906) — EVCity will now fill the marquee strip vertically while the others sit at 44px (61% of strip height) with vertical breathing room from `display: inline-flex; align-items: center` on `.marquee-item`. EFFECT: EVCity grows from 52×52 → 72×72; other 9 logos shrink from 52×~150 → 44×~127. The visual weight gap closes — EVCity now reads as roughly comparable size to e.g. UNIT or XUPERNOVA in the same strip. NOTE TO FUTURE CLAUDE: if user says EVCity is now TOO BIG, drop `height: 72px` → 60-64px. If user says the others are TOO SMALL after the shrink, bump default `height: 44px` → 48-50px (and proportionally bump EVCity to keep the ratio). If user wants ALL logos at the SAME exact perceived weight (not just close), the more robust solution is to re-export the source PNGs with consistent padding/aspect (e.g. crop EVCity to be roughly 3:1 by adding horizontal whitespace, or crop the wide logos to be roughly 2:1) — that's an asset-side fix rather than CSS. The `alt="Evcity"` attribute selector is fragile to typos: if the page.tsx `name: 'Evcity'` is later changed to `'EvCity'` or `'EVCITY'`, the CSS rule will silently stop matching. To make this more robust, consider adding a className like `marquee-item--evcity` to the wrapping `<span>` in page.tsx and selecting on that — but the attribute selector works fine while the alt stays stable. Similarly if user wants per-logo size tuning for OTHER logos (e.g. LV C-CHONG donkey mascot is also somewhat square-ish), add `.marquee-item img[alt="LV C-CHONG"] { height: 56px }` etc. The duplicate-array pattern in page.tsx (`MANUFACTURER_LOGOS.map((logo) => (...))` rendered twice into the marquee track) means each logo appears TWICE in the DOM — the attribute selector matches BOTH copies (the original and the wrap-around clone for the seamless loop), so the size override applies consistently throughout the scroll cycle. PRIOR (different session): \\
- 2026-05-15 (hero TypingText "CSMS Platform": kill the gold gradient, render as solid pure yellow `#FEBF1D`): user attached `/tmp/claude-paste-1778824122592-bc4e1e17.png` (Chinese-locale homepage hero with red box around the typewriter accent "CSMS Platform" — currently rendering with a `linear-gradient(120deg, #FEBF1D 0%, #FFD56B 45%, #FEBF1D 100%)` clipped to text, so the phrase shows a subtle gradient sheen where the middle stop `#FFD56B` is a lighter, paler yellow than the brand-primary `#FEBF1D` at the start/end stops) and said "红框这里的字体颜色，改成纯黄色，不要有渐变" — replace the gradient with a flat, uniform brand-primary yellow. ROOT CAUSE: in `src/app/styles/sections.css:123-131` the rule `.hero-with-video .hero-title .typing-text, .hero-with-video .hero-title .text-gradient` used `background: linear-gradient(120deg, #FEBF1D 0%, #FFD56B 45%, #FEBF1D 100%)` combined with `-webkit-background-clip: text` + `-webkit-text-fill-color: transparent` to paint the text with a 3-stop gold gradient (the `#FFD56B` middle stop is what gave the visible color shift across the text width). The TypingText component (`src/components/effects/TypingText.tsx:99`) ALSO applies a `.gradient-text-animated` class which has its own `linear-gradient(90deg, #E6A817, #D4890A, #F5C946, #E6A817)` + `animation: gradientShift 4s ease infinite` defined in `enhancements.css:135-142` — but the sections.css selector `.hero-with-video .hero-title .typing-text` (specificity 0,3,0) overrides `.gradient-text-animated` (0,1,0) cleanly via cascade, so only the sections.css rule needed editing. ONE FILE EDITED: `src/app/styles/sections.css`. ONE rule (`.hero-with-video .hero-title .typing-text, .hero-with-video .hero-title .text-gradient`) modified. FIVE prop changes: (1) `background: linear-gradient(120deg, #FEBF1D 0%, #FFD56B 45%, #FEBF1D 100%)` → `background: none` — kills the 3-stop gradient entirely. (2) `-webkit-background-clip: text; background-clip: text` → `-webkit-background-clip: initial; background-clip: initial` — resets clip so the text fill applies normally (otherwise a transparent fill on text-clipped-bg with no bg = invisible text). (3) `-webkit-text-fill-color: transparent` → `-webkit-text-fill-color: #FEBF1D` — paints the text with brand-primary bright yellow, the exact same color as the start/end stops of the OLD gradient and the same color used by `.btn-primary` hero CTAs, active sub-nav tabs, and matrix CTA buttons site-wide. (4) `color: transparent` → `color: #FEBF1D` — fallback for browsers that don't honor `-webkit-text-fill-color` (older Firefox), they'll use the `color` prop. (5) `text-shadow: 0 2px 18px rgba(254, 191, 29, 0.35)` UNCHANGED — preserves the subtle gold glow halo behind the text. EFFECT: "CSMS Platform" now renders as a single uniform `#FEBF1D` across the entire phrase, no visible color transitions. The cursor `|` blink from `.typing-text::after` (enhancements.css:151-154) has no explicit color rule so it inherits `color` from the parent — now blinks as `#FEBF1D` bright yellow too. The `animation: gradientShift 4s ease infinite` from `.gradient-text-animated` still runs but is visually inert since `background: none` means there's no `background-position` to animate. The `min-height: 1.2em; min-width: 380px` typing-text layout sizing (enhancements.css:145-150) untouched — still prevents layout shift when the text content swaps between the 4 rotating phrases ('CSMS Platform', '管理系统', 'Charge Hub', '充电平台'). NOTE TO FUTURE CLAUDE: if user wants the SHADOW removed too (currently a subtle gold glow `text-shadow: 0 2px 18px rgba(254,191,29,0.35)`), drop the text-shadow line — "纯黄色" could be interpreted as "pure yellow no other effects"; I kept the shadow because it's subtle and gives depth, user didn't explicitly call it out. If user wants the cursor `|` a DIFFERENT color than the text (e.g. dark cursor on yellow text), add `.hero-with-video .hero-title .typing-text::after { color: var(--text-primary); -webkit-text-fill-color: var(--text-primary) }`. If user wants the yellow to be a DIFFERENT SHADE (darker `#E6A817` matching polish.css's `--primary-dark`, or brighter `#FFCC33`), swap BOTH `-webkit-text-fill-color` AND `color` to the new value — keep them in sync. If user wants the gradient BACK but with different/slower stops, restore `background: linear-gradient(...)` + `background-clip: text` + transparent fills — all five props need to flip together. The `.contact` page (`src/app/[locale]/contact/PageClient.tsx:142`) ALSO uses `gradient-text-animated` for its title but is UNAFFECTED by this fix because the `.hero-with-video` ancestor selector doesn't match there — that page still shows the animated 4-stop gradient. The TypingText also appears in `/b2c` and `/b2b` hero pages; those DO use `.hero-with-video` so they share this fix (their rotating brand phrases now also render as flat `#FEBF1D`). PRIOR (different session): 2026-05-15 (diagram modal: float title above modal frame + enlarge illustration 1000→1280px) — moved title via position:absolute top:-56px, widened modal to 1280px, relaxed overflow:visible on modal-content; details in commit ff0b0cf.
