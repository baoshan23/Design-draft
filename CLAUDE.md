# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session


- 2026-05-20 — **Homepage `/[locale]` payment matrix: 3D sphere dropped → flat-grid fisheye lens** (commit `c8dacf7`; `tsc` 0, sections.css braces 859/859). User: "不做球体效果，整体平铺，中间做鱼眼凸起，放大的效果，中间放大，四周小，滑动交互，logo之间不交叠" + ramp-style grid screenshot. Removed `PaymentSphere.tsx` (3D Fibonacci sphere), added `PaymentFisheye.tsx` and `.payment-fisheye*` CSS. Layout: CSS-grid `repeat(8,1fr)` of `aspect-ratio:1/1` cells (8×6=48 desktop; 6×8 ≤900; 4×12 ≤640) — fixed slots = non-overlap by construction. Each `.payment-fisheye-tile` is `width/height:100%` of cell with `padding:14%` (proportional inset) + transform `scale()` (so logo scales with tile). Component: rAF loop computes per-tile Gaussian `exp(-(d² )/(2σ²))` with σ=1.7 from focus point in grid coords; `scale = 0.32 + g*(0.92-0.32)` and `opacity = 0.22 + g*0.78`. `MAX_SCALE 0.92` (<1) leaves a visible gap between adjacent centre-cluster tiles. Cursor over grid sets `targetFx/Fy` (clamped to `[0, cols-1]`); pointerleave eases back to grid centre; focus eased by `0.18` per frame. Column count read live via `getComputedStyle(gridTemplateColumns).split(/\s+/).length` so the JS reacts to breakpoints; `ResizeObserver` re-measures and re-centres if at rest. `prefers-reduced-motion` → static centred lens, no loop, no pointer. `page.tsx` import + `<PaymentSphere>` → `<PaymentFisheye>` (same items prop). Revert: `git revert c8dacf7` (restores PaymentSphere.tsx + sphere CSS). **NOT VISUALLY VERIFIED in-browser.** ───── EARLIER (same thread, moot): **Homepage `/[locale]` payment sphere: logos enlarged 2x** (commit `7114443`; `tsc` 0, sections.css braces 860/860; pushed fork `design-draft-local`). User: "logo放大到现在的2倍". Doubled the tile/node so the rendered logo doubles in area; grew the stage and trimmed the depth² front-scale coefficient so the larger tiles still don't collide. Edits: `PaymentSphere.tsx` radius factor `0.46→0.50`, scale formula `0.34+d2*0.98 → 0.34+d2*0.86` (front max 1.32→1.20). `sections.css` `.payment-sphere` height `620→940`, `.payment-sphere-node` `92→184px`, `.payment-sphere-tile` padding `9→18`, border-radius `20→28`, shadow `8/22→10/28`; responsive ≤900 stage `520→780` node `78→156` padding `8→16` radius `16→24`; ≤640 stage `440→640` node `64→128` padding `7→14` radius `14→20`. Front-cluster geometry check (desktop): tile = 184×1.20 = 221px, neighbour spacing ≈ radius*0.49 = 230px → ~10px clearance. Narrower than ~1000px viewport the ≤900 media kicks in and rescales (156*1.20=187 vs spacing 191). Revert this step: restore 184→92 / 156→78 / 128→64 + the padding/radius/stage triples, radius factor 0.46, scale coef 0.98. **NOT VISUALLY VERIFIED in-browser.** ───── EARLIER (same thread): **Homepage `/[locale]` payment sphere refined per ref [img]: borderless tiles, logo fills tile, strong centre-big/edge-small bias, no overlap** (commit `50445ce`; `tsc` 0, sections.css braces 859/859; **push status: see prior bullet's push-retry note — same fork `design-draft-local`**). User: "整体是球形展示效果，色块中间大，四周小，logo占满背景的色块，色块无边线，且不交叠" + ramp-grid screenshot. `PaymentSphere.tsx`: radius factor `0.42→0.46` (wider spacing); scale/opacity now **depth-squared** (`d2=depth*depth`) → `scale=0.34+d2*0.98` (front big ~1.32, rim ~0.58, back ~0.34), `opacity=0.10+d2*0.90` (rim/back strongly faded) — gives the 中间大四周小 sphere look AND shrinks the crowded silhouette so tiles don't overlap. `sections.css` `.payment-sphere-tile`: `border:0` (色块无边线), `padding:18px→9px` (logo占满色块), `border-radius:22→20`, softer shadow `0 8px 22px -16px rgba(28,40,80,.26)`; responsive paddings ≤900 `15→8`, ≤640 `12→7`. Revert this step: restore `border:1px solid var(--border-subtle)`, padding 18/15/12, and the linear `scale=0.5+depth*0.8`/`opacity=0.32+depth*0.68` + radius `0.42`. **NOT VISUALLY VERIFIED in-browser.** ───── EARLIER SAME SESSION (trail): **Homepage `/[locale]` "Payment Partners" matrix → Ramp-style interactive 3D logo sphere** (commit `8501546`; `tsc` exit 0, sections.css braces 837/837; **push to fork `design-draft-local` FAILED — TLS handshake error, sandbox network; commit is local-only, RETRY `git push fork main:design-draft-local` when network allows**). User: screenshot of the homepage `.payment-orbit` 48-logo flat floating grid + ramp.com ref + red-box screenshot of ramp's "200+ Integrations" sphere → "做成球形动态交互效果，鼠标悬浮时球形可滚动展示". The matrix lives on the HOMEPAGE (`/[locale]/page.tsx` "Payment Partners Section", NOT b2c — b2c's `product.pay` section is tag-pill cards, untouched). Changes: (1) NEW client comp `src/components/sections/home/PaymentSphere.tsx` — 48 `PAYMENT_METHODS_FLAT` icons placed on a Fibonacci-lattice unit sphere; rAF loop applies Y-then-X rotation, projects to `translate3d(px,py,0) translate(-50%,-50%) scale()`, depth→opacity(0.32–1)/zIndex/scale(0.5–1.3); idle = slow Y drift (`IDLE_VY 0.0024`), on `pointermove` over the stage cursor offset from center sets target angular velocity (eased, `MAX_SPEED 0.05`, rotX clamped ±1.15) TagCanvas-style, `pointerleave`/window-blur → decay to idle; `prefers-reduced-motion` → static sphere, no loop, no pointer. Per-frame writes inline styles on cached refs (no React re-render). Icons passed server→client as `ReactNode` props (standard RSC pattern, same `PAYMENT_ICONS` proxy as before). (2) `page.tsx`: import + replaced the `.payment-orbit`/`PAYMENT_BUBBLE_POSITIONS.map` block with `<PaymentSphere items={PAYMENT_METHODS_FLAT.map(name=>({name,icon:PAYMENT_ICONS[name]}))} />`; deleted the now-unused `PAYMENT_BUBBLE_POSITIONS` array + comment. (3) `sections.css`: deleted the entire old `.payment-orbit`/`.payment-bubble*`/`paymentBubbleFloatA-C` block + its media queries (was between `/* --- Payment Partners --- */` and `/* --- CTA Section --- */`); added new `.payment-sphere*` block in the same place — `.payment-sphere` 620px stage `cursor:grab`, `.payment-sphere-node` 92px abs, `.payment-sphere-tile` rounded white card (`var(--dark-card)`+`--border-subtle`+`radius:22px`+soft shadow, ramp look), `:hover` gold drop-shadow + `z-index:200!important` (beats JS inline zIndex), inner svg/img fill rules carried over; ≤900 → 520px/78px, ≤640 → 440px/64px. **Caution recorded:** a first attempt deleted by stale line numbers and clobbered unrelated `language-grid` reduced-motion + `[data-theme=dark]` CSS — recovered via `git checkout -- sections.css` then re-deleted by content match (verified `language-grid` 12 refs intact, no `payment-orbit/bubble` refs anywhere). Revert all: `git revert 8501546` (or restore the `.payment-orbit` JSX+`PAYMENT_BUBBLE_POSITIONS` in page.tsx, delete `PaymentSphere.tsx`, swap `.payment-sphere*` CSS back to `.payment-orbit*`). **NOT VISUALLY VERIFIED in-browser.** ───── EARLIER SAME SESSION (moot, trail): **Homepage `/[locale]` first-screen (hero) background image swapped** (CSS-only, committed + pushed fork `design-draft-local`). User: "将这张图，替换到首页首屏背景图" + screenshot (yellow/gold gradient w/ Global Charge dashboard mock, 3840×1600). New img copied to `public/images/hero-bg-6.png`; `pages-extracted.css:2447` (the `html:not([data-theme=dark]) .hero.hero-centered, html[data-theme=dark] .hero.hero-centered` rule, the ONLY hero-centered bg) `url(/images/hero-bg-5.png)` -> `url(/images/hero-bg-6.png)` (sole change; `background-color:#f5f6f8` cover/center-top kept). hero-bg-5.png left in place (recoverable; revert = point url back). No other `hero-bg-5` refs. **NOT VISUALLY VERIFIED in-browser.** ───── EARLIER SAME SESSION (moot, trail): **b2c (`/[locale]/b2c`) `#features`: the switchable showcase was FULLY REVERTED — original 4 stacked `feature-row` blocks restored, no tabs** (committed + pushed fork `design-draft-local`, forward-only; tsc PASS, sections.css braces 829/829, en/zh JSON valid). User: "恢复原来的展示方案，不做切换了" + screenshot of the live showcase. Undo path: `git checkout 3c61b92 -- b2c/page.tsx` — `3c61b92` is the parent of showcase commit `5f1e437`; net page.tsx diff `3c61b92..HEAD` was ONLY the showcase import+block (the #multilingual edits `dceacc6`/`0f0d8f8` net-cancelled), so the wholesale restore is collateral-free → brings back the 4 `<ScrollAnimation>` `feature-row` blocks (ui mt:80; brand `reverse b2c-feat-shift` mt:40; APP/H5 `feature-row` mt:40 w/ `<AppSlideshow>`; ops `reverse b2c-feat-shift` mt:40 fixed 666×500). Also `git rm` `b2c/FeatureShowcase.tsx` + `public/images/b2c-showcase-bg.png`; deleted the `.b2c-showcase*` CSS block from `sections.css` (was ~2621-2776, sits between `.feature-list li svg` and `/* --- Business Models Section --- */`); removed the 4 `product.{ui,brand,app,ops}.tab` i18n keys from en.json+zh.json (other unrelated `"tab"` keys left intact). b2c `#features` now == its pre-showcase layout. To rebuild the showcase later: `git show 5f1e437` (initial) / `b0b533c` (left-rail) for component+CSS+i18n. **NOT VISUALLY VERIFIED in-browser** — robust-by-construction (pure structural restore from a known-good commit). ───── EARLIER SAME SESSION (now moot, kept for trail): **REVERTED the b2c `#multilingual` capsule-grid removal** (commit `0f0d8f8`, pushed fork `design-draft-local` per PUSH POLICY, no force; local-bin tsc exit 0). User: "取消上一步操作" — undo my immediately-prior change `dceacc6` (it had swapped the b2c "20+ 种全球语言" `.language-grid` → a plain `.b2c-lang-plain` inline list). Files were still in the post-`dceacc6` state (concurrent commits didn't touch this block). REVERT = exact inverse, 2 files: (1) `b2c/page.tsx:403` `className="b2c-lang-plain"` → `"language-grid"` (role/aria/28 spans untouched); (2) `sections.css` — deleted the whole `.b2c-lang-plain` block + its inserted comment, restored the lone `/* --- Language Grid … --- */` + `.language-grid {` header. Net = identical to pre-`dceacc6`; `#multilingual` renders the shared homepage-style bordered capsule grid again. `grep b2c-lang-plain` = 0 both files; /zh/b2c HTML has `language-grid`; tsc exit 0. Re-apply later: `git show dceacc6`. **NOT VISUALLY VERIFIED in-browser** — robust-by-construction (pure inverse of a 2-file diff). ───── PRIOR: **b2c (`/[locale]/b2c`) `#features`: 4 product blocks (`product.{ui,brand,app,ops}`) -> ONE switchable full-screen showcase, then redesigned to a left vertical rail on a full-bleed soft-gradient band** (committed + pushed fork `design-draft-local`; `tsc` PASS, braces 859/859). STEP 1 (user: "做成一个可切换展示的区域...完整的一屏"): NEW client comp `b2c/FeatureShowcase.tsx` (ARIA tablist, roving arrows, 4 panels SSR + `hidden` toggle); `page.tsx` swapped the 4 stacked `<ScrollAnimation>` `feature-row` blocks for `<ScrollAnimation><FeatureShowcase panels={[…]}/>` (nodes verbatim, dropped `reverse`/`b2c-feat-shift`, removed ops fixed 666×500 keeping aspect); i18n `product.{ui,brand,app,ops}.tab` short labels EN/ZH (both JSON valid). STEP 2 (user: "把切换栏做到左边竖向...增加这一屏的背景[img1]...切换样式参考[img2]"): bg image `/tmp/...` copied to `public/images/b2c-showcase-bg.png` (light blue-white soft shapes, 3192×1695). `FeatureShowcase.tsx` restructured: `.b2c-showcase > .b2c-showcase-inner > .b2c-showcase-card` (grid `248px 1fr`) with `.b2c-showcase-nav` (vertical `role=tablist aria-orientation=vertical`, Up/Down+Left/Right arrows) left + `.b2c-showcase-stage` right. `sections.css` `.b2c-showcase*` block (after `.feature-list li svg`, b2c-only) rewritten: `.b2c-showcase` full-bleed `width:100vw;margin-inline:calc(50% - 50vw)`, `min-height:calc(100svh - var(--header-height))`, bg `#eef3fc` + `url(/images/b2c-showcase-bg.png) cover center`; white card `var(--dark-card)` + `--border-subtle` + soft shadow `0 24px 60px -28px rgba(28,50,110,.28)` `radius-xl`; nav tabs text-left rounded, active = `var(--primary-dim)` bg + `--text-primary` (ref [img2] vertical list style); ≤900 card -> 1-col, nav -> horizontal scroll row. STEP 3 (user: "左边切换栏，无卡片限制，只有右边展示才有" + "整体展示高度改成960"): card chrome (bg/border/shadow/radius) moved OFF `.b2c-showcase-card` (now just grid `216px 1fr; gap:28px`, transparent) and `.b2c-showcase-nav` (dropped `border-right`, `align-self:center`, naked on the band) ONTO `.b2c-showcase-stage` only (`var(--dark-card)`+`--border-subtle`+`radius-xl`+soft shadow, padding 44/52). `.b2c-showcase` `min-height` `calc(100svh - var(--header-height))` -> fixed `720px` (set 960 then 720 per follow-ups). ≤900: card 1-col gap16, nav horizontal scroll naked (border-bottom divider removed). Braces 855/855, tsc PASS. To revert STEP 2/3: vertical->top tab bar by reverting this `.b2c-showcase*` CSS block + `FeatureShowcase.tsx` markup (git show <prev>); delete `public/images/b2c-showcase-bg.png`. To revert all: restore 4 `feature-row` blocks in `page.tsx`, delete `FeatureShowcase.tsx` + `.b2c-showcase*` CSS + 4 `.tab` i18n keys + bg png. **NOT VISUALLY VERIFIED in-browser** — robust-by-construction (b2c-only selectors, standard `calc(50% - 50vw)` full-bleed, theme-aware tokens, ARIA vertical tablist, SSR content preserved). ───── SEPARATE concurrent thread (NOT the showcase above): b2c `#features` 6-feature ff1–ff6 `.b2c-feature-*` cards (CSS-only, `sections.css`) — card grid + tinted->solid-yellow icon tile + borderless/hover-shadow + black line icons. Latest: user "将这里的图标增加一点点投影，参考'在线演示'的黄色图标效果" → added `box-shadow:0 0 20px rgba(254,191,29,0.35)` to `.b2c-feature-cell .feature-card-icon` (exact `/demo` `.demo-edition-card-icon-gold` gold glow). Braces 859/859, pushed fork. Revert: drop that box-shadow line. THEN user "把这里的语言展示方式改成这种" + Mercury logo-grid ref → b2c `#multilingual`: `page.tsx` only the grid `className "language-grid"->"b2c-lang-grid"` (role/aria + 26 `<span listitem>` verbatim; UNSHARES from the homepage `.language-grid` mirror which is left intact). `sections.css`: new `.b2c-lang-grid*` block placed right before `.language-grid` (~2884) — clean white grid (`var(--dark-card)`), `repeat(6,minmax(0,1fr))`, hairline cell `border-right/bottom:var(--border-subtle)` (theme-aware), `border-radius:20px;overflow:hidden`, `:nth-child(6n)` right-border reset, `span::after{content:'+'}` at each cell top-left = "+" intersection marks (edge ones clipped by rounded overflow), muted `--text-secondary` text, subtle hover; ≤900→4col ≤560→3col nth resets + dark hover/'+' overrides + reduced-motion. tsc OK, braces 847/847, pushed fork. Revert: className back to `language-grid` + delete the `.b2c-lang-grid*` block. THEN user "左边增加一个黄色色块紧贴网格，把主标题和副标题放进色块": `page.tsx` wrapped the header + grid in `<div class="b2c-lang-panel">`, header `.section-header` -> `.b2c-lang-aside` (label/title/desc moved inside, verbatim t() keys); LanguageRequestForm stays outside the panel. `sections.css` (before `.language-grid`): `.b2c-lang-panel` grid `minmax(240px,340px) 1fr`, max-width 1100, owns the `border-radius:20px;overflow:hidden;border` frame; `.b2c-lang-aside` solid `var(--primary)` yellow, dark `#181818` text, flex-col centered, `.section-label` de-pilled to flat black-alpha on yellow; `.b2c-lang-panel .b2c-lang-grid` neutralised (`max-width:none;margin:0;border:0;border-radius:0`) but keeps its own `overflow:hidden` so edge "+" don't bleed onto the aside; ≤760 stacks (aside on top, centered). tsc OK, braces 857/857, pushed fork. Revert: unwrap panel/aside in page.tsx (header back to `.section-header`) + delete `.b2c-lang-panel`/`.b2c-lang-aside` CSS. THEN user "标题内容位于色块顶部，间隙调整": `.b2c-lang-aside` `justify-content:center -> flex-start` (content top-aligned), `gap:14->16`, `padding:44px 38px -> 40px 38px`. Single rule, braces 857/857, pushed fork. THEN user "红框样式参考这里" (red box = the `多语言` `.section-label` in the aside; ref = /demo `.demo-hero-eyebrow` pill+dot): `sections.css` `.b2c-lang-aside .section-label` restyled to the demo eyebrow pill — `padding:8px 18px;border:1px solid rgba(255,255,255,.7);border-radius:999px;background:rgba(255,255,255,.3);color:#5c3f00;font-size:.78rem;letter-spacing:.1em;gap:10px` + override the inherited global `.section-label::before` dot (polish.css) to `8px white + box-shadow 0 0 12px rgba(255,255,255,.85)` (content/border-radius/`polish-label-pulse` anim inherited). Braces 858/858, pushed fork. Revert: restore the prior 3-line `.b2c-lang-aside .section-label{align-self;background:rgba(0,0,0,.1);color:#181818}` + drop the `::before` override. THEN user "调做成1440宽 + 把[LanguageRequestForm]放进左边黄色块": `page.tsx` moved `<LanguageRequestForm/>` from after `.b2c-lang-panel` to INSIDE `.b2c-lang-aside` (after the desc `<p>`); removed the now-empty trailing form block. `sections.css`: `.b2c-lang-panel` `max-width:1100->1440`, aside track `minmax(240px,340px)->minmax(320px,440px)` (room for the form); added `.b2c-lang-aside .lang-request-*`/`.form-*`/`.btn*` overrides — strip the white `.card` chrome (transparent, no border/shadow/pad) + a `border-top:1px rgba(0,0,0,.12)` divider, dark text, `.form-row` -> column (narrow col), frosted inputs `rgba(255,255,255,.6)`, dark CTA `#181818` (gold btn would vanish on yellow). tsc OK, braces 848/848, pushed fork. Revert: move `<LanguageRequestForm/>` back below the panel + delete the `.lang-request-*` aside overrides + panel max-width/track back to 1100/340. THEN user "把标题内容和效果恢复到原来中间的位置": `page.tsx` moved the `<span section-label>/<h2>/<p>` OUT of `.b2c-lang-aside` back into a centered `<div class="section-header">` placed before `.b2c-lang-panel` (original position; global `.section-header`/`.section-label` styling auto-restores the original gold-pill + pulse effect since the `.b2c-lang-aside …` overrides no longer match). `.b2c-lang-aside` now holds ONLY `<LanguageRequestForm/>`. `sections.css` `.b2c-lang-aside .lang-request-card` dropped the now-pointless top divider (`border-top`+`padding-top:22px`+`margin-top:8px` -> `margin:0`); the unused `.b2c-lang-aside .section-label/h2/p` overrides left in place (harmless, no matching els). NOTE: changes were swept into concurrent commit `97ba6df` by another session's broad `git add`; verified present in HEAD, pushed fork. tsc OK, braces 860/860. Revert: move the 3 title els back into `.b2c-lang-aside` (before the form), delete the standalone `.section-header`. THEN user "这里的内容去掉容器限制" (red box = the LanguageRequestForm in the yellow aside): `sections.css` — `.b2c-lang-aside .lang-request-card` added `max-width:none; width:100%` to drop the global `.lang-request-card{max-width:600px}` (pages.css:551) constraint; `.b2c-lang-panel grid-template-columns: minmax(320px,440px) 1fr -> minmax(380px,560px) 1fr` (wider aside so the form gets more horizontal room without touching grid quality). Braces 859/859, pushed fork. Revert: drop `max-width:none/width:100%` + aside track back to 320/440.

  **PUSH POLICY (settled):** push every commit with `git push fork main:design-draft-local`. Local `main` → fork branch `design-draft-local`. NEVER force-push or touch fork `main` (it holds 54 security-pin/contact-page commits that diverged from local at `8a4d15c` on 2026-05-11; local-only design work is the 400+ commits). `origin` = `anindya127/Design-draft` also diverged — don't push there.

  **Current b2c `/[locale]/b2c` state (3 same-day changes, all live):**
  1. `a1d0d59` — moved the `{/* APP / H5 */}` ScrollAnimation block to sit right after the "Your Brand"/`product.brand` `feature-row reverse` block and before `{/* Exclusive UI Customization & Zero-Touch Config */}`; its wrapper `marginTop` is `40`. Section order in Section 4 `.container`: ui → brand(reverse) → APP/H5 → uicustom/zeroconfig grid-2 → Payment Matrix → 20+ Languages(#multilingual) → Smart Ops.
  2. `385ca3a` — sized the slideshow box to match the brand image above: `src/app/styles/sections.css` `.feature-image-placeholder:has(.app-slideshow)` now `aspect-ratio:4/3` (was `aspect-ratio:auto; min-height:520px`), plus phone-fit sub-rules `.app-slideshow{container-type:size; padding:8px 0}`, `.app-slideshow-track{width:auto; height:calc(100cqh - 36px)}` (cqh-responsive, derives width from base `aspect-ratio:9/19.5`), `.app-slideshow-dots{margin-top:10px}`. If phone too small → bump `100cqh - 36px` toward `- 20px` (don't change the 4/3 — it intentionally matches the row above).
  3. `28f1535` — (a) same `:has(.app-slideshow)` rule `background:transparent` → `#F1F2F4` (rounded card matching the OEM "Your Brand" row above). (b) Slide mechanic changed from cross-fade to a real horizontal carousel: added `.app-slideshow-screen{position:absolute;inset:10px;border-radius:40px;overflow:hidden;z-index:1}` (clip box inside the phone bezel — the `.app-slideshow-track` bezel keeps `overflow:visible` so its `::after` dynamic-island notch at z-index:3 stays over the screen) + `.app-slideshow-rail{display:flex;width/height:100%;transition:transform 600ms cubic-bezier(.16,1,.3,1)}`; `.app-slideshow-slide` is now `flex:0 0 100%` (no more absolute/opacity); reduced-motion now kills `.app-slideshow-rail` transition. `AppSlideshow.tsx`: slides wrapped in `<div class="app-slideshow-screen"><div class="app-slideshow-rail" style={{transform:translateX(-index*100%)}}>…</div></div>`, no per-slide `is-active` class. Auto-advance 2000ms + dots unchanged. `npx tsc --noEmit` PASSES.
  4. `035c84c` — fixed the wrap quirk: `AppSlideshow.tsx` now appends a clone of `SLIDES[0]` after the 9 real slides (`CLONE_INDEX = SLIDES.length`); `index` increments without modulo (`advance` = `i+1`), and an `onTransitionEnd` on the rail detects `index === CLONE_INDEX` → sets `animate=false` + `index=0` (rail jumps back to slide 0 with `transition:'none'` so it's invisible), then a double-`requestAnimationFrame` effect re-enables `animate` for the next move. Dots use `realIndex = index % SLIDES.length`. Result: loop slides forward seamlessly, no backward sweep. `npx tsc --noEmit` PASSES.
  5. `f00dc5e` — scaled the iPhone frame to the shrunken phone (fixed px bezel/radius/island looked oversized in the 4:3 box). Added scoped overrides in the `:has(.app-slideshow)` block using `cqh` (1% of `.app-slideshow` box height ≈ phone height): `.app-slideshow-track{border:0.3cqh solid #2a2a2a;border-radius:7.5cqh;padding:1.3cqh}`, `.app-slideshow-screen{inset:1.3cqh;border-radius:6.2cqh}` (inset must equal track padding so screen sits flush in bezel), `.app-slideshow-track::after{top:2.2cqh;width:13cqh;height:4cqh}` (island; border-radius stays 999px pill). Base px rules at sections.css ~486-518 untouched (fallback). To tune: thinner bezel→lower the two `1.3cqh`; less round→lower `7.5cqh`/`6.2cqh`; smaller island→lower `13cqh`/`4cqh`. Pure CSS.

  6. `4ed922d` — (HOMEPAGE `/[locale]`, not b2c) user asked to move the hero text block (label/title/desc/buttons/stats) up a bit. `pages-extracted.css`: `.hero.hero-centered` top padding extra `clamp(6px,1.2vh,20px)` → `clamp(2px,0.5vh,8px)` (the `72px +` fixed-header buffer KEPT), and `.hero-centered-inner` top padding `clamp(14px,2.4vh,36px)` → `clamp(4px,0.8vh,12px)`. Net ≈ 20–36px higher. Mobile/short-vh overrides at ~2565/2589 unchanged. Pure CSS; if user wants it higher still, trim those two clamps further (don't touch the `72px`).

  7. `004c425` — (HOMEPAGE) widened the gap between the 3 hero stats and the dashboard illustration below: `pages-extracted.css` `.hero-dashboard-mock` top margin `clamp(4px,0.8vh,14px)` → `clamp(12px,2vh,26px)` (~+8–12px). The total stats↔mock spacing also includes `.hero-centered-inner{gap:clamp(10px,1.6vh,20px)}` — bump that too if more is wanted. Pure CSS.

  8. `75eae7c` — (b2c) moved the `{/* Smart Operations Center */}` block (`product.ops.*`, `feature-row reverse`, `smart-ops-dashboard.png`, `marginTop:40`) from the END of Section 4 (it was after the `20+ Global Languages`/#multilingual block, last before `</div></section>`) to right AFTER the `{/* APP / H5 */}` block and before `{/* Global Payment Matrix */}`. New Section-4 order: ui → brand(reverse) → APP/H5(feature-row) → Smart Ops(feature-row reverse) → Payment Matrix → 20+ Languages(#multilingual). Pure JSX reorder, no i18n/CSS change; `npx tsc --noEmit` PASSES. To undo: move the same block back to just before the Section-4 closing `</div>`.

  **NOT VISUALLY VERIFIED** (autodebug needs user to open preview + enable debug) — all changes are robust-by-construction but unconfirmed in-browser.

  **Older still-relevant flags (do NOT reapply unless asked):** homepage hero `.hero.hero-centered .hero-centered-inner` uses `url('/images/hero-grid-bg.png') cover` — a CSS dot-grid was tried then REVERTED (`6ed2f2b`). b2c `.product-hero::before` light-mode dot field IS live (`d692ec4`, gold `rgba(214,158,0,0.55)` 2.5px/24px; pale-yellow on `#F1F2F4` ≈ invisible — use darker gold ≥0.4 alpha for texture on that bg); same selector also styles the b2b hero. Homepage yellow nav bar was reverted (`bcbd0aa`); `logo-globe.png` recoverable via `git show 8fef934`. b2c `.product-hero` is `min-height:calc(100svh - 42px)` flex-column with `.hero-footer-bar{margin-top:auto}` pinning the 3 stats to the fold (b2b shares `.product-hero` — verify if touching).
