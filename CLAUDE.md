# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend: `website/frontend/` — Next.js (App Router), TypeScript, static export.
- Pages under `website/frontend/src/app/[locale]/` — locale routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Backend: Go + SQLite in `website/backend/` with encrypted `app_secrets` store (AES-256-GCM).
- i18n: `messages/en.json` + `messages/zh.json`.

## Pages

index, product, pricing, buy, buy/review, buy/success, buy/bank-transfer, about, blog, docs, contact, forum, faq, careers, partners, b2b, login, register, forgot-password, dashboard (+ payments / server / orders), admin (+ blog / products / orders / servers / settings / bank-accounts / bank-slips), invoices.

## Deployment

Production server **47.242.75.250** (staging domain `v3.gcss.hk`; final will be `gcss.hk`).
Manual deploy (GitHub Actions secrets unset): from `website/frontend/` run
`SFTP_PASSWORD='Gcss123.' npm run deploy`.
Backend: `node deploy/deploy-backend.js` cross-compiles Go + SFTP + systemd restart.

**Current workflow (overrides prior "always deploy" rule):**
- **Do NOT deploy to the production server.** Skip `npm run deploy` and `deploy-backend.js`.
- **After every code change, push to fork:** `https://github.com/baoshan23/Design-draft` on `main`.
- **Log the change** by updating the "Last session" bullet at the bottom of this file.

## Current state (as of 2026-04-24)

- **Pricing**: 5 tiers from official PDF — SaaS Hosted ($84/yr/charger), Custom Web APP ($300 setup + $120/mo), APP Enterprise ($16,900), Web APP Platform ($21,800), APP Platform ($34,200). 5 add-ons. Dedicated plan removed.
- **Deposit**: $200 (const `DepositCents` / `DEPOSIT_CENTS`). Platform plans default to deposit-ON; balance paid via bank transfer. Charges > $1,500 are forced to bank transfer server-side.
- **Payments**: Stripe, PayPal (Orders v2), Ping++ (Alipay/WeChat, USD→CNY via `PINGXX_USD_TO_CNY_RATE` secret), and bank transfer with admin-reviewed slip upload.
- **Orders**: 5-stage steppers (order + server), printable invoices at `/invoices?n=`, admin overrides at `/admin/orders`.
- **User servers**: auto-provisioned on payment. API keys stored as sha256 + last4 only; plaintext shown once on rotation.
- **Admin secrets**: encrypted store with write-only values, masked last-four display, audit trail. Managed at `/admin/settings`.
- **Auth**: rate-limited, email-change with 6-digit code, avatar/cover uploads restricted to `/uploads/*`.
- **Visual**: golden orbs R3F canvas + frosted glass layer behind all pages; editorial typography; `--primary-text: #8C5F00` for AA contrast on light mode.
- **A11y**: skip-nav, ARIA stepper, focus-visible rings, autoComplete tokens on forms, labelled admin groups.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.
- **Session log**: keep only the **latest** session entry below. When starting a new session, replace the previous bullet — do not append. This keeps CLAUDE.md lean.

## Last session

- 2026-06-04 — **section transition — removed the drop-shadow between stacked screen panels** (`b7b14f4`; user `取消每一屏切换之间的投影`). The go-electra color-block stacking transition (`SectionReveal.tsx` + `.section-panel`, added `c10a1aa`/`ed07dc7`) gave each rounded-top panel a `box-shadow: 0 -14px 38px -22px rgba(0,0,0,.22)` at its top seam = the shadow between screens. Removed that one line from `.section-panel` (polish.css ~1295); kept the rounded top corners + `margin-top:-1px` seam-kill. CSS-only. Revert: re-add the box-shadow.

- 2026-06-03 — **contact CTA banner — darker rest scrim + image fade-in on hover** (`b4e63c3`; user red-boxed the b2c contact banner + `红框的这块区域，鼠标光标移动到上面，做图片淡入的动效，静态展示时，黑色的不透明度更高一点`). Shared `.b2b-contact-cta` (b2b + b2c). `::before` scrim (sections.css ~668): rest gradient darkened `rgba(10,18,32,.82/.62/.28)` → `rgba(8,14,26,.92/.8/.55)` (higher black opacity at rest) + added `transition:opacity .6s cubic-bezier(.16,1,.3,1)`; NEW `.b2b-contact-cta:hover::before { opacity:0.5 }` so the photo "fades in" (scrim lightens) on hover. Mobile static scrim also darkened to `.82/.92`. CSS-only. Revert: rest gradient back to `.82/.62/.28`, drop the transition + `:hover::before` rule + mobile darken.

- 2026-06-03 — **home #payments "全球支付矩阵" — fixed MoMo/Toss Pay (looked off-center) + enlarged ALL fisheye tiles** (`3d55771`; user red-boxed the MoMo + Toss Pay pills `为什么总是不居中对齐` then `全部都尽可能的拉大`). The matrix is the homepage `PaymentFisheye` (`page.tsx` ~449, NOT the b2c region marquee). MoMo/Toss/Tabby/Sezzle render as inline-SVG wordmark pills (`LOGO_NODE` in `components/sections/home/paymentIcons.tsx`, priority over the PNG `LOGO_FILE` of the same name) — all others are PNG `<Image 120×48>`. Root cause of the "off-center" look: the pills used a **square** `viewBox 0 0 96 96` with `rect x4 y32 w88 h32` → pill is mathematically dead-center but only fills ~33% of tile height (floated, looked small/misaligned vs the PNGs). Fix: rewrote all 4 to a **landscape `viewBox 0 0 120 56`** (pill fills it: `rect x1 y1 w118 h54 rx12`, text true-centered, fonts bumped MoMo16→24/Toss13→19/Tabby+Sezzle14→21) so — like the PNG wordmarks — `preserveAspectRatio meet` inside the square tile renders them full-width + centered, matching neighbors. The `.payment-fisheye-inner .payment-logo-svg svg{width/height:100%!important}` rule (sections.css ~3693) forces the svg square so preserveAspectRatio does the landscape centering (comment updated). **拉大 all**: `.payment-fisheye-tile` padding `6%→2%` (logo fills more of each white card) + fisheye scale band `MIN_SCALE 0.32→0.5 / MAX_SCALE 0.92→0.98` (PaymentFisheye.tsx ~28; bigger non-focal + focal tiles; SSR pre-hydration `transform:scale(0.55)→0.7`). tsc 0. NOT visually verified (autodebug unavailable on this IP). Revert: 4 LOGO_NODE svgs back to `viewBox 0 0 96 96` + `rect x4 y32 w88 h32 rx6` + original fonts; padding→6%, scales→0.32/0.92, static→0.55.

- 2026-06-03 — **b2b — rounded the BOTTOM corners of the white #releases section (last block before the dark footer)** (user `这里的白背景，也同样做圆角处理`, red marks on the L/R corners at the #releases/footer seam — same treatment as the homepage testimonials). `#releases` (`b2b/page.tsx` ~654, `<section className="section section-alt" id="releases">`, the LAST section in `<main>`; forced opaque white by `#releases.section-alt{background:#fff}` at sections.css ~6068). The global `<footer className="footer">` (`#181818`, pages.css ~908) is a sibling of `<main>` (layout.tsx ~47-48), NOT of #releases — so used `main:has(#releases) + .footer` (scopes to the b2b page; #releases is only on b2b). New rules (sections.css ~3742, after the #testimonials block): `#releases{position:relative; z-index:1; border-bottom-left/right-radius:clamp(20px,3vw,36px)}` + `main:has(#releases) + .footer{margin-top:clamp(-36px,-3vw,-20px); border-top:none}` + `main:has(#releases) + .footer .footer-rule{display:none}`. White section sits above (z-index:1), footer pulled UP by the radius so its dark fills the carved corner notches; footer top edge is hidden behind the white so its `border-top`/`.footer-rule` pinstripe are dropped on this page to avoid a stray light line in the notches. CSS-only, tsc n/a. Revert: delete the three `#releases`/`main:has(#releases)` rules.

- 2026-06-03 — **b2c #demo (系统演示) — full-bleed grey → inset grey rounded panel** (`f551641`; user pasted the b2c demo screen + `这里的系统演示的灰色背景，也跟首页的支付流程的灰色背景一样，做圆角和宽度的调整` — "首页的支付流程" = loosely the homepage #how-it-works grey rounded panel). Replaced `.b2c-demo-section { background: var(--dark) }` (full-bleed grey) with the SAME pattern as `#how-it-works.section-alt` (pages.css 3138): `.b2c-demo-section { background:#fff; padding:clamp(40px,6vh,72px) clamp(16px,1.5vw,24px) }` (white surround + ~24px L/R gap) + `.b2c-demo-section .container { max-width:none; background:var(--dark); border-radius:clamp(20px,3vw,32px); padding:clamp(48px,7vw,88px) clamp(20px,4vw,56px) }` (grey rounded near-full-bleed panel; the 系统演示 header + demo cards sit inside it). Scoped to `.b2c-demo-section` so b2b `#demo` (`.b2b-demo-section`) is untouched. CSS-only. Revert: `.b2c-demo-section` back to just `{ background: var(--dark) }`, drop the `.container` rule.

- 2026-06-03 — **home + b2b + b2c — "色块切换" sticky stacking-panel scroll transition (go-electra style)** (user ref `https://www.go-electra.com/en/` `所有的白色块，灰色块，切换，都跟...的色块切换动画效果一样`; ref site UNREACHABLE via WebFetch [socket-closes ×3] + autodebug has NO session for this IP → NOT visually verified — confirmed effect via AskUserQuestion: user picked **层叠覆盖 / sticky stacking cover**). **First attempt was a subtle whole-section fade-up (`c10a1aa`) → user `没有改到`**; likely root cause = they view a STATIC export (a `serve -s /app -l 81` is running) and/or a stale :8000 dev bundle (the `next dev` went transiently down [curl 000] then back 200) — source edits need a rebuild + hard-refresh. Reworked into the panel-cover effect. NOTE: my 2nd-round working edits got swept into a CONCURRENT session's commit `b4e63c3` (another session commits on `main` in parallel) — content verified complete at HEAD (SectionReveal sticky logic + polish.css panel CSS + 3 page imports all present, tree clean). New client component `src/components/effects/SectionReveal.tsx` (renders null) mounted by `<ScrollResetOnLoad/>` on all 3 pages. On mount: `querySelectorAll('section')`, SKIP `.hero`/`.product-hero`/`.trusted-bar`/`.cta-banner` + any section containing `.b2c-feature-stack` (b2c #features has its OWN internal sticky deck — pinning it would nest-break sticky). Each remaining section → class `section-panel` + inline `zIndex=i+1` (kept 1..N, BELOW sub-nav z99 / header z100 so nav stays on top; footer is OUTSIDE `.page-transition` so never covered) + if computed bg is transparent, inline `backgroundColor:#fff` (white blocks must be OPAQUE to cover the grey beneath; `.section-alt` greys already opaque, untouched). **Pin only blocks that FIT a screen** (`getBoundingClientRect().height <= innerHeight*0.96`) → add `section-panel--pin` + inline `top = 72 + (.sub-nav? 42:0)`; taller blocks are NOT pinned (a pinned tall section freezes at top and HIDES its own lower content — the constraint that ruled out a literal pin-everything port). Below-fold sections also get `section-reveal` + a shared IntersectionObserver (threshold .08, rootMargin `0 0 -6% 0`) → `is-in` then unobserve (above-fold skipped via `top < innerHeight*0.85` → no flicker). CSS (polish.css ~1263): panel chrome OUTSIDE the motion guard — `.section-panel{position:relative; border-top-l/r-radius:clamp(20px,2.6vw,40px); margin-top:-1px; box-shadow:0 -14px 38px -22px rgba(0,0,0,.22)}` (rounded top + upward cover-shadow = the visible seam at every white↔grey block change); `.section-panel--pin{position:sticky}` (top inline). Reveal motion (inside `@media (prefers-reduced-motion:no-preference)`): non-pinned `.section-reveal:not(.--pin){opacity:0; transform:translateY(56px)→none .85s}`; pinned is opacity-only (translate would fight the sticky offset). tsc 0; all 3 SSR 200. **NOT visually verified** — tuning levers if off: cover-shadow `rgba(...,.22)`, radius clamp, pin fit-threshold `.96`, pin `top` offset (72 / +42 subnav). Revert: drop `<SectionReveal/>` + import from the 3 pages, delete `SectionReveal.tsx` + the `.section-panel`/`.section-reveal` CSS block in polish.css.

- 2026-06-03 — **home — rounded the BOTTOM corners of the WHITE testimonials section** (user `这里的白色背景，这里两个角做圆角`, red marks on the L/R corners at the white/grey seam; FIRST tried rounding the grey CTA's top corners → user `做反了，是白色背景做圆角，不是灰色` → flipped). The white area is the `#testimonials` section (`TestimonialsTabs.tsx` → `<section className="section" id="testimonials">`, transparent over the white body); directly below is the homepage CTA `<section className="cta-section" style={{background:'var(--dark)'}}>` (page.tsx ~461, grey #F1F2F4). FINAL rule (sections.css ~3726, right after `.cta-section`): `#testimonials{background:#fff; position:relative; z-index:1; border-bottom-left-radius:clamp(20px,3vw,36px); border-bottom-right-radius:same}` + `#testimonials + .cta-section{margin-top:clamp(-36px,-3vw,-20px)}`. The white section gets an explicit white bg + bottom radius and sits ABOVE the grey (z-index:1); the grey band is pulled UP by the matching radius (negative margin) so it slides behind the white and fills the carved corner notches → white reads as rounded-bottom with grey peeking in the corners. Adjacent-sibling `#testimonials +` scopes to homepage only. No `overflow:hidden` needed (`.cta-section::before` glow is `background:none` in light mode, polish.css ~1155). CSS-only, tsc n/a. Revert: delete both `#testimonials` rules.

- 2026-06-03 — **home — removed the divider line under the manufacturer logo bar** (`e53bd12`; user red-boxed the band between the logo marquee and #models + `这里的红线去掉`). Dropped `.trusted-bar { border-bottom: 1px solid var(--border-subtle) }` (pages.css ~2793) — that hairline was the "line" between the trusted-bar and the business-models section. CSS-only. Revert: re-add the `border-bottom`.

- 2026-06-03 — **home #how-it-works ("3 步快速上线") — white → inset grey rounded panel w/ side gaps** (`0df4863`; user paste of the homepage steps screen + `把这一屏的白色背景给成灰色然后四周做圆角，左右两边有间隙不完全靠边`). This is the homepage `#how-it-works.section-alt` (page.tsx ~141, `steps.*` i18n — NOT the b2b payflow). It was forced white by a shared `#how-it-works,#features,#models.section-alt{background:#fff}` rule (pages.css ~3130). Removed `#how-it-works` from that group; added a dedicated 2-rule block: `#how-it-works.section-alt{background:#fff; padding:clamp(40px,6vh,72px) clamp(16px,4vw,80px)}` (white surround + horizontal padding = the L/R gap so the panel isn't edge-to-edge) + `#how-it-works.section-alt .container{max-width:none; background:var(--dark)(#F1F2F4); border-radius:clamp(20px,3vw,32px); padding:clamp(48px,7vw,88px) clamp(20px,4vw,56px)}` (the grey rounded panel, overrides .container's default `padding:0 24px` + `max-width:1360`). #features/#models stay white. CSS-only, tsc n/a. **Followup (`8e17ea7`, user red-boxed a near-full-width target)**: panel was capped at the 1360 container → big white sides on wide screens. Added `max-width:none` on the container + shrank section horizontal padding `clamp(16,4vw,80)`→`clamp(16,1.5vw,24)` so the panel is near-full-bleed with only a ~24px L/R gap (content stays centered via its own max-widths). Revert: re-add `#how-it-works.section-alt,` to the white group + delete the dedicated block.

- 2026-06-03 — **b2b #features payflow — tightened vertical spacing between steps** (`bb9662c`; user `将B2B功能亮点里面的支付流程，高度展示间隙缩小一点`). Per-step row `marginTop` `i===0?8:40`→`8:16` (b2b/page.tsx ~342) + `.b2b-payflow-link` connector (sections.css ~590) compressed `height 104→72px` (v-stubs top16/52 h36→top12/36 h24; `-h` top52→36; down-arrow top86→56). Revert: marginTop 40, connector height 104, stubs top16/52 h36, h top52, down-arrow top86.

- 2026-06-03 — **home hero — moved the 3 stats down into the pre-dashboard band + enlarged** (`5052bbc`; user red-boxed the gap just above the dashboard mock + `这里的数据，移到我红框的位置，然后再放大一点`). The `.hero-stats` (9,999/1,069+/100+, `CounterAnimation`) is a sibling of `.hero-content` inside `.hero-centered-inner` (homepage `page.tsx` ~74); the dashboard is baked into the `hero-bg-8.png` bg (the `.hero-dashboard-mock` CSS is DEAD/unrendered), so stats just overlay it — safe to push down. CSS (pages-extracted.css ~2561): `.hero.hero-centered .hero-stats` margin-top `clamp(8px,1.6vh,18px)`→`clamp(24px,4vh,56px)` (down), gap `clamp(36,5vw,64)`→`clamp(44,6vw,80)`; `.hero-stat-value` `clamp(1.5rem,2.4vw,2.125rem)`→`clamp(2.1rem,3.6vw,3rem)`; new `.hero-stat-label{font-size:.95rem}` (was .85). CSS-only. **NOT visually verified** — if pushed too far it could touch the bg dashboard; nudge the margin clamp. Revert: margin→`clamp(8,1.6vh,18)`, value→`clamp(1.5,2.4vw,2.125)`, gap→`clamp(36,5vw,64)`, drop the label rule.

- 2026-06-03 — **b2b #aftersales + b2c #support — icons → two-tone gold/black line icons, dropped the yellow tile** (`cdc3f8e`; user red-boxed the b2c 完善售后 4 icon tiles + `把这里的图标改成黄黑双色的线性图标`). Same "黄黑双色线性图标" pattern as mode/addon icons: removed the solid yellow `.card-icon` tile (→ `background:transparent`) and made the 4 after-sales/support icons two-tone — black base strokes + a gold `.ic-accent` element each: video=play triangle, docs=added `M9 7h7M9 11h6` text lines, forum=added `M8 9h8M8 12h4` chat lines, headset=ear-cups path. Added `className="ic-accent"` in BOTH b2b `supportCards` (item1-4) AND b2c `supportCards` (s1-4) for consistency (they share the `.b2b-aftersales-grid` CSS). CSS (pages.css ~11249): `.b2b-aftersales-grid .card-icon` (+`#aftersales`/`#support` variants) bg `var(--primary)`→`transparent`; kept base `svg{stroke:#1a1a1a}`; NEW id-qualified `…svg .ic-accent{stroke:var(--primary)}` (id-qualified so it outranks the id-qualified black base rule — a class-only accent would lose to `#support …svg`). tsc 0, SSR b2c shows 4 `.ic-accent`. Revert: `.card-icon` bg back to `var(--primary)`, drop the `.ic-accent` CSS rule + the `className="ic-accent"` from both pages' supportCards (and the added doc/chat accent line elements).

- 2026-06-03 — **b2b #features payflow — tightened vertical spacing between steps** (`bb9662c`; user `将B2B功能亮点里面的支付流程，高度展示间隙缩小一点`). Two levers: (1) per-step row `marginTop` `i===0?8:40` → `8:16` (b2b/page.tsx ~342). (2) `.b2b-payflow-link` dashed connector (sections.css ~590) compressed `height 104→72px`; stubs `-v1`/`-v2` `top16/52 height36 → top12/36 height24`; `-h` bar `top52→36`; `-arrow--down` `top86→56` (`--up` stays top2). 25%/75% x-anchors + mirror overrides + ≤900 hide all unchanged. tsc 0. Revert: marginTop back to 40; connector height 104, v-stubs top16/52 h36, h top52, down-arrow top86.

- 2026-06-03 — **b2c #cta "准备部署？" yellow banner — near-full-width rounded band (full-bleed then pulled back per followup)** (`06c45ed` → `da2ae80`; user first `这里的黄色背景横向拉满` (full-bleed edge-to-edge), then `四周做圆角然后左右留一点间隙，不完全靠边拉满`). The banner is `.cta-banner-inner` (b2c/page.tsx ~597, yellow gradient card w/ dual-phone mockup + h2/desc + 支付$200/联系销售). FINAL state (sections.css ~3771): breaks out of `.container` but inset by a 24px gap each side — `width: calc(100vw - 48px); left:50%; margin-left: calc(-50vw + 24px)` (it's `position:relative`); `border-radius:28px` (rounded all 4 sides); `box-shadow:0 30px 60px -28px rgba(242,163,0,.5)`; `padding: 52px max(40px, calc((100vw - 1392px)/2 - 24px))` so content aligns to the container content edges on wide screens (1392 = --max-width 1440 − 2×24px gutter) and gets ≥40px inner padding on narrow. Works because body `overflow-x:clip`. Mobile (≤860) keeps its own `padding:0 28px 40px`. tsc n/a (CSS). Revert to plain contained card: drop the width/left/margin-left lines + restore `padding:52px 60px` (keep border-radius/box-shadow).

- 2026-06-03 — **b2b #features payflow — replaced step 3 illustration** (`07cd1c7`; user pasted a "Profit / Balance" GCSS-Charge dashboard illustration (1000×1000 RGBA) + `这个插图重新替换一下`). Confident match to step 3 (`i===2` in the `.b2b-payflow` map, `b2b/page.tsx` ~367) whose copy is `payment3` = "系统会自动计算你的盈利和商户的余额 / System auto-calculates your earnings and the merchant's balance" — the image literally shows **Profit** + **Balance**. Saved optimized → `public/images/b2b-payflow-step3-v2.png` (84KB) and bumped the `<Image src>` `b2b-payflow-step3.png`→`-v2.png` (width/height 1000, objectFit contain unchanged). Old `b2b-payflow-step3.png` left in place (unused, harmless). tsc 0. Pushed only this change; pre-existing uncommitted b2c contact-CTA edits (en/zh.json + b2c/page.tsx) left untouched in the working tree. Revert: src back to `b2b-payflow-step3.png`.

- 2026-06-03 — **nav: restored GCSS logo icon to original size** (user red-boxed nav + `顶部导航栏logo恢复原来的大小`). `.logo img` `76→52px` desktop (`components.css` 229) + `56→40px` mobile (`pages.css` ~4821 `@media`) — undoes the two prior enlargement passes (`5be603c` 52→64, `cc1d9e2` 64→76). Back to the original 52/40. CSS-only. Revert: 76/56.

- 2026-06-03 — **b2c #features stacking deck — removed card shadow AND border** (`693e558` + followup; user red-boxed the sticky stacking-deck cards: `这里的卡片投影去掉` then `卡片边线也去掉`). `.b2c-stack-card` (sections.css ~2809-2815): `box-shadow: 0 -10px 28px rgba(0,0,0,.06), 0 18px 50px rgba(0,0,0,.08)` → `none`, then `border: 1px solid rgba(0,0,0,.09)` → `none`. Cards are now white-on-white (the `#features` section is forced white, pages.css 3131-3133) with NO border/shadow — the only remaining separation is the per-card `top` peek offsets (+24/44/64/84px). The deck reads essentially flat now (intended per user). CSS-only. Revert: restore the two-part box-shadow + the 1px border.

- 2026-06-03 — **b2b — added closing contact CTA banner (Arcadia-style)** (`4d59685`; user: `帮我在这里增加一个插图`[red-boxed the #demo area] `可参考`[EV-city + AI-brain illustration] `然后配上文案，大标题"开始联系我们吧"` + 2 followups: subtitle `立即联系我们的团队，了解 GCSS 如何满足您的充电运营需求。` + `增加黄色按钮，"立即联系"`). New `#contact-cta` `<section className="section">` — FIRST appended at the END (after #releases), then MOVED to sit right BEFORE the #demo section (after #aftersales) because the user RE-SENT the same request still red-boxing the demo area (clear signal: wants it there, not at page bottom). Markup: `.b2b-contact-cta` (style `backgroundImage:url(/images/b2b-contact-cta.jpg)`) > `.b2b-contact-cta-inner` (eyebrow `.section-label` + h2 + p + `<Link href="/contact" className="btn btn-primary btn-lg">`). Saved the user's 1408×768 EV/AI illustration → `public/images/b2b-contact-cta.jpg` (Pillow q86, 322KB). New i18n `b2b.contactCta.{label,title,desc,cta}` (en+zh): zh 联系我们/开始联系我们吧/立即联系我们的团队，了解 GCSS 如何满足您的充电运营需求。/立即联系; en Contact Us/Let's get in touch/.../Contact Now. CSS (sections.css after `.b2b-payflow-link` ≤900 block): `.b2b-contact-cta` 28px-radius cover-bg banner + `::before` left-dark `linear-gradient(90deg, rgba(10,18,32,.82)→.28)` scrim (z-index:-1, isolate); `.b2b-contact-cta-inner` max-width 560 with white eyebrow pill (`rgba(255,255,255,.14)`), white h2 `clamp(2,4.4vw,3.2rem)/600`, `rgba(255,255,255,.82)` desc; ≤600 radius 20 + vertical scrim. tsc 0, SSR renders title/subtitle/button. **Followup (`76fb2bc`)**: (a) user gave exact subtitle → `b2b.contactCta.desc` updated to `立即联系我们的团队，了解GCSS如何支持您的充电需求。` (en: `...how GCSS supports your charging needs.`). Button spec `黄色底黑色字` already satisfied by `.btn-primary` (`background:var(--primary)` + `color:var(--black)`). (b) user `增加在B2C页面的"售后"一屏里面` → added the SAME `.b2b-contact-cta` banner at the END of the b2c `#support` (售后) section (b2c/page.tsx ~478, before #demo), REUSING the global `b2b.contactCta.*` keys + shared CSS + same jpg (inline `marginTop:64`). Revert: drop the `#contact-cta` section + `.b2b-contact-cta*` CSS + the 4 i18n keys + the jpg + the b2c banner block. **Followup (`36f0cf0`, `眉标去掉`)**: removed the `<span className="section-label">` eyebrow from the CTA banner on BOTH pages (just title+desc+button now); `contactCta.label` key now unused/harmless.

- 2026-06-03 — **nav: enlarged the GCSS logo icon** (`5be603c`; user red-boxed the b2c #payments nav + `logo图标放大一点`). `.logo img` `52→64px` desktop (`components.css` 228) + `40→48px` mobile (`pages.css` ~4820 `@media`). CSS-only. Revert: back to 52px / 40px.

- 2026-06-03 — **b2c #features — 4 feature rows → sticky stacking deck (layer-on-scroll)** (user: `把B2C里面的功能亮点里的"精美 UI 设计…""您的品牌…""APP / H5…""智能运营中心",做成滚动到顶部就一层一层叠加的交互方式` + a stacked-cards ref PNG). The 4 feature blocks (UI / Brand / App / SmartOps) in `b2c/page.tsx` were each wrapped in their own `<ScrollAnimation><div style={{marginTop}}>`. Replaced all 4 wrappers with ONE `<div className="b2c-feature-stack">` holding 4 sibling `<div className="b2c-stack-card">` (each still wraps the original `.feature-row` content unchanged — image+text, same 14/16-space indent so inner JSX untouched). CSS (`sections.css` ~2796, after `.b2c-feat-shift`): `.b2c-stack-card { position:sticky; top:calc(var(--header-height,72px)+24px); background:#fff; border:1px solid var(--border-subtle); border-radius:var(--radius-xl); box-shadow:0 12px 44px rgba(0,0,0,.07); padding:36px 48px; margin-bottom:32px }` + incremental `top` per `:nth-child(2/3/4)` (+44/+64/+84px) so each prior card's top edge peeks (the deck look); `.b2c-stack-card .feature-row{padding:0}`; 4th card `margin-bottom:0`. **Why sticky works**: body uses `overflow-x:clip` (NOT hidden) + `.page-transition` wrapper is opacity-only (no transform) — both documented sticky prereqs. Removed the `<ScrollAnimation>` fade wrappers from these 4 (their `animate-on-scroll` transform would create a containing block breaking sticky). ≤900px: `position:static` (cards taller than viewport → plain stack), padding 16/20px. tsc 0; SSR `/zh/b2c` renders 4 `.b2c-stack-card`. The 6-feature lined grid above + Global Payment Matrix below kept their ScrollAnimation. **NOT visually verified** — per-card top offsets/peek may want tuning. Revert: restore the 4 `<ScrollAnimation><div style={{marginTop:80/40/40/40}}>` wrappers around each `.feature-row` + drop `.b2c-feature-stack`/`.b2c-stack-card` CSS. **Followup (user re-sent the same request → effect too subtle)**: b2c `#features.section-alt` is forced WHITE (`pages.css` 3131-3133) so white cards on white read flat. Strengthened the card edge: border `var(--border-subtle)`→`rgba(0,0,0,.09)` + switched box-shadow to a TOP-cast shadow `0 -10px 28px rgba(0,0,0,.06), 0 18px 50px rgba(0,0,0,.08)` — in a top-pinned stack each incoming card sits OVER the previous, so the separating edge is at the top; the upward shadow makes the peeking layer behind read clearly. Autodebug unavailable on this IP so still NOT visually verified.

- 2026-06-03 — **b2b #features payflow — dashed connector w/ arrowheads between step 1 & 2** (`1b5ca60`; user red-boxed a path on the rendered payflow + a Z-elbow dotted-connector ref + `帮我按红色线增加一条黑色虚线带箭头，参考这个`). Inserted `.b2b-payflow-link` in the payflow map AFTER the step-1 row (`i === 0`, b2b/page.tsx ~370): up-arrow ↑ (SVG chevron) at left-col center (25%) → vertical stub → horizontal dashed bar (25%→75%) → vertical stub → down-arrow ↓ at right-col center (75%). Links step 1's LEFT illustration to step 2's RIGHT illustration (step2 is the `reverse` row). CSS (sections.css ~587): black `2px dashed #1a1a1a` borders on 3 absolutely-positioned `span`s (`-v1` left top-half, `-h` horizontal, `-v2` right bottom-half) inside a 104px-tall relative box; 2 `.b2b-payflow-link-arrow` SVG chevrons `translateX(-50%)` centered on the 25%/75% verticals. 25%/75% ≈ the feature-row 1fr/1fr column centers (gap 60px makes it ~±15px off, acceptable). Hidden ≤900px (rows stack to 1-col → horizontal connector meaningless). tsc 0, SSR renders. **Positions eyeballed, NOT visually verified** — may need nudging (the 25/75% x-anchors, the 104px height, the 16/52/86px y-stops). Revert: drop the `{i === 0 && <div className="b2b-payflow-link">...}` block + the `.b2b-payflow-link*` CSS. **Followup (`这里的红色线位置也增加一条`, `aaae271`)**: added a SECOND connector between step 2 & step 3 — generalized the JSX guard `i === 0` → `i < 2` and added a `b2b-payflow-link--mirror` class on `i === 1`. New CSS overrides flip the mirror's verticals/arrows: up-arrow + `-v1` at 75% (under step 2's RIGHT illustration), down-arrow + `-v2` at 25% (over step 3's LEFT empty placeholder); `-h` horizontal bar unchanged. Same hidden ≤900px.

- 2026-06-03 — **b2b #features cells — removed card hover lift, only icon reacts** (`8d0d7a7`; user red-boxed the EN Key Features 3 cells + `这里卡片无交互动效，只有图标有`). Added scoped `.b2b-hiw .b2c-feature-cell:hover { transform: none }` (sections.css ~416) to kill the `-4px` lift on the b2b feature cards; the icon gold-flip (`.b2c-feature-cell:hover .feature-card-icon`) stays. Scoped to `.b2b-hiw` (the b2b #features wrapper) so the SHARED `.b2c-feature-cell` keeps its lift on the b2c page (user tuned that earlier). Note both pages use `id="features"` so couldn't scope by id. tsc n/a (CSS). Revert: drop the `.b2b-hiw .b2c-feature-cell:hover` rule.

- 2026-06-03 — **b2b #revenue — re-added 营收 eyebrow; yellow bg + white icon-hover tried then REVERTED to grey/gold** (`977a849` → `e9b7bf7`; user: `将"营收"的眉标加回去...背景颜色改成黄色...图标交互的时候，黄色改成白色`, then `"营收"一屏恢复灰色背景和图标样式`). KEPT: `<span className="section-label">{t('b2b.nav.revenue')}</span>` (= 营收/Revenue) ABOVE the `<h2>` in `.b2b-revenue-intro` (b2b/page.tsx ~285) — shows the default gold pill on grey. REVERTED (user changed mind): `#revenue.section.b2b-revenue-section` bg back to `#F1F2F4` grey (was tried as `var(--primary)` yellow); dropped the `.b2b-revenue-intro .section-label` white-pill override; `.b2b-revenue-item:hover .b2b-revenue-item-arrow` fill back to `var(--primary)` gold (was tried as `#fff` white). So #revenue is back to grey bg + gold hover, just with the eyebrow now present. (Note: an intermediate ask `将黄色背景宽度缩小到1360，做四周做圆角` — contain the yellow panel — was interrupted/superseded by the full grey revert, NOT implemented.) tsc n/a (CSS). Revert the eyebrow too: drop the span.

- 2026-06-03 — **b2b #features payflow — inserted step 2 illustration** (`d2cde3a`; user pasted a standalone "Where did the money go? → Bank / Merchants" money-flow diagram (1000×1000 RGBA) + a screenshot red-boxing the empty step-2 placeholder on `/zh/b2b#features`). Step 2 is `i===1` in the `.b2b-payflow` map (`b2b/page.tsx` ~344, the `reverse` row text-left/image-right). Saved the asset to `public/images/b2b-payflow-step2.png` and added an `{i === 1 && <Image src="/images/b2b-payflow-step2.png" width=1000 height=1000 objectFit:contain>}` block right after the existing step-1 `{i === 0 && ...}` block. Step 3 (i===2) still renders an empty placeholder. tsc 0. Revert: drop the `i === 1` Image block + delete the PNG.

- 2026-06-03 — **b2b #features payflow — replaced step 1 illustration** (`24e9d8e`; user pasted a clean standalone orbital "Glogbal Charge / Credit Card" diagram (1000×1000 RGBA) + a screenshot red-boxing the current step-1 illustration on `/zh/b2b#features`). The step-1 image is rendered only for `i===0` in the `.b2b-payflow` map (`b2b/page.tsx` ~344): `<Image src="/images/b2b-payflow-step1.png" objectFit:contain>` inside `.feature-image-placeholder.b2b-payflow-img`. Overwrote `public/images/b2b-payflow-step1.png` with the new asset (`cp` from /tmp) and bumped the `<Image>` `height` `751→1000` to match the new square aspect (width stays 1000). Steps 2/3 still render empty placeholders. tsc 0. Revert: `git show 24e9d8e^:website/frontend/public/images/b2b-payflow-step1.png` for the old PNG + height back to 751.

- 2026-06-03 — **b2b #features payment-flow 3 steps → B2C feature-row layout (image left, text right)** (`031934c`; user pasted b2b #features dark bar + b2c feature-row ref + `将这里的三个步骤，改成B2C这种的，图在左，文案在右的布局` + `容器空出来，我将发图给你插入`). Replaced the dark `.b2b-hiw-bar` (kicker 支付流程 + 3 inline steps from `b2b.advantages.payment1/2/3`) with a `.b2b-payflow` block: a centered `.section-header` sub-label (paymentTitle 支付流程) + 3 `.feature-row`s (ALL image-left/text-right per explicit request — NOT alternating). Each row = an **EMPTY** `.feature-image-placeholder.b2b-payflow-img` (user inserts artwork later) + `.feature-text` (`.b2b-payflow-step` eyebrow = `b2b.features.step` + N = 步骤 1/2/3 + the step sentence as h3). New CSS (`sections.css` ~527): `.b2b-payflow{margin-top:64px}`, `.b2b-payflow-img` light placeholder bg `linear-gradient(180deg,#F1F2F4,#fff)` + border (overrides default dark-navy `.feature-image-placeholder` so empty boxes look clean), `.b2b-payflow-step` gold uppercase eyebrow, `.b2b-payflow .feature-text h3` `clamp(1.5,2.4vw,2rem)/500`. Old `.b2b-hiw-bar*` CSS now DEAD. tsc 0, SSR shows 3 rows. **Followup (`步骤2，文案在左，图在右`, `2a08f27`)**: step 2 row (i===1) gets the `reverse` class → text-left/image-right; steps 1 & 3 stay image-left/text-right. **NEXT: user will paste 3 images** → insert `<Image>` into each empty `.b2b-payflow-img` in DOM order (step 1/2/3). Revert: restore the `.b2b-hiw-bar` block from `git show 031934c^`; drop the `reverse` from the i===1 row.

- 2026-06-03 — **b2c hero: removed the footer stats bar; b2b/b2c hero heights consistent** (`c40ef2a`; user red-boxed the b2c 覆盖国家/全球运营商/部署服务器 stats bar + `将这里的内容去掉，同时调整B2B,B2C两页首屏的高度，保持一致`). Removed the b2c `.hero-footer-bar` block (3 `.hero-footer-stat` + `CounterAnimation`s) from `b2c/page.tsx` (~125-152) + the now-unused `CounterAnimation` import. Both heroes share the base `.product-hero { min-height: calc(66svh - 42px) }` (pages-extracted.css 27-34; no per-page min-height override), so dropping the footer puts b2c at the same shared floor as b2b. `.hero-footer-bar` CSS now dead. tsc 0, SSR 200. Offered: can shrink BOTH heroes' min-height together (the 66svh was tuned to pin the footer to the fold; without it b2c may have empty space below). Revert: restore the footer block + import from `git show c40ef2a^`.

- 2026-06-03 — **b2b #features 功能亮点 → B2C lined-grid style** (`8c35aa9`; user paste of b2b #features + `将B2B的功能亮点，按B2C的风格布局排版`). Replaced the 3-card `.grid.grid-3.b2b-features-grid` (big `01/02/03` `.b2b-feature-num` + h3 + p) with the B2C `.b2c-feature-lined` > `.b2c-feature-row` > `.b2c-feature-cell` layout (icon tile LEFT + h3 + p RIGHT; tile white+border → flips gold on hover). New `featIcons` array (3 inline line SVGs: monitor / server-stack / code-brackets) in b2b/page.tsx, indexed by feat order. Dark payment-flow `.b2b-hiw-bar` KEPT below. Extended the b2c icon stroke override (`sections.css` ~475) to add `polyline, polygon` so feat3's polyline icon also renders black. Old `.b2b-features-grid`/`.b2b-feature-num` CSS now DEAD/unused. tsc 0, SSR shows `.b2c-feature-cell` ×3 / no `.b2b-feature-num`. Revert: restore the `.grid.grid-3` card markup from `git show 8c35aa9^` + drop `featIcons` + the polyline/polygon stroke selectors.

- 2026-06-03 — **b2c #support bento → clean 4-card grid (matches b2b #aftersales) + yellow/black icons** (`35d328c`; user paste of b2c #support bento + `将B2C的售后，样式改成与B2B的一样，然后图标都改成黄底黑色线性图标`). NOTE: this reverses the earlier "keep the support bento" decision — user now wants it converted. Replaced the `.support-bento` (tall/wide/small/accent) with `.grid.grid-4.b2b-aftersales-grid` of `.card` (icon+h3+p), new `supportCards` array in b2c page (keys s1-s4 + the same 4 line icons as b2b). Old `.support-bento*` markup/CSS + `product.support.bento.*` i18n now DEAD/unused (s1-s4 title/desc still used). Icon tiles: added `.b2b-aftersales-grid .card-icon { background:var(--primary); color:#1a1a1a }` (pages.css ~11227) → **solid yellow bg + black line icon**, shared by BOTH b2b #aftersales and b2c #support (icons use `stroke=currentColor`). tsc 0, SSR 200. Revert: restore the bento markup from `git show 35d328c^` + drop the `.card-icon` yellow rule.

- 2026-06-03 — **FIX: B2B/B2C sticky sub-nav broke (regression from the page-transition)** (`bfd5410`; user `把B2B,B2C页面的二级导航，滑动时，固定在顶部不动，调回出来`). The `.sub-nav`/`.product-sub-nav` is `position:fixed; top:var(--header-height)` (sections.css 4592 / pages-extracted.css 4) — was correct. But the motion-suite `template.tsx` wraps each page in `.page-transition`, whose `pageFadeIn` animation held a `transform: translateY(0)` (via `animation-fill-mode: both`). **A transform on an ancestor establishes a containing block for `position:fixed` descendants** → the in-page sub-nav anchored to `.page-transition` and scrolled away (the header is fine — it lives in layout.tsx, OUTSIDE the wrapper). Fix: made `pageFadeIn` **opacity-only** (dropped the translateY; opacity does NOT form a containing block). Page-fade kept, sub-nav fixed again. Lesson: never put `transform` on a wrapper that contains `position:fixed` UI. Revert: n/a (keep). 

- 2026-06-03 — **b2b #revenue — card grid → Arcadia intro-left / item-list-right, full-bleed grey** (`20e6812`; user pasted current + Arcadia ref, `标题副标题在左边，右边列表的形式，原来底部灰色背景，直接铺满整一屏`). Replaced the 2×2 `.card` grid (in `.b2b-revenue-panel` grey rounded panel) with a 2-col `.b2b-revenue-split` (`1fr 1.15fr`): LEFT `.b2b-revenue-intro` = base h2 title (NOT `.section-header`, so no nowrap → wraps in the narrow col) + muted subtitle (dropped the eyebrow pill per ref); RIGHT `.b2b-revenue-list` = 4 `.b2b-revenue-item` rows (title h3 1.4rem/600 + desc + a 46px circular `.b2b-revenue-item-arrow` that fills `--primary` on row hover), divided by `border-bottom rgba(0,0,0,.1)`. BG: removed `#revenue.section` from the white-override list (`sections.css` ~5797) + new `#revenue.section.b2b-revenue-section{background:#F1F2F4}` full-bleed (was a contained `rgba(242,242,242,.5)` 28px panel). `revenueCards` still mapped but `.icon` now unused (kept, harmless). Responsive ≤900 stacks to 1-col. tsc 0, SSR 200. Revert: restore the `.b2b-revenue-panel`+grid markup & CSS + re-add `#revenue.section,` to the white list (`git show 20e6812^`).

- 2026-06-03 — **Arcadia motion/interaction suite** (`3e990bb`; user `网站刷新的交互方式，学习一下Arcadia` → AskUserQuestion: chose ALL of load-entrance + scroll-reveal-refine + micro-interactions + page-transition). New `src/app/[locale]/template.tsx` (server component) wraps each route in `.page-transition`; appended an "Arcadia motion layer" to `polish.css` (after the orbs reduced-motion block, ~line 1207): (1) **page transition** `.page-transition{animation:pageFadeIn .45s ...both}` — template re-mounts on nav so it re-fires (fade+glide-up between pages); (2) **hero entrance** staggered `heroEntrance` on `.hero .hero-badge/.hero-title/.hero-desc/.hero-buttons/.hero-footer-bar/.hero-video-wrap/.hero-media-bare` (delays .05-.42s; base opacity stays 1 as no-anim fallback); (3) **scroll-reveal stagger** `.animate-on-scroll .grid > .card/.glass-card/.model-card` fade-up with nth-child delays (.05-.40s) so grids fan in card-by-card; (4) **image hover** slow `scale(1.045)` zoom-within-frame on `.feature-row:hover .feature-image-placeholder img` + `.card-with-img:hover .card-image`. ALL under `@media (prefers-reduced-motion: no-preference)` (page-transition keyframe itself is fine reduced — base.css zeroes anim duration). NOTE: someone formalized a canonical type scale in `base.css` h1-h6 (h1/h2 weight 500, h3/h4/h5 600, p 400) + the `.grid` gap 28 / `.section` 120 — intentional, kept. tsc 0; all real routes (zh/b2b/b2c/pricing/home) SSR 200 (`/zh/product` 404 is pre-existing — no such route, `product.*` is just the b2c i18n namespace); `.page-transition` renders. **NOT visually verified** (motion — user feels it live). **DEFERRED: number count-up** (the "数字" micro-interaction) — needs a small `'use client'` CountUp component per stat; offered. Revert: delete `template.tsx` + the "Arcadia motion layer" block in polish.css. **Followup (`数字滚动计数加上，套在首页那几个`)**: the homepage's only stats (3 hero: 9999/1069+/100+ at `page.tsx` 76/83/90) ALREADY used `<CounterAnimation>` — but it was BROKEN: rendered an empty `<span>` (0×0) so the `threshold:0.1` IntersectionObserver could fail to fire → count-up never ran (blank/static). Fixed `899705b` (`CounterAnimation.tsx` rewrite): SSR renders the final number (sizes the span + SEO), effect resets to `0` then counts up, `threshold:0` + a 1.2s `setTimeout` fallback so it ALWAYS runs, reduced-motion shows final. Also added `.hero-stats` to the hero-entrance stagger (polish.css). SSR now shows `9,999/1,069+/100+` (was empty). Revert: restore the empty-span component from `git show 899705b^`.

- 2026-06-03 — **Push Arcadia approach to b2c** (user: `把同样这套Arcadia结构思路推到b2c`, then clarified: `售后服务那里先保留，支付方式保留跑马灯但是卡片样式可以更改，其他你帮我看着调整`). Decisions: **#support bento = KEEP** (heavily tuned), **#gallery Gallery3D + feature-rows = KEEP** (signature / already Arcadia 2-col splits), **#pay = keep marquee, restyle cards only**. Done (`546e8b7`): (1) `.b2c-pay-card` (`sections.css` ~2647) transparent/no-shadow → solid white `var(--dark-card)` + `var(--shadow-sm)` (border+radius from `.card`); `:hover` → `0 8px 20px rgba(0,0,0,.08)`; `.b2c-pay-card .tag` loud yellow border+text → neutral chip (`#F4F5F7` bg + `rgba(0,0,0,.06)` border + `--text-secondary`), dark-theme variant neutralized too. (2) `.b2c-feature-head h2` (502) `2.9rem/700` → standard `clamp(2.25rem,4.8vw,3.4rem)/500` for title consistency. Rest of b2c already inherits the global Arcadia system (cards/buttons/shadows/section-intros/whitespace/reveal/light titles). tsc n/a (CSS only), SSR 200. Revert: pay-card back to transparent/no-shadow + yellow tags; feature-head h2 back to 2.9rem/700. **Followup (`你帮我规范一下b2c整个页面`)**: audited b2c — already mostly consistent post-global-passes; the real outliers were card radii (the air-pass bumped `.card`→`--radius-xl` 24px but these stayed `--radius-lg` 20px) + one title weight. Fixed (`b36fa84`): `.b2c-feature-cell` (395) + `.feature-image-placeholder` (2688) radius `--radius-lg`→`--radius-xl`; `.b2c-feature-cell h3` (419) weight `600→700` (matches all other card titles). Left intact: `.feature-image-placeholder` dark-navy bg (`#1a1a2e→#0f3460`, covered by images/AppSlideshow backdrop — risky to change blind), `.b2c-feature-cell:hover` no-shadow (prior explicit user choice `交互时卡片无投影`), support-bento solid-gold icon (kept bento). Revert: those radii back to `--radius-lg`, h3 weight back to 600.

- 2026-06-02 — **B2B page Arcadia redesign (ref: arcadia.com/use-cases/ev-charging) — IN PROGRESS, hybrid approach** (user: `参考...这一页...帮我调整B2B一整页的设计，包括交互动效`). Arcadia ev-charging blueprint (via WebFetch, text-only — can't see it visually): full-bleed hero → 3-col icon-feature grid → linked card tiles → 2-col customer-story split → centered CTA band → 3-card resources → footer; heavy whitespace, muted clean cards, single CTAs, smooth scroll-reveal + hover. **Constraint**: user NOT on Desktop app → no autodebug preview → doing it section-by-section, user screenshots each to calibrate. Via AskUserQuestion user chose **混合 (hybrid)**: KEEP the #architecture hub diagram (Arcadia polish only), SIMPLIFY the rest (#aftersales/#revenue/#modes/#features) into clean Arcadia grids/2-col splits. **Round 1 (`31d471c`)**: smoother unified scroll-reveal — `.animate-on-scroll` now eases in as a whole block (opacity) + signature `cubic-bezier(0.16,1,0.3,1)` on heading/label/lead glide (`sections.css` ~4692). Site-wide. **Round 2 (`08e74c6`)**: #aftersales orbital diagram → clean `.grid.grid-4.b2b-aftersales-grid` of `.card`s (inline line icon in `.card-icon` + h3 + p), `b2b/page.tsx` `supportCards` now `{key,icon}` (was `{key,pos}`), section class `section b2b-support`→`section`. Old `.b2b-support*` orbital CSS (`pages.css` ~11210) + orbit-diagram.png/card-badge.png now DEAD/unused (left harmless). New scoped CSS `.b2b-aftersales-grid .card h3{1.2rem}/p{.95rem}` at pages.css ~11206. tsc 0, SSR 200. **Round 3 (`3fb880e`) #revenue**: RevenueAccordion → static `.grid.grid-2.b2b-revenue-grid` of `.card`s (icon+title+desc) inside the kept grey `.b2b-revenue-panel`; dropped the `RevenueAccordion` import (component + `.revenue-acc*` CSS now dead). **Round 4 (`f866a3c`) #modes**: `.mode-bento-card` (full-card bg artwork) → `.grid.grid-2.b2b-modes-grid` of `.card`s (gold line icon via reused `modeIcons` + title+desc); bento CSS + `mode-N.png` artwork dead. **Round 5 (`db01e00`) #features**: step cards (top illustration + half cards) → `.grid.grid-3.b2b-features-grid` of `.card`s (big `01/02/03` `.b2b-feature-num` + title+desc), KEPT the dark `.b2b-hiw-bar` payment-flow bar; step1 illustration + `.b2b-hiw-card/.b2b-hiw-visual` CSS dead. Shared card-text CSS at `pages.css` ~11206 (`.b2b-aftersales-grid/.b2b-revenue-grid/.b2b-modes-grid/.b2b-features-grid .card h3{1.2rem}/p{.95rem}`). All 4 use one `<ScrollAnimation>` around the grid. tsc 0, SSR 200 each. **Dropped (per user's 简化 choice)**: orbital diagram, revenue accordion interaction, modes bg artwork, features step-1 illustration — all dead CSS/assets left harmless, revertable per-commit. **Round 6 (`ab8f009`, FINAL) #architecture**: user confirmed `方向OK` → hub diagram KEPT (already clean: white cards, 24px radius, soft grey shadow, stacked-deck); added only a gentle hover lift + smooth transition to `.b2b-hub-card` (`pages.css` 1501) so it matches the new grids' interaction. **B2B Arcadia hybrid redesign DONE** (rounds 1-6: reveal + aftersales/revenue/modes/features→clean grids + architecture kept/polished). Revert any round: `git show <hash>^` for that section's prior JSX.

- 2026-06-02 — **Arcadia "air & softness" pass — global whitespace + card radius** (`base.css` `.section`/`.section-lg`/`.grid`, `sections.css` `.card`/`.glass-card` radius ~2398). User: `继续按照Arcadia的网站改动吧`. Chose the safe system-level Arcadia move (whitespace is THE Arcadia signature; no blind per-page JSX). `.section` padding `100→120px`, `.section-lg` `120→144px` (mobile ≤480 compression untouched), `.grid` gap `24→28px`, `.card`/`.glass-card` radius `var(--radius-lg)`(20px)→`var(--radius-xl)`(24px). Braces balanced; `tsc` n/a. Committed `c1e21b0`. **NOT visually verified** (autodebug needs user preview). NOTE: `components.css` was externally edited this session (logo `font-size:1.3→1.5rem` + `.logo img{44px}` + gap 8→10; `.btn-primary` gold-glow shadow re-added at line 33/38 — but that's **inert**, polish.css `.btn-primary` neutral shadow still wins the cascade). Revert: `.section`→100px, `.section-lg`→120px, `.grid` gap→24px, card radius→`--radius-lg`. **Followup (`继续`)**: Arcadia-substantial buttons — `components.css` `.btn` padding `10/22→12/26px`, radius `var(--radius)`(10px)→`12px`, font-size `0.85→0.9rem` (`2d77bf0`). Affects all `.btn` site-wide. Revert: back to 10/22px / var(--radius) / 0.85rem. **Followup (`继续`→user not on Desktop app so NO autodebug preview; proceeding screenshot-only)**: applied the deferred LEFT-align — `.section-header` `text-align:center→left`, `margin:0 auto 72px→0 0 72px` (`6c598e1`). One reversible rule, awaiting user screenshots to judge balance (some centered-content sections may look off → can revert globally or re-center individual sections via per-section overrides). Also created `.claude/launch.json` (port 8000, cwd website/frontend) for the Desktop preview, but user isn't on Desktop so it's unused. Revert: `.section-header` back to `text-align:center; margin:0 auto 72px`. **→ REVERTED** (`6c25d83`, user: `整体回退居中吧`) — section headers back to centered. Left-align verdict: rejected. Don't re-apply unless asked. **Followup (`这里的标题字号也要跟其他的一样`, homepage MobileShowcase title "原生 iOS 与安卓 App")**: `.mobile-showcase-info h2` was still the OLD `36px/800/-0.02em` (didn't track the section-intro pass) → bumped to match `.section-header h2`: `clamp(2.25rem,4.8vw,3.4rem)/700/-0.035em/line-height1.08` (kept `text-align:left` — it's the 2-col showcase, below the homepage first screen so allowed). `4f8763d`. Sibling `.mobile-showcase-text h2` (5139, `clamp(1.75rem,3vw,2.4rem)`) is a separate smaller variant — left as-is. Revert: back to `36px/800/-0.02em`. **Followup (`主标题的不要加粗了，参考一下Arcadia`)**: Arcadia uses light/airy large display headlines → `.section-header h2` + `.mobile-showcase-info h2` `font-weight:700→500` (`ae99d28`). Only section titles (h2); hero h1s NOT touched (offered). If 500 reads too thin for CJK, lever is 600; if still too bold, 400. Revert: weight back to 700. **Followup (`把首页的大标题也一起调轻`)**: homepage hero `<h1>` inherited base h1 700 (no hero-title rule set weight) → added `font-weight:500` to `.hero.hero-centered .hero-title` (`pages-extracted.css` 2529, homepage-scoped; b2b/b2c `.product-hero` heroes NOT changed) (`518a83c`). This is the one explicit exception to "首页第一屏不改" — user asked for it. Revert: remove the `font-weight:500` line.

- 2026-06-02 — **Arcadia layout layer — site-wide section-intro re-layout** (`sections.css` `.section-header`/`.section-label`/`h2`/`p` 2336-2374, `polish.css` `.section-header h2`/`.section-label`/`::before` 250-268). After the design-system pass the user: `除了首页第一屏的设计不改，其他的视觉布局层，你都按照Arcadia重新布局下，我看看`. **Constraint**: can't visually verify (autodebug needs user-enabled preview) → chose the SAFE high-leverage route: re-layout via the **shared** `.section-header` pattern every marketing page already uses (`<div.section-header><span.section-label>eyebrow</span><h2>title</h2><p>lead</p></div>`, ~18-20 instances across product/b2b/b2c/pricing/about) rather than blind per-page JSX surgery. Homepage **first screen** uses its own hero classes (`.hero-with-video`/`.hero-centered`), NOT `.section-header`, so it's inherently untouched (homepage's below-fold sections do change — allowed, only the first screen was off-limits). **Changes**: (1) eyebrow `.section-label` — dropped the gold filled pill (`background:var(--primary-dim)`+`border`+`border-radius:100px`+`padding:6px 14px`) → clean uppercase accent label `color:var(--primary-text)` `letter-spacing:.14em` `font-weight:700`, `margin-bottom:16→20`; `polish.css` `::before` dot made STATIC (removed `box-shadow` glow + `polish-label-pulse` infinite animation; `@keyframes polish-label-pulse` now unused but left harmless). (2) headline `.section-header h2` `36px`→`clamp(2rem,4vw,3rem)` + `line-height:1.1`; `polish.css` h2 tracking `-0.022em`→`-0.03em` (it loads last, was loosening it). (3) lead `.section-header p` `16px/#6C717A`→`1.0625rem/var(--text-secondary)`. (4) air: `.section-header` max-width `700→760`, margin-bottom `56→72px`. **Special cases preserved** by their own overrides: `.b2c-lang-aside .section-label` (3105) re-adds its white hero pill; `.b2c-feature-head` (499) keeps its own h2/label. Braces balanced; `tsc` n/a (CSS only). **NOT visually verified** — review + hard-refresh (Ctrl+Shift+R). Committed `a8568ab`. **Deeper per-page structural re-layout (two-column editorial splits, hero bgs, etc.) deferred** — those need per-section JSX changes that are unsafe to ship blind; do page-by-page once the user enables preview so autodebug can verify. Revert: restore the gold-pill `.section-label` + `36px`/`#6C717A`/`700`/`56px` values + the pulsing dot (`box-shadow:0 0 8px var(--primary)` + `animation:polish-label-pulse 2.4s ease-in-out infinite`) + polish h2 `-0.022em`. **Followup (`先调整眉标，标题`)**: user didn't answer the AskUserQuestion (align/eyebrow/title-size) → proceeded with the most-Arcadia safe default (kept CENTERED — left-align deferred as risky-blind). `a83711c`: headline `clamp(2rem,4vw,3rem)`→`clamp(2.25rem,4.8vw,3.75rem)` (~36-60px), line-height `1.1→1.08`, tracking `-0.03→-0.035em`; eyebrow `0.78rem/0.14em/mb20`→`0.75rem/0.18em/mb22`. Still on the table if user wants: LEFT-align the whole `.section-header` (`text-align:left;margin:0 0 72px`), drop the eyebrow dot, or eyebrow→outline pill. **Followup 2 (`中文标题换行`)**: user screenshot — the homepage `#models` ZH title "灵活的运营模式，适配各种规模" (13 chars) wrapped because the bigger headline (60px max) exceeded the 760px header. Fix `b9d5933`: `@media (min-width:768px){ html[lang="zh"] .section-header h2 { white-space:nowrap } }` — ZH titles stay one line on desktop (centered → overflows the 760 box symmetrically, never clipped on a wide screen; `<html lang="zh">` confirmed served). Scoped so EN (longer titles) still wraps and <768px ZH wraps (a long CJK title can't fit one line on a phone, and body `overflow-x:clip` would otherwise clip it). If a very long ZH title (~24+ chars) overflows a narrow laptop, the lever is trimming the h2 max clamp (3.75→~3.4rem). Revert: delete the `@media` nowrap rule. **Followup 3 (`安排`)**: proactively applied that safety trim — h2 max clamp `3.75rem`→`3.4rem` (~54px) so nowrap ZH titles don't overflow a narrow laptop (`bf2c148`). Revert: max back to `3.75rem`. **Followup 4 (`字体的粗细也调整一下`, dir unspecified)**: `.section-header h2` `font-weight:800→700` (`dc98860`) — cleaner at large sizes, esp. heavy CJK glyphs that looked blocky at 800. Eyebrow stays 700, lead normal. If user wants lighter (600) or bolder, that's the lever. Revert: weight back to 800. **Followup 5 (`眉标改成圆角胶囊` + `按钮盖住副标题`, `33ee648`)**: (a) user disliked the plain-text eyebrow → restored `.section-label` to a **rounded pill** (`padding:7px 16px; background:var(--primary-dim); border:1px solid rgba(254,191,29,.25); border-radius:999px; color:var(--primary-text); letter-spacing:.12em` + the polish.css static dot inside) — basically the original pill but calm (no pulse). (b) b2c `#payments` `.payment-request-cta` (`pages.css` 380) `margin-top:-16px`→`8px` — the `-16px` (from a prior over-nudge, log entry "32→8→-16") pulled the 申请支付方式 button up over the section subtitle. Revert: eyebrow back to the borderless text label; CTA margin-top back to -16px.

- 2026-06-02 — **Design-system unification pass (Arcadia-clean foundation, gold kept) — marketing pages** (`polish.css`, `sections.css`, `components.css`, `pages-extracted.css`). User: `https://www.arcadia.com/根据这个网站的风格，帮我规范一下现在整个官网的UI设计`. Via AskUserQuestion the user chose: **keep gold brand + adopt Arcadia's clean foundation**, **design-system unification first** (not page-layout redesign), **marketing pages first**. Researched arcadia.com = clean minimalism, heavy whitespace, near-black-on-white + a single teal-green accent, bold sans headlines, flat low-shadow cards, rounded solid buttons, calm/precise mood. **ROOT-CAUSE finding**: the recurring "黄色投影" (gold hover glow) the user has deleted section-by-section across dozens of sessions came from the **canonical** `.card:hover,.glass-card:hover,.feature-card:hover` in `polish.css` (~141, loads LAST so wins site-wide) applying `0 0 0 1px rgba(254,191,29,.22), 0 0 36px rgba(254,191,29,.14) + border rgba(254,191,29,.3)` to EVERY card on hover. CSS load order (globals.css): tokens→base→components→sections→pages→pages-extracted→**polish** (polish wins). `enhancements.css` is NOT imported (dead file). **Changes**: (1) **Cards** — `sections.css` ~2391 canonical surface: dropped frosted-glass (`background:var(--glass-bg)`+`backdrop-filter:blur(20px) saturate(1.2)`+white inset) → flat `background:var(--dark-card)` (#fff) + `border:1px solid var(--border-subtle)` + `box-shadow:var(--shadow-sm)`; hover gold→neutral `0 12px 28px rgba(0,0,0,.08)` + `border-color:var(--border-light)`. `polish.css` ~141 canonical hover gold→same neutral; card-icon hover `scale(1.06) rotate(-2deg)`→`scale(1.05)` (calmer, no rotate). (2) **Buttons** — `polish.css` ~50 `.btn-primary/.btn-accent` rest glow `0 6px 20px rgba(254,191,29,.25)`→`0 1px 2px rgba(0,0,0,.08)` (kept inset white sheen + the ::before shimmer sweep); hover `0 14px 32px rgba(254,191,29,.38)+ring`→`0 8px 20px rgba(0,0,0,.12)`, brightness 1.04→1.03. `components.css` ~536 `.btn-buy` (header CTA, not covered by polish) gold ring→neutral. Gold `:focus-visible` rings KEPT (a11y/brand). (3) **Stray system shadows neutralized**: `sections.css` `.hero-with-video .btn-primary:hover` (197), `.model-card:hover .btn-accent` (2749), `.audience-card:hover` (3949, also dropped gold border), `.pricing-card-ribbon--featured` (4141), `.pricing-tabs-btn.is-active` (4237); `polish.css` `.tag:hover` (515), `.image-ph:hover` (564, dropped gold ring+glow); `pages-extracted.css` round FAB buttons (622/1217 via replace_all), `.forum-sidebar .new-topic-btn:hover` (800), `.submit-btn` rest+hover (2011/2032). **Left intact** (intentional accents / recently user-tuned): gold status-dot & label-pulse glows, faint `rgba(...,.05/.1)` accent panels, `#support .card-icon` hover glow, the b2b `#modes` `.mode-bento-card` (5805) + `#revenue` open-card (5884) bespoke shadows. All brace-counts balanced; `tsc` n/a (CSS only). **NOT visually verified** — autodebug needs user-enabled preview (session IP has no active debug session); changes reasoned from the CSS + confirmed cascade-winning, not screenshotted. Committed `495b3f0`. Revert: `git revert 495b3f0` (or restore the gold rgba values per hunk). **Follow-up levers if user wants more Arcadia-clean**: calm the global golden-orb R3F canvas + frosted page bg; reduce `.section-alt::after` gold dot texture; the `.icon-gradient--green/blue/purple` rainbow utility classes are off-brand. These are layout/visual (the option the user deferred), not system. **Continuation (same session, `接着做调整`)**: investigated those 3 deferred levers — **2 are already moot in light mode** (the default/only effective theme; dark toggle was removed): the golden-orb canvas + `.orbs-glass-layer` are hidden via `html:not([data-theme='dark']) .orbs-root{display:none}` (`polish.css` 1114) and `.section-alt::after` gold dot texture is `opacity:0!important` (`polish.css` 1166) in light mode (there's a whole "Light-mode ambient cleanup" block 1118-1182 already suppressing gold haze). The `icon-gradient--blue/purple/green` classes in `Header.tsx` mega-menu are **inert** — `.nav-dropdown-menu .dropdown-icon` (`components.css` 472, specificity 0,2,0) overrides them to white tiles (→gold on hover), so no rainbow renders. So instead finished the **interaction-state consistency** (the real remaining gold-on-hover leftover): `polish.css` ~174 input/textarea/select `:hover` border `rgba(254,191,29,.4)`→`var(--border-medium)` (gold kept on `:focus` ring); `components.css` ~810 `.scroll-to-top:hover` gold color/border/`0 0 20px var(--primary-dim)` glow → neutral (`--text-primary`/`--border-medium`/`--shadow-lg`). Braces balanced; `tsc` n/a. Committed `3704371`. Remaining Arcadia-clean gains are now purely the **visual/layout layer** the user deferred (hero image bgs, editorial typography scale, whitespace) — needs opt-in. Revert: input hover back to `rgba(254,191,29,.4)`, scroll-to-top hover back to `--primary` + the gold glow.

- 2026-06-02 — **b2c `#payments` "全球支付矩阵" — raised the 申请支付方式 CTA button** (`pages.css` `.payment-request-cta` 380). User red-boxed the centered yellow `申请支付方式` button + `将这个按钮的位置网上移一点` (twice). The button is `.payment-request-cta` (the CTA row replacing the inline form, `PaymentRequestForm.tsx` 157) — `margin-top: 32px → 8px → -16px` across two nudges to pull it up under the section description. `tsc` n/a (CSS only). Pushed `9c3adb2`→`0c912e1`. Revert: margin-top back to `32px`.

- 2026-06-02 — **b2c `#support` bento — 3 light cards white → light grey `#F1F2F4`** (`sections.css` `.support-bento-card` 5495). User red-boxed the tall (分步视频教程), wide (完整技术文档), small (官方社区论坛) cards + `将红框的卡片颜色改成浅灰色，参考其他灰色卡片的颜色`. Changed the shared `.support-bento-card` `background:#fff → #F1F2F4` (site grey `--dark`). The dark accent card overrides via `.support-bento-accent` gradient so it's unaffected. `tsc` n/a (CSS only). Pushed `ef4402d`. Revert: back to `#fff`.

- 2026-06-02 — **b2b `#features` "功能亮点" — moved the section up on anchor jump so all content is visible when clicking the 功能 nav link** (`pages.css` new `#features` rule after the 3120 white-bg block). User screenshot of `/zh/b2b#features` after jumping: the "功能亮点" title landed mid-viewport with whitespace above and the 步骤2/3 + dark payment bar fell below the fold; `把功能亮点的整体位置往上移，确保我点击功能的时候，跳转到这一页能看到全部内容`. Root cause: anchor jump uses the global `:where([id]){scroll-margin-top:calc(72px+16px)}` (base.css 18) so `#features` top lands 88px down, THEN `.section{padding:100px 0}` (base.css 152) pushes the title another 100px → title ~mid-screen. Fix: scoped `#features { scroll-margin-top: calc(var(--header-height,72px)+8px); padding-top: 40px }` — tighter jump offset (88→80px) + top padding 100→40px so the title sits near the top of the fold and more cards show. Bottom padding stays 100px (only `padding-top` overridden). `tsc` n/a (CSS only). **Eyeballed, NOT visually verified** (autodebug needs user preview) — if title still too low, drop `padding-top` further (e.g. 24px). Revert: delete the `#features` rule.

- 2026-06-02 — **b2c `#support` dark accent card (一对一技术支持) — left-aligned the `1:1` stat + added a white GCSS globe watermark on the right at 50% opacity** (`sections.css` `.support-bento-accent`/`-stat` ~5536-5570, new `public/images/support-accent-globe.png`). User: `将1:1专属对接，左对齐小标题和内容，然后右边插入<png> 这个插图，做50%的透明度，做背景图`. The pasted PNG looked blank — it's the **GCSS globe logo as WHITE line-art on transparent bg** (mode P + transparency; invisible on white, shows on the dark card) — confirmed by compositing over dark. Saved optimized (900×672, 83KB, kept alpha). (1) **Stat**: `.support-bento-accent-stat` was `position:absolute; right:32px; bottom:32px; align-items/text-align flex-end/right` → `position:relative; z-index:1; align-items/text-align flex-start/left`. Body keeps `flex:1 1 auto` so the `1:1` block sits **bottom-left**, left-aligned with h3+desc. (2) **Watermark**: new `.support-bento-accent::after` = `position:absolute; top:0; bottom:0; right:-24px; width:52%; background:url(/images/support-accent-globe.png) no-repeat center right; background-size:contain; opacity:.5; z-index:0` (card already `overflow:hidden`). Added `.support-bento-accent-body{position:relative;z-index:1}`. Mobile (≤640) `-accent-stat` still has the old `position:static` override (harmless). `tsc` n/a (CSS only). Pushed `da5a7d5`. Revert: `-accent-stat` back to `absolute right/bottom 32px + flex-end/right`, delete the `::after` + `-accent-body z-index` rule + the PNG.

- 2026-06-02 — **b2c `#payments` "全球支付矩阵" — nudged the 申请支付方式 CTA button up** (`pages.css` `.payment-request-cta` 380). User screenshot red-boxed the centered yellow `申请支付方式` button + `将这个按钮的位置网上移一点`. The button is `.payment-request-cta` (the CTA row that replaces the inline form, `PaymentRequestForm.tsx` 157) — `margin-top: 32px` → `8px` to raise it closer to the section description. `tsc` n/a (CSS only). Pushed `9c3adb2`. Revert: margin-top back to `32px`.

- 2026-06-02 — **b2c `#demo` "系统演示" — restored its grey section background** (`sections.css` ~5748, new `.b2c-demo-section` rule). User screenshot of the b2c demo (now white after the body→white fix `e1a4592`) + `将这里的系统演示的背景，恢复之前的灰色`. b2c `#demo` is `<section className="section b2c-demo-section">` (plain transparent `.section`) — so the prior body→white change turned its surround white. Added `.b2c-demo-section { background: var(--dark) }` (= #F1F2F4 grey in light mode) scoped to its unique class to bring back the previous grey band (b2b `#demo` is a separate `section-alt`, already grey from `0f6beff`). `tsc` n/a (CSS only). Pushed `ba53ff1`. Revert: delete the `.b2c-demo-section` rule.

- 2026-06-02 — **b2c `#support` bento cards — hover shadow gold-tint → neutral black** (`sections.css` `.support-bento-card:hover` 5503). User: `卡片的投影颜色，与其他的统一一致，不要黄色`. The 4 bento cards' shared hover `box-shadow` was `0 14px 32px rgba(214,158,0,.10)` (gold) → `rgba(0,0,0,.10)` (neutral, matching the site's other white cards). `tsc` n/a (CSS only). Pushed `cc4be98`. Revert: back to `rgba(214,158,0,.10)`.

- 2026-06-02 — **b2b/b2c — ROOT CAUSE of all the recurring "grey background" complaints: page `body` was grey. Changed `body:has(.product-hero)` from `#F1F2F4` → `#fff`** (`pages-extracted.css` 58). User screenshot of the b2c "准备部署？" orange CTA banner (`.cta-section.cta-banner`, `product.cta`, b2c/page.tsx ~630) sitting on grey + `这里的灰色背景改成白色`. **Diagnosis**: `.cta-banner` is `background:transparent` (sections.css 3508) — same as the earlier `#support`/plain-`.section` cases. The grey they ALL showed came from `body:has(.product-hero){ background:#F1F2F4 }` (only b2b+b2c use `.product-hero`, so it's scoped to exactly those 2 pages). Every transparent section revealed that grey body. Fixed at the root: body → `#fff`. **Effect**: all *transparent* sections (b2b #architecture/#revenue/#pricing/#aftersales, b2c #gallery/#demo, the CTA banner) now read white — matches the original `将B2B,B2C灰色的背景都改成白色` goal. **`section-alt` sections are UNAFFECTED** (they set their own explicit bg), so the b2b `#demo` grey exception from `0f6beff` STAYS grey. Hero keeps its own image bg. Earlier per-section white overrides (`#support` id rule etc.) are now largely redundant but harmless. `tsc` n/a (CSS only). Pushed `e1a4592`. Revert: `body:has(.product-hero)` back to `#F1F2F4`.

- 2026-06-02 — **b2c `#support` bento — shortened the tall left card's yellow lesson-list box to the red-box height + restructured the dark accent card** (`sections.css` `.support-bento-list` 5548 / `.support-bento-accent` 5536 / `-accent-stat` 5663 + mobile 5722). User pasted a screenshot red-boxing the lower portion of the left card's yellow list box (`将左边卡片黄色区域，改成红框的高度`) then a 2nd msg `黑色卡片的内容，小标题和内容与左边卡片高度对齐，1:1专属对接放卡片右下角位置`. (1) **Yellow box**: `.support-bento-list` was `flex:1; justify-content:space-between` (stretched to fill the whole grid-row-span-2 card, spreading the 4 lesson items over the full height). Pixel-measured (red box ≈64% of the yellow box, bottom-aligned) → dropped flex:1/space-between, set `margin:auto 0 0` (pushes the ul to the card bottom, the `.support-bento-card` is flex-column so `margin-top:auto` bottom-aligns it) so the box now sizes to its content (~red-box height) and pins to the bottom; gap 10→12px. (2) **Black accent card** (`.support-bento-accent`): was `flex-direction:row; align-items:center; justify-content:space-between` (body+stat side-by-side, vertically centered → title sat lower than the 官方社区论坛 card's title to its left). Changed to `flex-direction:column; align-items:flex-start; gap:10px` so h3+desc top-align with the community card. (3) **1:1 block** (`.support-bento-accent-stat`): `flex-shrink:0` → `position:absolute; right:32px; bottom:32px` (32px = card padding) so "1:1 专属对接" sits in the bottom-right corner (card already `position:relative`). Mobile (≤640) `.support-bento-accent-stat` gets `position:static` so it flows back into the stacked column. `tsc` n/a (CSS only). Pushed `045411d`. **Eyeballed, NOT visually verified** (autodebug needs user preview) — yellow-box height + accent overlap may need a nudge. Revert: `.support-bento-list` back to `margin:8px 0 0; flex:1; justify-content:space-between; gap:10px`; `.support-bento-accent` back to `flex-direction:row; align-items:center; justify-content:space-between; gap:20px`; `-accent-stat` back to `flex-shrink:0` (drop absolute) + drop the mobile `position:static`.

- 2026-06-02 — **b2b/b2c `#demo` "系统演示" section — reverted its background BACK to grey** (`sections.css` 5744, the white-override rule). User screenshot of `#demo` (now white) + `这里系统演示这一屏的背景，恢复之前的灰色`. The grey→white change from `01e18cb` had added `#demo.section-alt` to the `#modes…#pricing { background:#fff }` override rule. Removed just that one selector so `#demo` falls back to `.section-alt { background: var(--dark) }` (grey). The other selectors (`#modes`,`#revenue`,`#support`,`#license`,`#changelog`,`#releases`,`#pricing`) stay white. `tsc` n/a (CSS only). Pushed `0f6beff`. Revert: re-add `#demo.section-alt,` to the white-override rule.

- 2026-06-02 — **b2b/b2c `#demo` "System Demo" credential card — made FULLY static on hover (no lift, no shadow, no border change)** (`sections.css` `.b2c-demo-card:hover,:focus-within` 554-559). Two turns: (1) user red-boxed the left "Global Admin Panel" creds card + `这里红框的卡片无交互投影效果` → set hover `box-shadow` to `none` (kept the `translateY(-4px)` lift + `border-color:var(--border-light)`). (2) `鼠标悬浮，卡片也不做悬停效果,直接静态展示` → zeroed the rest: `transform:none`, `box-shadow:none`, `border-color:var(--border-subtle)` so hover is visually identical to rest. `.b2c-demo-card` is shared by BOTH the b2b (`B2BDemoCredCard`) and b2c `#demo` creds cards. `tsc` n/a. Pushed `93839bb`→`4bcc6b1`. Revert: hover back to `transform:translateY(-4px); box-shadow:0 14px 40px rgba(0,0,0,.12); border-color:var(--border-light)`.

- 2026-06-02 — **b2b `#aftersales` orbital diagram — repositioned the top-left (Video Deployment Guide, `--tl`) + bottom-right (Professional Technical Team, `--br`) floating cards per red-box refs** (`pages.css` `.b2b-support-card--tl`/`--br` ~11254). User pasted a screenshot with 2 red boxes + `左上角，左下角的两个卡片，分别按红框的位置调整下` (said 左下角/bottom-left but the 2nd box is on the bottom-**RIGHT** Team card — pixel-detected box at img x[2334-2704], right of globe center x=1900; flagged to user). **Method**: pixel analysis (PIL+scipy.ndimage) — detected the 4 gold card-icon tiles + central globe to solve the stage frame (img x[1086-2753] W=1667, y[890-1790] H=900) and confirmed **scale ≈1.5×** (Windows 150% display: tl red-box width 371px ≈ card 248css×1.5=372px, so boxes ≈ desired card bounds). Deltas: tl → right ~6.5% + up ~4%; br → down ~15%. Applied: `--tl { top:0; left:3% }`→`{ top:-4%; left:9% }`; `--br { bottom:0; right:3% }`→`{ bottom:-15%; right:3% }`. Negative top/bottom push the 2 diagonal cards slightly outside the stage box (no clip — `.b2b-support-stage` has no `overflow`); they sit in the section whitespace. Tablet (≤980) overrides only `--tr`/`--bl` top, phone (≤720) stacks static — both unaffected. `tsc` n/a (CSS only). **Eyeballed estimate, NOT visually verified** (autodebug needs user preview) — likely needs a nudge. Pushed `2559c6e`. Revert: `--tl`→`top:0;left:3%`, `--br`→`bottom:0;right:3%`.

- 2026-06-02 — **b2b `#features` Step 1 card — GCSS browser illustration replaces the "01" big number; FINAL sizing = fills column at max size, vertically centered, no crop, flush top/bottom (gap 0); card bg #F1F2F4** (`b2b/page.tsx` ~279 hiw-visual, `pages.css` `.b2b-hiw-card--top`/`.b2b-hiw-visual--img`/`.b2b-hiw-visual-fill` ~1996-2090 + mobile ~2185, new `public/images/b2b-features-step1.png` 2216×1472 RGBA). Only the top card (`i===0`/`top`) renders `<div className="b2b-hiw-visual-fill"><Image src="/images/b2b-features-step1.png" width=1108 height=736 style={{width:'100%',height:'auto',display:'block'}}/></div>` instead of `.b2b-hiw-bignum`; Steps 2/3 keep `02`/`03`. After ~10 sizing turns the settled spec: `.b2b-hiw-card--top` `grid-template-columns:1.1fr 1fr` (wider visual col), `background:#F1F2F4` (THIS card only — 2/3 stay white), `padding:0 32px` (vertical padding REMOVED so the in-flow image reaches the card top/bottom edges, gap 0); `.b2b-hiw-card--top .b2b-hiw-content{padding:32px 0}` carries the text's vertical padding. Image is in-flow (drives card height — card grows, no crop), `.b2b-hiw-visual--img` has NO overflow:hidden, card's own `overflow:hidden`+`border-radius:24px` clips corners. `.b2b-hiw-visual-fill` = `width:100%` (was an absolute negative-inset box in an earlier iteration). Mobile (≤760) resets `.b2b-hiw-visual-fill` static + image `width:100%!important;height:auto!important`. Rejected en route: `maxHeight:190`/contain-letterbox (too small), `objectFit:cover` (cropped), `min-height:300px` tall card, negative-inset flush. `tsc` 0. Pushed `1259df7`. Revert: restore `<span className="b2b-hiw-bignum">0{i+1}</span>` for all cards, drop the `--img`/`-fill` markup+CSS, `.b2b-hiw-card--top` back to `1.15fr 0.85fr` + no bg + inherited padding, drop the content-padding rule + the PNG.

- 2026-06-02 — **b2b/b2c `#support` ("多种功能支持" / Multi-Platform Feature Support) — grey section bg → white** (`sections.css` ~5740, broadened the existing white-override rule). User pasted a b2b screenshot red-boxing the grey band behind the support-matrix + `这里灰色背景改成白色`. Grey = `#F1F2F4` (`--dark`). NOTE: b2b `#support` is `<section className="section">` (transparent — the prior `#support.section-alt` selector did NOT match it; b2c `#support` IS `section-alt`). Changed `#support.section-alt` → plain `#support` (id specificity 1,0,0 beats `.section-alt` 0,1,0) so it forces white on BOTH pages regardless of the `.section`/`.section-alt` variant. Diagnosis: verified via the served dev bundle (`curl` + parse) that `#features`/`#support`/body all already resolve to white in CSS — so the user's grey was likely a stale Turbopack content-addressed CSS bundle; the id-selector change is a guaranteed override either way. `tsc` n/a (CSS only). Pushed `4356ff5`. **If still grey: hard-refresh (Ctrl+Shift+R).** Revert: `#support` back to `#support.section-alt`. (Earlier this session: the same rule was first extended to add `#support/#license/#changelog/#releases/#demo .section-alt` → all white per `将B3B,B2C 灰色的背景，都改成白色`, pushed `01e18cb`.)

- 2026-06-02 — **b2b `#features` Step 1 card — inserted GCSS browser illustration into the visual panel (replaces the "01" big number), short card + `object-fit:cover` fill (cropped, contained)** (`b2b/page.tsx` ~279 hiw-visual + image, `pages.css` `.b2b-hiw-visual--img`/`.b2b-hiw-card--top` ~2063 + mobile ~2170, new `public/images/b2b-features-step1.png` 2216×1472 RGBA). User red-boxed the Step 1 (top, full-width) card's right visual panel (was the faint grey `01` bignum) + pasted a browser-mockup illustration ("Welcom to GCSS" + http://app.gcss.hk URL bar + 2 phone screens + a "Global Charge" globe card). Only the top card (`i===0`/`top`) renders `<Image src="/images/b2b-features-step1.png" width=1108 height=736 sizes="(max-width:760px) 90vw, 420px" style={{position:'absolute',inset:0,width:'100%',height:'100%',objectFit:'cover',objectPosition:'center'}}>` instead of `.b2b-hiw-bignum`; Steps 2/3 keep `02`/`03`. **Sizing iterated 6 turns** → user (AskUserQuestion) chose "矮卡片+插图铺满裁剪": short (text-driven) card height, illustration enlarged to fill the panel, overflow cropped. `.b2b-hiw-visual--img` = transparent (no grey panel chrome) + `position:relative; overflow:hidden`. `.b2b-hiw-card--top` `align-items:center`→`stretch` so the visual cell stretches to the text-driven row height (gives the cover image a definite box). Mobile (≤760): card `overflow:hidden`, image forced `position:static!important; width:100%!important; height:auto!important` to flow normally in the stacked column. **Rejected attempts**: `maxHeight:190` (too small), absolute `bottom:0` (bottom spilled past card), `top:0/bottom:0` contain (read too small), `min-height:300px` taller card (rejected — wanted original short height). If the cover crop cuts something important, shift `objectPosition` (e.g. `center top`). **Followups**: (a) `取消插图的容器限制…铺满卡片右边上下间隙为0` → flush to card top/bottom/right BORDER edges via negative insets cancelling the `30px 32px` padding (was cover/cropped). (b) `不要裁切插图，要整体展示` → **FINAL**: switched to **contain** + a wrapper `<div className="b2b-hiw-visual-fill">` (`position:absolute; top:-30; bottom:-30; right:-32; left:0` — reaches the card edges) holding the `<Image>` at `{width:'100%',height:'100%',objectFit:'contain',objectPosition:'right center'}`, so the WHOLE illustration shows at full card height (flush top/bottom, right-aligned), no crop, left side letterboxed onto the white card. `.b2b-hiw-visual--img` has NO `overflow:hidden`; the card's own `overflow:hidden`+`border-radius:24px` clips to rounded corners. Mobile resets `.b2b-hiw-visual-fill` to `position:static`. `tsc` 0. Pushed `575d394` (chain: `2e2788a`→`f0706f4`→`8b78aae`→`51b05ae`→`780907d`→`1781d7d`→`f965c54`→`9be7769`→`575d394`). Revert: restore `<span className="b2b-hiw-bignum">0{i+1}</span>` for all cards, drop the `--img` class + the `.b2b-hiw-visual--img`/`-fill`/`--top align-items`/mobile CSS + the PNG.

- 2026-06-02 — **b2b/b2c `#demo` "System Demo" credential card — made FULLY static on hover (no lift, no shadow, no border change)** (`sections.css` `.b2c-demo-card:hover,:focus-within` 554-559). Two turns: (1) user red-boxed the left "Global Admin Panel" creds card + `这里红框的卡片无交互投影效果` → set hover `box-shadow` to `none` (kept the `translateY(-4px)` lift + `border-color:var(--border-light)`). (2) `鼠标悬浮，卡片也不做悬停效果,直接静态展示` → zeroed the rest: `transform:none`, `box-shadow:none`, `border-color:var(--border-subtle)` (= the static border) so hover is visually identical to rest. The `transition` on `.b2c-demo-card` (552) is harmless now. `.b2c-demo-card` is shared by BOTH the b2b (`B2BDemoCredCard`) and b2c `#demo` creds cards. Did NOT touch the QR-card side or the Launch button. `tsc` n/a (CSS only). Pushed `93839bb`→`4bcc6b1`. Revert: hover back to `transform:translateY(-4px); box-shadow:0 14px 40px rgba(0,0,0,.12); border-color:var(--border-light)`.

- 2026-06-02 — **b2b + b2c — forced all grey `.section-alt` section backgrounds to white** (`sections.css` ~5735, extended the existing `#modes/#revenue` white-override rule). User: `将B3B,B2C 灰色的背景，都改成白色` (B3B = typo for B2B). The grey came from `.section-alt { background: var(--dark) }` (`pages.css` 1247). `#features` was already pinned white (`pages.css` 3104), and `#modes`/`#revenue` via the existing `sections.css` 5737 rule. Added `#support`, `#license`, `#changelog`, `#releases`, `#demo` (all `.section-alt`) to that same `background:#fff` rule — verified those IDs are used ONLY on the b2b/b2c pages so the override stays scoped. The `.section-alt::after` gold dot-texture (`polish.css` 382) left intact (sits on top, harmless). `tsc` n/a (CSS only). Pushed `01e18cb`. Revert: drop the 5 added selectors, leaving just `#modes.section-alt, #revenue.section`.

- 2026-06-02 — **b2b `#features` Step 1 card — inserted GCSS browser illustration into the visual panel (replaces the "01" big number), fills card top-to-bottom, contained** (`b2b/page.tsx` ~279 hiw-visual, `pages.css` `.b2b-hiw-visual--img`/`.b2b-hiw-card--top` ~2063 + mobile ~2170, new `public/images/b2b-features-step1.png` 2216×1472 RGBA). User red-boxed the Step 1 (top, full-width) card's right-side visual panel (was the faint grey `01` bignum) + pasted a browser-mockup illustration ("Welcom to GCSS" + http://app.gcss.hk URL bar + 2 phone screens + a "Global Charge" globe card). Only the top card (`i===0`/`top`) renders the `<Image src="/images/b2b-features-step1.png" width=1108 height=736>` instead of the `.b2b-hiw-bignum` span; Steps 2/3 keep their `02`/`03`. **Iterated on sizing** (4 turns): final = image `style={{position:'absolute',top:0,bottom:0,right:0,height:'100%',width:'auto',objectFit:'contain'}}` — absolutely positioned so it does NOT drive the card height (card height = text column), `top:0/bottom:0` makes it fill the panel top-to-bottom, `width:auto` keeps ratio (~card_height×1.5 ≈ 225px, fits the 0.85fr ~258px col), right-aligned. `.b2b-hiw-visual--img` = transparent (no grey panel chrome) + `position:relative; overflow:hidden` (clips any spill). `.b2b-hiw-card--top` changed `align-items:center`→`stretch` so the visual cell stretches to the text-driven row height (gives the absolute image a definite containing height). Mobile (≤760): card `overflow:hidden`, image forced `position:static!important; width:100%!important; height:auto!important` so it flows normally in the stacked column. Rejected earlier attempts: `maxHeight:190` (too small), absolute `top:50%` centered overflowing top+bottom (bottom spilled past card). `tsc` 0. Pushed `2e2788a`→`f0706f4`→`8b78aae`→`51b05ae`→`780907d`. Revert: restore `<span className="b2b-hiw-bignum">0{i+1}</span>` for all cards, drop the `--img` class + the `.b2b-hiw-visual--img`/`--top align-items`/mobile CSS + the PNG.

- 2026-06-01 — **b2b `#revenue` accordion — wider gap between items** (`sections.css` `.revenue-accordion` ~5845). User red-boxed the 4 accordion rows + `这里的间距再大一点`. `gap: 10px → 22px`. `tsc` n/a (CSS only). Revert: gap back to 10px.

- 2026-06-01 — **b2b `#architecture` hub avatar icon circles — faint-gold tint → solid yellow bg + black icon** (`pages.css` `.b2b-hub-avatar-ic` 1568). User red-boxed the 6 operator/user avatar circles + `这里的图标颜色改成黄色底，黑色线性图标`. `background: rgba(254,191,29,.14)` (faint tint) + `color: #B07A1E` (brown icon) → `background: var(--primary,#febf1d)` (solid yellow) + `color: #1a1a1a` (black line icon, lucide Building2/User inherit via currentColor). CSS-only. Also this turn: operator/user 3rd node label `N → C` (`b2b/page.tsx` `['A','B','N']`→`['A','B','C']` both rows, `72f4059`). Pushed `e1be3e3`. Revert: bg back to `rgba(254,191,29,.14)` + color `#B07A1E`.

- 2026-06-01 — **b2b `#architecture` hub core title — "APP · CPMS · ADMIN" → "APP/CPMS/ADMIN", forced single line** (`messages/{en,zh}.json` `b2b.overview.center`, `pages.css` `.b2b-hub-core-title` 1639). User: `APP · CPMS · ADMIN 中间卡片的标题改成APP/CPMS/ADMIN,确保内容不换行`. Changed the i18n value in BOTH locales (· dot-separated → slash) and added `white-space: nowrap` to `.b2b-hub-core-title` so the 2.2rem bold title stays one line (fits the col2 ~380px content width; mobile ≤460 drops to 1.5rem so still fits). `center` is also the core div's aria-label — fine. `tsc` n/a. Pushed `7b4a293`. Revert: value back to `APP · CPMS · ADMIN`, drop the `white-space: nowrap`.

- 2026-06-01 — **pricing `#addons` "可选扩展" — swapped the `adminLang` + `gateway` icons (were near-identical rectangles)** (`pricing/page.tsx` `addonIcons` 88-99). User red-boxed the top-row 2nd (后台额外语言翻译) + 3rd (额外支付网关) icons + `红框的图标太像了，更换一下` — both were two-tone rounded-rect "window/card" shapes. `adminLang` → a **languages/translate** glyph (black left stylized char `M4 5h7` + two curved strokes, gold `.ic-accent` right "A" `M12.5 20l4-9 4 9` + crossbar) so it clearly reads as translation, distinct from the phone (`mobileLang`). `gateway` → a **credit card** (black `rect 2,5,20,14 rx2.5`) with a gold `.ic-accent` chip (`rect 5,9,4.5,3.2`) + stripe (`M14 15h4`) so it reads as a payment card, distinct from the `pos` monitor. Kept the two-tone scheme (base black `currentColor` + `.ic-accent` gold via the `.addon-card-icon svg .ic-accent{stroke:var(--primary)}` rule). `tsc` 0. Pushed `80fa0c7`. Revert: restore the prior `adminLang` (`rect 3,4,18,14` + `M3 9h18…`) + `gateway` (`rect 2,6,20,13` + `M2 11h20M6 16h3`) SVGs from `git show 80fa0c7^:`.

- 2026-06-01 — **pricing `#addons` "可选扩展" — removed the vertical divider lines between cards** (`sections.css` ~4352). User red-boxed the two vertical lines between the 3 columns + `红框的线条去掉`. Deleted both `::before` divider rules (the `@media(min-width:901px) .addons-grid .addon-card:not(:nth-child(3n+1))::before` desktop dividers + the `@media(max-width:900px) and (min-width:561px) ...:nth-child(2n)::before` tablet companion) added back in `2dd3e6f`. The `.addon-card{position:relative}` from the same commit is left (harmless). `tsc` n/a (CSS only). Pushed `0ebe760`. Revert: restore the two `::before` divider `@media` blocks from `git show 0ebe760^:`.

- 2026-06-01 — **b2b `#features` cards — grey/gold → white + border + neutral hover shadow (de-golded)** (`pages.css` `.b2b-hiw-card`/`-step`/`-visual`/`-bignum` ~1977-2057). User: `卡片颜色改成白色，增加边线，参考其他白色带边线的卡片样式，包括交互时的投影样式，不要黄色`. Matched the `.b2b-hub-card` white-card pattern: `.b2b-hiw-card` bg `#F1F2F4`→`#fff`, border `rgba(0,0,0,.06)`→`.08`; hover gold (`border rgba(254,191,29,.28)` + `box-shadow 0 14px 32px rgba(214,158,0,.10)`) → **neutral** (`border rgba(0,0,0,.12)` + `0 18px 40px rgba(0,0,0,.10)`). Stripped all yellow from the card sub-elements: `.b2b-hiw-step` badge gold-border/`--primary-text` → `#F4F5F7` bg + `rgba(0,0,0,.10)` border + `--text-secondary`; `.b2b-hiw-visual` gold-gradient panel → flat `#F4F5F7` + `rgba(0,0,0,.06)` border; `.b2b-hiw-bignum` `--primary-text` → `rgba(0,0,0,.16)` grey. Deleted the orphaned `.b2b-hiw-num` rule (unused since the prior layout restructure). Dark `.b2b-hiw-bar` (payment flow, not a card) left untouched. `tsc` 0. Pushed `ee436c2`. Revert: restore the gold values from `git show ee436c2^:`.

- 2026-06-01 — **b2b `#features` "功能亮点" — restructured into a step-card layout (full-width Step 1 + two half cards) per ToDesktop-style reference** (`b2b/page.tsx` ~268 feats map, `pages.css` `.b2b-hiw-card--top`/`--half` ~1994 + mobile ~2138). User red-boxed the whole feature block (3 step cards + dark payment bar) + pasted a ToDesktop reference (Step 1 full-width hero card text-left/visual-right; Step 2 + Step 3 side-by-side below each text-above-visual; dark stats bar at bottom) + `把这里的布局改成这种`. **Before**: grid had feat1 `--wide` (text+visual side-by-side) sharing row 1 with feat2 `--num` (corner watermark number, no visual panel), feat3 `--full` spanning row 2. **After**: feat1 (i===0) → `--top` full-width card (`gridColumn:'1 / -1'`), `display:grid; grid-template-columns:1.15fr 0.85fr` text-left + gold-gradient bignum visual right; feat2 + feat3 → `--half`, each `display:flex; flex-direction:column` so the `.b2b-hiw-content` (step badge + h3 + p) stacks ABOVE a full-width `.b2b-hiw-visual` bignum panel — the two sit side-by-side in the 2-col `.b2b-hiw-grid`. Every card now renders a `.b2b-hiw-visual`/`.b2b-hiw-bignum` (dropped the conditional `.b2b-hiw-num` corner-number branch — that CSS rule at ~2038 is now orphaned but left harmless). Dark `.b2b-hiw-bar` (payment flow) untouched. Mobile (≤760): `--top` collapses to `flex-column`; `.b2b-hiw-visual` min-height 120px. `tsc` 0. Pushed `11b60d3`. Not screenshotted (autodebug needs user preview). Revert: restore the `wide`/`full` const logic + the conditional num/visual branch in the feats map, and the `.b2b-hiw-card--wide`/`--full` CSS (both from `git show 11b60d3^:`).

- 2026-06-01 — **b2b `#aftersales` orbital diagram — swapped in user-uploaded icons (center globe + video node), gradient node style** (`b2b/page.tsx` supportCards + core/node markup; `pages.css` `.b2b-support-core`/`-node`/`-card-ic`; new `public/images/b2b-support/center-globe.png` 320² + `node-video.png` 120²). User uploaded icons but the clipboard kept DUPLICATING — across 2 batches only 2 distinct assets arrived (GCSS globe squircle + a yellow-radial-gradient play circle); the rest were byte-identical play-icon copies (md5-confirmed). After `没有更换到，重新更换下` + `整体设计都按照参考图来`, implemented with what arrived: **center core** renders `center-globe.png` via `<Image 104²>` (core bg→transparent, kept box-shadow + pulse `::before`, `img{border-radius:26px}`); **video node** (item1) renders `node-video.png` via `<Image 44²>` + a `b2b-support-node--img` modifier (`background:transparent`); the **other 3 nodes** use a CSS yellow radial-gradient circle (`linear-gradient(140deg,#FFD45E,#F5A623)`) + black lucide glyph — item4 Headphones→**Gem** (reference's diamond); card chips gradient-matched. `supportCards` typed `{key;pos;Icon:typeof Play;nodeImg?:string}[]`; dropped unused `Globe`/`Headphones`, added `Gem`. **Still need distinct uploads for forum/knowledge/team** (send one-per-message; clipboard dupes otherwise) to fill their `nodeImg` slots like item1. tsc 0. Not screenshotted (autodebug needs user preview). Revert: core/video back to lucide `Globe`/`Play`, node bg flat `var(--primary)`, drop the 2 PNGs + `--img`.

- 2026-06-01 — **b2b `#architecture` hub cards — shadow color warm-brown/gold → faint light-grey** (`pages.css` `.b2b-hub-card` 1509, `.b2b-hub-card::before/::after` 1522, `.b2b-hub-core` 1630). User screenshot of the rendered hub + `投影颜色改成浅灰色，一点点淡淡的就行`. Outer cards `0 18px 44px rgba(140,120,80,.08)` → `0 16px 40px rgba(0,0,0,.05)`; stacked-deck pseudo layers `0 14px 30px rgba(140,120,80,.07)` → `0 12px 28px rgba(0,0,0,.04)`; core `0 22px 60px rgba(212,158,0,.14)` (gold tint) → `0 20px 50px rgba(0,0,0,.06)`. All now neutral grey + very subtle. CSS-only. NOTE: a concurrent session was committing in the repo this turn, so these edits landed bundled into HEAD `ee3790e` (not a standalone commit) — verified present via `git show HEAD:`; fork up-to-date. Revert: restore the brown/gold rgba shadow values.

- 2026-06-01 — **b2b `#revenue` grey panel — bg opacity now 50%** (`sections.css` `#revenue .b2b-revenue-panel`). User: `背景颜色降低50%` (→`rgba(242,242,242,0.5)`), `再降低50%` (→`0.25`), then `取消上一步操作` (undo → back to `0.5`). Final state = `rgba(242,242,242,0.5)`. `tsc` n/a (CSS only). Revert: back to solid `#f2f2f2`.

- 2026-06-01 — **b2b `#revenue` grey panel — taller** (`sections.css` `#revenue .b2b-revenue-panel`). User: `背景高度高一点` (typo "高一旦"). Vertical padding `56→96px` (mobile `36→60px`); horizontal padding (48/20) + radius (28/20) unchanged. `tsc` n/a (CSS only). Revert: padding back to `56px 48px` / mobile `36px 20px`.

- 2026-06-01 — **b2b `#architecture` hub diagram — enlarged overall + stacked-deck card effect** (`pages.css` `.b2b-hub*` 1468-1645). User: `整体展示放大一点，卡片做出层叠的效果`. (1) **Enlarge**: `.b2b-hub` max-width 1180→1340, column-gap 48→60, row-gap 28→44 (extra room for the stack layers below each card), bottom padding 16→24; `.b2b-hub-card` padding 24/26→30/34, gap 18→22, radius 22→24; card-title .82→.9rem (dot 8→9px); avatar-ic 46→54px; avatar-label .78→.84rem; tag padding 9/12→11/14, font .82→.88rem, radius 10→11; core padding 42/36→54/46, radius 26→28, gap 14→16, core-title 1.85→2.2rem, eyebrow .78→.84rem, pill 9/20→11/24 + .86→.95rem. (2) **Stacked deck**: added `.b2b-hub-card::before` + `::after` (NOT on the center `.b2b-hub-core` — matches ref where only the 3 outer cards stack) = two `position:absolute; inset:0; z-index:-1` white layers w/ faint border+shadow, offset DOWN + narrowed via `transform: translateY(10px) scaleX(0.95)` (::after, opacity .7) + `translateY(20px) scaleX(0.89)` (::before, opacity .45) → reads as a deck of 3 fanned cards peeking at the bottom-left/right. Works because `.b2b-hub-card` is `position:relative; z-index:1` (own stacking context) so `z-index:-1` pseudos sit behind its bg. Mobile (≤900) hides both pseudos (`display:none`) to avoid clutter in the 1-col stack (alongside the existing connector-SVG hide). CSS-only, `tsc` n/a. Pushed `8bac4ed`. Revert: restore the smaller size values + delete the `.b2b-hub-card::before/::after` deck rules + the mobile `display:none` for them.

- 2026-06-01 — **b2b `#revenue` — wrapped the header + accordion in a soft grey rounded panel** (`b2b/page.tsx` ~240, `sections.css` ~5859). User red-boxed the whole 多元化营收渠道 block (section header + the 4-item RevenueAccordion) + `这里的内容，按我红框的大小，增加背景颜色#f2f2f2，四周做圆角设计`. Wrapped the two `<ScrollAnimation>` blocks inside a new `<div className="b2b-revenue-panel">` (inside the existing `.container`). CSS: `#revenue .b2b-revenue-panel { background:#f2f2f2; border-radius:28px; padding:56px 48px }` + mobile (≤760) `padding:36px 20px; border-radius:20px`. Section stays white (the forced `#revenue.section{background:#fff}` rule untouched) so the grey panel reads as an inset card the container width. Accordion keeps its own `max-width:1080px;margin:0 auto` inside the panel. `tsc` 0. Revert: unwrap the `.b2b-revenue-panel` div + delete the CSS rule.

- 2026-06-01 — **b2b `#architecture` flow diagram — REDESIGNED from L→R "flow" into a hub & spoke layout** (`b2b/page.tsx` 141-205 replaced the `.b2b-flow` JSX with `.b2b-hub`; `pages.css` 1463-1668 replaced the whole `.b2b-flow*`/`.b2b-arch*` block with `.b2b-hub*`; added `b2b.overview.coreLabel` to both `messages/{en,zh}.json`). User pasted current screenshot + a reference (center "CORE SYSTEM ADMIN / APP·CPMS·ADMIN" node + dark "自营+运营商模式" pill; top "PLATFORM SIDE" card w/ 2×2 capability grid; left "OPERATORS" + right "USERS" cards each w/ a 3-avatar row + 2×2 tag grid; subtle curved connectors) + `将这里的布局排版设计，改成参考图的这种`. **Mapping (reused ALL existing i18n)**: core eyebrow=new `coreLabel` (Core System Admin/核心系统管理), core title=`overview.center` (APP·CPMS·ADMIN), pill=`overview.model`; platform card title=`overview.platform` + tags=`platformItems` (4→2×2); operators card title=`overview.operator` + 3 avatars `{operator} A/B/N` (Building2 icon) + tags=`operatorItems` (4→2×2); users card title=`overview.user` + 3 avatars `{user} A/B/N` (User icon) + tags=`userItems` (3). **Layout**: `.b2b-hub` = 3-col grid `1fr 1.15fr 1fr` × 2 rows, `align-items:center`, max-width 1180; platform at col2/row1, operators col1/row2, core col2/row2, users col3/row2. **Colors kept BRAND GOLD** (ref was blue — user asked for the LAYOUT, not the palette): cards white + soft shadow + gold-dot title; avatars gold-tint circles `#B07A1E` icon; tags `#FBFAF7` pill + gold square dot; core white + gold border + dark `#2C2519` pill. **Connectors**: a single abs `<svg viewBox="0 0 100 60" preserveAspectRatio="none">` w/ 3 orthogonal `.b2b-hub-link` paths (vertical core→platform `M50 32 V17`, horizontals core→operators `M41 40 H26` + core→users `M59 40 H74`), `z-index:0` behind cards (cards z1, core z2) so any overshoot hides under cards; stroke `rgba(230,162,60,.4)` `vector-effect:non-scaling-stroke`. Lines are orthogonal so `preserveAspectRatio=none` distortion is nil; the y-coords are ESTIMATED (row heights), behind cards so approximate is fine. **Responsive** (≤900): collapse to 1-col `max-width:460`, stack order core→platform→operators→users via `order`, hide the SVG; (≤460) tags go 1-col. Dropped the now-unused `Smartphone`/`ServerIcon`/`ShieldCheck` imports. `tsc` 0 (dev server not up to screenshot). Pushed `75ca36f`. Revert: restore the prior `.b2b-flow` JSX + the old `.b2b-flow*` CSS block (both from `git show 75ca36f^:`), drop the `coreLabel` key + re-add the 3 lucide imports.

- 2026-06-01 — **b2b `#aftersales` "全方位支持" — rebuilt as a full-screen orbital support diagram (replaces the 4-card `grid-2`)** (`b2b/page.tsx` imports + `supportCards` array + section markup; `pages.css` appended `.b2b-support*` block at EOF). User pasted the current section screenshot + a reference design (central gold globe tile, 3 concentric rings, small icon nodes ON the rings, 4 info cards floating around) + `参考我这个设计…重新更改布局排版，内容占满一整屏展示，加一点交互`. NOTE: this is the DOM-rebuilt version of the same design I'd earlier done as a baked PNG (that PNG approach was reverted by the user). Built with real elements so i18n keys still drive the text. **Markup**: section gets class `b2b-support`; `.b2b-support-stage` (relative, max-width 1120, min-height 600) holds: `.b2b-support-rings` (4 absolutely-centred circles — ring--1/2/3 solid faint-gold + ring--dash a dashed circle that slowly rotates via `b2b-support-spin` 38s), `.b2b-support-core` (104px gold-gradient rounded tile w/ lucide `Globe`, pulsing `::before` ring), and 4 `.b2b-support-orbit` groups each = a `.b2b-support-node` (44px icon circle positioned on a ring via `--tl/tr/bl/br` top/left %) + a `.b2b-support-card` (248px white card, icon chip + h3 + p, positioned at a corner). **`supportCards`** maps the 4 i18n items to a `pos` + a lucide `Icon` (item1 视频→Play tl, item3 论坛→MessageSquare tr, item2 知识库→BookOpen bl, item4 团队→Headphones br) — rendered via `<c.Icon/>` member-expression JSX (replaced the old `aftersalesItems`/`aftersalesIcons` SVG arrays). **Interaction**: the `.b2b-support-orbit` wrapper uses inline `display:contents` (no box → its node+card still position against the stage, but `:hover` fires from either child) so `.b2b-support-orbit:hover` lights BOTH the card (lift + gold border) AND its ring node (scale 1.18 + gold fill); cards fade-up staggered (`b2b-support-in`, `--i`); rings rotate + core pulses. **Full-screen**: `.b2b-support{min-height:100vh;display:flex;flex-direction:column;justify-content:center}`. **Responsive**: ≤980 shrinks rings/cards; ≤720 hides rings+nodes, core goes inline, cards become a static 1-col stack (the `display:contents` orbit lets them flow as flex items of the stage). reduced-motion kills spin/pulse/fade. tsc 0; SSR `/zh/b2b` renders `.b2b-support-stage/core/card--tl/node--tr`. **NOT screenshotted** (autodebug needs user-enabled preview) — node/card absolute positions (the `--tl/tr/bl/br` %s) are eyeballed from the reference, so they likely need nudging once seen (cards may overlap rings/each other at some widths). Revert: restore the `grid-2` 4-card block + `aftersalesItems`/`aftersalesIcons`, drop the `.b2b-support*` CSS + the 5 new lucide imports.

- 2026-06-01 — **b2b `#architecture` flow diagram — grey panel bg removed** (`pages.css` `.b2b-flow` 1469). User red-boxed the whole diagram panel + `把这里的灰色背景去掉`. The panel had `background: #F1F2F4` (neutral grey) + `border: 1px solid rgba(0,0,0,.06)` → both removed (`background: transparent; border: none`). Kept `border-radius: 28px` + `padding: 64px 80px 52px` (harmless without bg/border — preserves spacing; the core card/nodes/wires keep their own chrome). Diagram now sits directly on the white section. `tsc` n/a (CSS only). Pushed `fc05dba`. Revert: restore `background: #F1F2F4` + `border: 1px solid rgba(0,0,0,.06)`.

- 2026-06-01 — **pricing `#addons` "可选扩展" icon tiles — removed gold bg, line icons → yellow/black two-tone** (`sections.css` `.addon-card-icon` 4381, `pricing/page.tsx` `addonIcons` 79-116). User red-boxed the 6 addon cards + `图标的黄色去掉，线性图标改成黄黑双色`. The 56px tiles were `background: var(--primary,#febf1d)` (solid gold) + black icon → bg `transparent`, base color `#1a1a1a` (black), svg 26→30px. Made the 6 SVGs two-tone via a new `.addon-card-icon svg .ic-accent { stroke: var(--primary,#febf1d) }` rule + `className="ic-accent"` on one element each: mobileLang=phone-rect black + inner circle/cross gold; adminLang=window-rect black + detail-path gold; gateway=router-rect black + lines gold; pos=terminal-rect black (split: legs black, screen-lines gold); custom=brackets black + slash `M14 4l-4 16` gold; store=storefront black + checkmark `M9 11l3 3 3-3` gold. CSS-var in a presentation attr doesn't resolve so gold comes from the class rule. `tsc` 0. Pushed `74b6959`. Revert: `.addon-card-icon` bg back to `var(--primary)` + color `#181818` + svg 26px, drop the `.ic-accent` rule, remove the `ic-accent` classes + re-merge the split `pos`/`custom`/`store` paths.

- 2026-06-01 — **b2c `#gallery` bottom yellow glow — enlarged** (`polish.css` `.gallery-row::after` ~595). User: `底下模糊的圆形放大一点`. Bumped the blurred radial-gradient ellipse behind the phones: `width 560→820px`, `height 300→420px`, `max-width 80→96%`, `filter blur 56→64px`. Gradient colours/stops unchanged (`rgba(254,191,29,.55→.28→0)`). `tsc` n/a (CSS only). Revert: width 820→560, height 420→300, max-width 96→80%, blur 64→56.

- 2026-06-01 — **b2c `#support` bento — wide top-right card title/subtitle now top-aligns with the tall left card title** (`sections.css` `.support-bento-wide` 5562). User red-boxed "完整技术文档" + subtitle in the wide card; `这里红框的标题和副标题，与左边卡片的要对齐`. The wide card is `flex-direction:row` and had `align-items:center`, which vertically centered the `.support-bento-wide-text` block — so its title sat lower than "分步视频教程" in the tall left card (both cards share `padding:32px`). Fix: `align-items:center`→`flex-start` so the h3/subtitle start at the top, lining up the two titles. The API/SDK `.support-bento-docs` preview also rises to the top alongside the text (reads balanced). Did NOT touch padding, the tall/small/accent cards, or markup. `tsc` n/a (CSS only). Pushed `f2feb11`. Revert: `.support-bento-wide` `align-items` back to `center`.

- 2026-06-01 — **b2c `#gallery` phone screens — black fringe at the rounded corners → removed** (`polish.css` `.g3d-screen` ~686). User pasted the gallery screenshot with all 4 phones' screen corners (top + bottom) yellow-boxed + `手机界面圆角位置，都有黑色线，解决一下`. Same root cause as the documented feature-image fringe: `.g3d-screen` is a `border-radius:13.5%/6.2%` + `overflow:hidden` container holding a square-cornered screenshot `<img>`; its `background:#000` bled a dark fringe through the rounded-corner AA edge, and a heavy `box-shadow: inset 0 0 0 1px rgba(0,0,0,0.9)` drew an explicit ~black ring that read as black lines tracing the curves. **Fix**: bg `#000`→`#fff` (white can't fringe), inset ring `rgba(0,0,0,0.9)`→`rgba(0,0,0,0.05)` (hairline grey for subtle screen/bezel separation against the now-white frame, no longer "black"). Became more visible after the prior turn's white-10% frame swap. Did NOT touch the screenshots, the `.g3d-device` frame, or `.g3d-notch`. `tsc` n/a (CSS only). Revert: `.g3d-screen` bg back to `#000` + box-shadow back to `inset 0 0 0 1px rgba(0,0,0,.9)`. **Env note**: a stray `git revert a1d1cd2` was in progress this turn (not mine — likely a hook); aborted it, preserved the polish.css edit (backed up to `/tmp/polish-backup-gallery.css`).

- 2026-06-01 — **b2b `#architecture` flow diagram — APP/CPMS/ADMIN core badges recolored: dark → yellow bg, black icon + black text** (`pages.css` `.b2b-flow-core-badge span` 1651 + `svg` 1664). User red-boxed the 3 badge pills atop the Platform core card + `这里的黑色改成黄色，图标改成黑色，文字也是黑色`. The `.badge-highlight`-spec pills had `background: #2C2519` (dark warm-brown) + `color: #FFF9E6` (cream text) + svg `color: #E6A23C` (gold icon). Now: bg → `var(--primary, #febf1d)` (brand yellow), text `color: #1a1a1a` (near-black), svg `color: #1a1a1a` (black icon). Kept the 20px radius / 6px-16px padding / 700 weight / 1px letter-spacing. Did NOT touch the node-icon circles, tier pills, or core card. `tsc` n/a (CSS only). Pushed `79d1a74`. Revert: bg back to `#2C2519`, text `#FFF9E6`, svg `#E6A23C`.

- 2026-06-01 — **b2c `#gallery` "产品展示" phone mockups — graphite frame → white-10% frosted glass + blurred yellow glow behind the row** (`polish.css` `.gallery-row`/`.gallery-row::after`/`.g3d-phone`/`.g3d-device`/`.g3d-device::before` ~574-660). User pasted the rendered 4-phone gallery screenshot + `将这里的手机样机，改成白色10%的颜色，底部增加一个模糊的黄色圆形`. (1) **Frame**: `.g3d-device` bg `linear-gradient(157deg,#45454b…#19191c)` (graphite) → `rgba(255,255,255,0.1)` ("白色10%") + `backdrop-filter:blur(10px)` so it reads as frosted glass that the yellow glow shows through; softened the dark inset shadows (`inset 0 -3px 6px rgba(0,0,0,.6)`→`-2px 5px rgba(0,0,0,.1)`, white inset `.16`→`.45`). The metallic rim `.g3d-device::before` (dark→light gradient: was white/black metallic, now all-white `rgba(255,255,255,.95→.15→.6)`) so the bezel edge stays defined on the now-light frame. `.g3d-screen` (app screenshot) + `.g3d-notch` untouched — only the surrounding bezel went translucent-white. (2) **Yellow glow**: `.gallery-row` got `position:relative`; new `.gallery-row::after` = a `560×300px` (max-width 80%) radial-gradient ellipse `rgba(254,191,29,.55→0)` + `filter:blur(56px)`, pinned `bottom:8px; left:50%; translateX(-50%)`, `z-index:0`, `pointer-events:none`. `.g3d-phone` got `z-index:1` so the phones sit ABOVE the glow (positioned elems paint in DOM order, and ::after is last in DOM, so without the explicit z-index the blur would cover the phones). `tsc` n/a (CSS only). Not screenshotted (autodebug needs user preview). Revert: `.g3d-device` bg back to the graphite gradient + original inset shadows + dark metallic rim, delete `.gallery-row::after` + the `position:relative`/`z-index:1` additions. Note: `rgba(255,255,255,.1)` over the light grey section bg is subtle — if user wants a more obvious white frame, bump the alpha (e.g. `.18`).

- 2026-06-01 — **b2b `#aftersales` "Comprehensive Support" — replaced the 4-card grid with a single illustration + white section bg** (`b2b/page.tsx` 418-439 + deleted `aftersalesItems`/`aftersalesIcons`; `pages.css` `.b2b-aftersales-section`/`.b2b-aftersales-illustration`; new `public/images/b2b-aftersales-illustration.png` 2150×1922 RGBA, 285 KB). User: `将这里一整屏，更换成这个插图…这一屏的背景颜色改成白色` + pasted a radial concentric-circle illustration (gold globe centre + 4 floating cards orbiting: 视频部署指南 / 官方社区论坛 / 全方位知识库 / 专业技术团队). Saved the PNG via Pillow (`optimize=True`), swapped the `<div className="grid-2">{4× .card.glass-card}</div>` for a single `<Image src=".../b2b-aftersales-illustration.png" width={1075} height={961} sizes="…760px">` inside `.b2b-aftersales-illustration{max-width:760px;margin:0 auto}`. Section got class `b2b-aftersales-section` with `background:#fff` (was the global golden-orb/frosted page bg showing through the plain `.section`). Removed the now-unused `aftersalesItems` const + `aftersalesIcons` SVG array (noUnusedLocals would flag them). Section header (label/title/desc) still from i18n `b2b.aftersales.{title,desc}` + `b2b.nav.aftersales`. **Caveat**: the illustration has BAKED-IN Chinese text (视频部署指南 etc.) — on the EN locale it still shows Chinese; user supplied this exact asset so left as-is. `tsc` 0; SSR `/zh/b2b` renders both classes. Not screenshotted (autodebug needs user preview). Revert: restore the `grid-2` 4-card block + the `aftersalesItems`/`aftersalesIcons` arrays, drop the `.b2b-aftersales-*` CSS + the PNG.

- 2026-06-01 — **b2b `#architecture` flow diagram — operator/user node icon circles: gold border line removed** (`pages.css` `.b2b-flow-node-ic` 1514 + hover 1530). User red-boxed the left (运营商 A/B/N, `Building2`) + right (用户 A/B/N, `User`) node circles + `把红框的图标边线去掉`. The 74px white circles had `border: 1.5px solid rgba(230,162,60,.35)` (gold rim) → set to `border: none`; dropped the `border-color: #E6A23C` line from the `:hover` rule too (hover keeps the `translateY(-2px) scale(1.06)` lift + gold box-shadow). Left `background: rgba(255,255,255,.85)` + `box-shadow: 0 6px 18px rgba(212,197,165,.22)` + `#B07A1E` icon intact — circles now read as borderless soft-shadow chips. Did NOT touch the core card border, the tier pills, or the SVG connectors. `tsc` n/a (CSS only). Pushed `cb4516c`. Revert: restore `border: 1.5px solid rgba(230,162,60,.35)` + the hover `border-color: #E6A23C`.

- 2026-06-01 — **b2b `#modes` + `#revenue` — section backgrounds forced white** (`sections.css` ~5758, before the `.modes-bento-grid` block). User: `B2B里面的模式和营收板块，两屏的背景改成白色`. `#modes` is `section section-alt` → its grey `.section-alt { background: var(--dark) }` tint was the grey; `#revenue` is a plain `.section` (transparent, showed the page bg). Added a scoped 2-selector rule `#modes.section-alt, #revenue.section { background: #fff }` — ID+class specificity (1,1,0) beats the bare `.section-alt` (0,1,0) so #modes goes white without touching the global `.section-alt` (still grey on #features, #releases, #demo, etc.). #revenue pinned white too so both "screens" match. Did NOT add a dark-theme override (site is fixed-light-leaning; white is intentional). `tsc` 0. Pushed `c7a461d`. Revert: delete the `#modes.section-alt, #revenue.section { background:#fff }` block.

- 2026-06-01 — **b2c `#payments` region payment-method chips — recolored to yellow border + yellow text** (`sections.css` 2637). User pasted the 全球支付矩阵 marquee screenshot + `将这里的支付方式，改成黄色边线，黄色文字`. The `.b2c-pay-card .tag` chips (Stripe/SEPA/Alipay/… inside each region card) were already border-only transparent from a prior turn, but with a faint gold border `rgba(214,158,0,.35)` + AA dark-gold text `#8C5F00`. Bumped both to the solid brand gold: `border: 1px solid var(--primary,#febf1d)` + `color: var(--primary,#febf1d)`. Reads clearly yellow now (note: #febf1d text on white is below AA — accepted per explicit user request). Left the `:hover` no-lift rule + the dark-theme override (already `--primary` gold) untouched. CSS-only, `tsc` n/a. Pushed `a9f681c`. Revert: border/text back to `rgba(214,158,0,.35)` / `var(--primary-text,#8C5F00)`.

- 2026-06-01 — **b2b `#architecture` flow diagram — restyled to "浅黄科技风" (light-yellow tech) per user-supplied CSS spec + switched connectors to SVG bezier w/ gradient stroke** (`pages.css` `.b2b-flow-*`; `b2b/page.tsx` wire `<span>`→`<svg>`). User pasted a CSS snippet (`.chart-container` radial bg, `.core-node` frosted glass, `.badge-highlight` dark pill, `.function-tag` white/warm-border tag, `.connector-line` bezier w/ `url(#line-gradient)`) + the current rendered screenshot; `根据代码，重新生成这里的B2B层级架构图`. Adopted each token: (1) **panel**: `.b2b-flow` got `background: radial-gradient(circle at 50% 42%, #FCF9F2, #F5EFE2)` + `border:1px solid rgba(230,162,60,.14)` + `border-radius:28px` + `padding:48px 56px 40px` — the whole diagram is now an inset warm-cream tech panel (also gives the frosted core something to blur against). (2) **core** `.b2b-flow-core`: 4px-yellow-border → `rgba(255,255,255,.75)` + `backdrop-filter:blur(10px)` + `border:1px solid rgba(230,162,60,.2)` + `radius:24px` + `box-shadow:0 12px 40px rgba(212,197,165,.2)`. (3) **APP/CPMS/ADMIN badges** `.b2b-flow-core-badge span`: gold-soft → dark `#2C2519` bg + `#FFF9E6` text + `radius:20px` + `letter-spacing:1px`, svg tinted `#E6A23C`. (4) **capability tags** (`.b2b-flow-pill` platform pills + `.b2b-flow-chip` side chips): gold-tinted round pills → `.function-tag` = `#fff` + `1px solid #EBE4D3` + `#5C4B37` text + `radius:8px`, hover `border #E6A23C / bg #FFFDF8 / translateY(-2px)`. (5) **tier pills** `.b2b-flow-tier`: white glass + `rgba(230,162,60,.3)` border + `#2C2519` text + gold dot. Node circles → `rgba(255,255,255,.85)` + warm border + soft shadow + `#B07A1E` icon; labels `#5C4B37`; title `#2C2519`; caption `#8A7B66`. (6) **connectors**: replaced the CSS-elbow `<span>` (background-bar + border corner boxes) with an `<svg viewBox="0 0 160 284" preserveAspectRatio="none">` per rail — now feasible WITHOUT distortion because rails are a fixed 160px wide and row1 a fixed 284px tall, so viewBox≈1:1 to the element. Each SVG has its own `<linearGradient>` (`#b2b-wire-l` faint→strong, `#b2b-wire-r` strong→faint, gold `#E6A23C`, `gradientUnits=userSpaceOnUse`) and a single `.connector-line` path: 3 cubic-bezier curves fan from the node edge (x0) into a merge point (x68) + a feeder (x92→160) to the core; the 24px gap (68-92) is hidden under the centred tier pill so it reads as "3 nodes → pill → core". `.connector-line{fill:none;stroke-width:2;stroke-linecap:round;vector-effect:non-scaling-stroke}` keeps stroke crisp across breakpoints. Node curve y-coords 47/142/236 = the space-around block centres (matches the prior working elbow positions). Dark-theme: removed the dark-card flips — the panel is an intentional fixed-light design. Mobile (≤820) still hides the SVG + uses the `.b2b-flow-rail::before` vertical connector (recoloured to `#E6A23C` gradient); padding bumped 8→28px so the panel breathes. tsc 0; SSR `/zh/b2b` renders `connector-line` + `#b2b-wire-l` + the dark badge. NOT screenshotted (autodebug needs user-enabled preview). Revert: restore the gold-soft core/badge/pill/chip rules, the CSS-elbow `.b2b-flow-wire` block, the `<span>` wires, and drop the `.b2b-flow` radial panel bg/border/padding. **Followup (same day, `架构图展示区域再大一点`)**: enlarged the panel — `.b2b-flow` max-width 1180→1320, row1 284→328px, rails 160→172px, core min 320→360, node cols 120→130px, panel padding 48/56→56/72; node circle 60→66 (svg 24→26), core-title 1.75→1.95rem, platform pills 1→1.05rem. Left the SVG `viewBox="0 0 160 284"` UNCHANGED — `preserveAspectRatio="none"` rescales it to the new rail/row size so the bezier endpoints still land on the (proportional) node centres; no path edits needed. **Followup 2** (`再大一点`): max-width 1320→1440 (now fills the `--max-width:1440px` container, ~1392px content — wider would need a full-bleed break-out), row1 328→372, rails 172→188, core min 360→400, node cols 130→142, node circle 66→74 (svg 26→30), title 1.95→2.15rem, pills 1.05→1.1rem, label .92→.98rem. **Followup 3** (`浅黄色背景颜色改成#F1F2F4`): `.b2b-flow` panel bg `radial-gradient(#FCF9F2→#F5EFE2)` → solid `#F1F2F4` neutral grey, border `rgba(230,162,60,.14)`→`rgba(0,0,0,.06)`. Core/badges/tags/wires keep the warm gold palette (only the panel wash went grey).

- 2026-06-01 — **b2b `#modes` card 1 ("统一网络聚合模式") icon — globe → network/hub** (`b2b/page.tsx` 63, `modeIcons[0]` aka `key="net"`). User red-boxed the first card's icon + `红框的图标更换一下`. Swapped the circle-globe-with-meridians SVG for a 3-node hub icon (top rect + connector bracket black `currentColor`; the two bottom rects `className="mode-ic-accent"` = gold) — better matches "网络聚合" (aggregation) than a globe. Same 24×24 viewBox, strokeWidth 2, two-tone scheme as the other 3 icons (bld/link/ref untouched). `tsc` 0. Revert: restore the `<circle cx=12 cy=12 r=10>` + meridian `<path class=mode-ic-accent>` globe.

- 2026-06-01 — **b2b `#modes` "多元化运营模式" bento — overall scaled down a notch** (`sections.css` `.modes-bento-grid` + `.mode-bento-*` ~5762-5829). User pasted the rendered 2×2 modes screenshot + `整体缩小一点`. CSS-only compaction: grid `gap 20→16px` + added `max-width:1080px; margin:0 auto` (so the 2 columns no longer stretch the full `--max-width:1440px` container — the whole bento is now a tighter centred block); card `gap 20→16px`, `padding 28/32→24/28px`, `min-height 320→268px`; icon svg `42→36px`; title `1.2→1.1rem` (`margin-bottom 10→8px`); desc `.92→.88rem` (`line-height 1.65→1.6`). Mobile (≤760) block untouched. Did NOT touch the bg banner images, the two-tone line icons, `.mode-bento-body{max-width:66%}`, or markup. `tsc` n/a (CSS only). Revert: gap→20, drop max-width/margin, padding→28/32, min-height→320, icon svg→42, title→1.2rem/10px, desc→.92rem/1.65.

- 2026-06-01 — **b2b `#modes` bento cards — 4 banner artworks now set as full-card BACKGROUND images** (`b2b/page.tsx` ~230, `sections.css` `.mode-bento-card`, 4 PNGs re-added to `public/images/b2b-modes/`, commit `1bbcfb8`). Saga this turn: (1) inserted the 4 user-pasted pale-gold banners (1372×646) as a top-panel `.mode-bento-visual` replacing the gold icon (`9461143`); (2) user `取消上一步操作` → fully reverted to the gold icon tile; (3) user `重新插图图片，是作为卡片的背景图` → re-added the images, this time as the whole card's `background-image`. **Final markup**: `<article className="mode-bento-card" style={{ backgroundImage: 'url(/images/b2b-modes/mode-${i+1}.png)' }}>` — the gold `.mode-bento-icon` tile + `.mode-bento-body` (h3/p) STAY on top of the bg (not replaced this time). **CSS**: `.mode-bento-card` `background:#fff` → `background-color:#fffaf0` (fallback) + `background-size:cover; background-position:center right; background-repeat:no-repeat` + `overflow:hidden`. Used `center right` so each banner's bottom-right graphic stays in frame when `cover` crops the wider-than-card image (card ≈1.7:1 vs image 2.12:1 → crops left/right; the near-white gradient left side that gets cropped is where the dark title/grey desc sit, so text stays readable). Source PNGs re-saved from the still-present `/tmp/claude-paste-*` (Pillow optimize, ~145-157 KB), paste order → card reading order (mode1←img1 network, mode2←img2 person, mode3←img3 city, mode4←img4 db-stack). `tsc` 0. Not screenshotted (autodebug needs user preview on their IP). Revert: drop the inline `backgroundImage` style + restore `.mode-bento-card { background:#fff }` (remove the cover/position/overflow lines), delete `public/images/b2b-modes/`. **Followup (`布局大概按照这个` + wireframe ref: icon top-left square, then a gap, then a short title bar + 2 long desc bars in the lower-left, graphic on the right)**: repositioned the card content to match. `.mode-bento-card` got `justify-content: space-between` (was just `gap:20px` top-stacked) so the icon pins top-left and the `.mode-bento-body` block drops to the bottom-left; `min-height 300→320`, `padding 28→28px 32px`. `.mode-bento-body` got `max-width: 66%` so the title/desc don't run under the right-side bg graphic (mobile ≤760 relaxes to `78%`). `tsc` 0, `2bb7614`. Revert this followup: drop `justify-content:space-between` + the `.mode-bento-body{max-width}` rules, min-height back to 300. **Followup #2 (`黄色图标底色去掉，线性图标颜色改成黄黑双色`, `76259b2`)**: removed the 64px gold `.mode-bento-icon` tile (dropped `width/height/border-radius/background/box-shadow`) → now a bare 42px line icon sitting directly on the card bg; made the 4 SVGs **two-tone** — base strokes black (`.mode-bento-icon { color:#1a1a1a }` via `currentColor`), one accent element per icon gets `className="mode-ic-accent"` → `.mode-bento-icon svg .mode-ic-accent { stroke: var(--primary,#febf1d) }` (gold). Accent splits: net=globe-circle black + meridians gold; bld=building-rect black + windows/ground gold; link=split the single 2-subpath `d` into two `<path>`s (1st half black, 2nd gold); ref=arc black + arrowhead-polyline gold. CSS-var in a presentation attr doesn't resolve, so the gold is applied via the CSS class rule (NOT inline `stroke="var(...)"`). `tsc` 0. Revert: re-merge the link path, drop the `mode-ic-accent` classes, restore the gold-tile `.mode-bento-icon` CSS (64px/radius/bg/shadow + svg 28px).

- 2026-06-01 — **b2c `#features` "Stunning UI Design" + "Your Brand. Your Rules." feature images — black fringe at the four rounded corners → removed** (`b2c/page.tsx` 187 + 220). User (3rd msg, frustrated `没有解决到`): the two illustrations (`/images/ui-design-b2c-2.png`, `/images/OEM.png`) showed a thin **black/dark band at all four rounded corners**. My first two answers wrongly blamed baked-in crop-marks in the PNG (the gold L-brackets/crosshairs ARE in the artwork, but those sit around the inner card mockups, NOT at the container's outer corners — verified the absolute corners of both PNGs are blank light-gray). **Real root cause = CSS**: `.feature-image-placeholder` (`sections.css` 2661) has `background: linear-gradient(135deg,#1a1a2e,#0f3460)` (dark navy, a legacy loading-placeholder colour) + `overflow:hidden` + `border-radius: var(--radius-lg)` (=20px, `tokens.css` 86). The two `<Image>` ALSO carried their own inline `borderRadius: 20`. Even though child radius == parent radius (both 20px), the child image's rounded-corner **antialiased 1px edge** let the parent's dark navy bg bleed through as a dark fringe at each corner. (The 3rd feature image, smart-ops at 286-308, was already immune — a prior session overrode ITS parent bg to a light `linear-gradient(180deg,#F1F2F4,#FFFFFF)` + `isolation:isolate`; it keeps its own `borderRadius:20` and is fine.) **Fix**: removed the redundant `borderRadius: 20` from BOTH `<Image>` inline styles (now just `width:100%;height:100%;objectFit:cover`). The square image fully covers the parent including its corners; the parent's own `overflow:hidden`+`border-radius:20` clips the whole thing into clean rounded corners, and the dark navy bg is 100% hidden behind the square image → no fringe, regardless of bg colour. Did NOT touch the `.feature-image-placeholder` base rule (shared sitewide; the empty AppSlideshow placeholder at 248 still legitimately uses the dark bg as a backdrop) nor the smart-ops block. `tsc` 0. Revert: re-add `borderRadius: 20` to the two `<Image>` styles (which reinstates the bug). **Lesson**: a "black corner on a rounded image" is almost always a dark parent bg showing through a rounded child's AA edge — check the CONTAINER bg before assuming the asset is at fault; I burned 2 turns insisting it was baked into the PNG.

- 2026-06-01 — **b2b `#architecture` flow diagram — scaled up + gradient connector lines** (`pages.css` `.b2b-flow-*`). User: `内容放大一点展示，线条做成渐变效果，分别从运营商渐变到ABC,用户渐变到ABC`. (1) **Scale up**: `.b2b-flow` max-width 1080→1180, rows `240px→284px`, core min col 300→340, node cols 104→118px, rails 60→66px, gaps up. Node circle 50→60px (+ new `.b2b-flow-node-ic svg{width:24px;height:24px}` since lucide `size={20}` is overridden by CSS), node-label .82→.92rem, tier pill .88→.98rem (+pad), core padding 32/30→40/38, core-title 1.5→1.75rem, pills .9→1rem (+pad), badge .74→.82, caption .74→.82, chips .78→.86. Tablet breakpoint (821-1100) bumped to match (rows 248, ic 50px, etc). Mobile (≤820) untouched. (2) **Gradient lines**: replaced the solid `--wire` with `--wire-from rgba(214,158,0,.7)` (strong, tier/core side) + `--wire-to rgba(214,158,0,.12)` (faint, node side). The feeder is now drawn via `background-image: linear-gradient(to right, …); background-size:100% var(--wire-w); background-position:0 50%; background-repeat:no-repeat` (a 3px horizontal bar that is itself a left→right colour gradient — replaces the old vertical-gradient band trick). Direction is per-rail so it always fades TOWARD the A/B/N nodes: left rail `to right, --wire-to, --wire-from` (faint at nodes-left, strong at core-right); right rail `to right, --wire-from, --wire-to` (strong at core-left, faint at nodes-right). The branch corner boxes (::before/::after) now use `--wire-to` (faint node-end) for their borders so they blend into the faded end. `--wire-w` 2→3px, `--wire-r` 12→14px. Note: border-image can't do a gradient + keep border-radius, so the rounded branches stay a flat faint tone while the straight feeder carries the visible gradient — reads correctly as "运营商/用户 → ABC". tsc 0; SSR `/zh/b2b` renders. Not screenshotted (autodebug needs user-enabled preview). Revert: restore the solid `--wire` background band, the smaller size values, and branch borders back to `var(--wire)`. **Followup (same day)**: user screenshot red-boxed the left rail — `这里的线条被挡住了` (lines invisible/blocked). Root cause = the faint gradient end `--wire-to` was `rgba(214,158,0,0.12)`, basically transparent on the near-white section, so the branch elbows toward the A/B/N nodes (which use `--wire-to` for their border colour) vanished. NOT an actual z-index/overlap block — just too faint. Fix: `--wire-to 0.12→0.4` (still-visible light gold) and `--wire-from 0.7→0.85`; gradient range now 0.85→0.4 so the whole line stays visible while still reading as a fade toward the nodes. **Followup #2 (same day, user re-sent the EXACT same screenshot + same `这里的线条被挡住了`)**: "被挡住" literally = COVERED, not faint — re-read it correctly the 2nd time. Real root cause: the white tier pill (`.b2b-flow-tier`, `position:absolute; left:50%; translateX(-50%)`, z-index 2, opaque white bg) is WIDER than the narrow ~66px rail (EN "Operator" pill ≈ 100px), so it overflowed and sat ON TOP of the branch-merge elbows, hiding them. Asymmetry the screenshot showed (left lines gone, right lines visible) = "Operator" (long) made a wider pill than "User" (short). Fix: (a) rails widened + made FIXED `160px` (was `minmax(66px,.75fr)`) so the centred pill (~95px → spans x≈33-127px) can't reach the merge bus; (b) merge bus pulled in to the node side — branch `::before/::after` `width:18%→12%` (bus at 12%×160≈19px, well left of the pill's 33px left edge); (c) node groups anchored toward their rails (`.b2b-flow-nodes--op{align-items:flex-end}`, `--user{align-items:flex-start}`) so circles sit next to where the lines start (else the now-wider node columns left a big circle↔line gap) — mobile overrides this back to `align-items:center` for the wrapped row; (d) `--wire-to 0.4→0.5`, `--wire-from 0.85→0.9`; (e) tier pill trimmed `padding 9/17→8/15`, font `.98→.95rem`. Tablet rails also fixed (`132px`). tsc 0. STILL not screenshotted (autodebug needs the user to enable preview on their IP — could not self-verify either fix visually). **If it's STILL wrong after this**: the next suspect is the user's browser serving a stale Turbopack CSS bundle (documented: content-addressed bundles persist across dev restarts) — have them hard-refresh (Ctrl+Shift+R); if still wrong, the dev file-watcher likely died (inotify) → kill `next dev`, `rm -rf .next`, restart. Revert this followup: rails back to `minmax(66px,.75fr)`, branch width 12%→18%, drop the node `align-items` anchors.

- 2026-06-01 — **b2c `#payments` "全球支付矩阵" region marquee cards — pale-gold glass fill → border-only** (`sections.css` 2621-2635). User screenshot red-boxed the two scrolling rows of region cards (非洲/南美洲/北美洲/亚洲/欧洲 etc., each holding payment-method `.tag` chips); `这里红框浅黄色背景，改成只有边线`. The cards are `.card.b2c-pay-card` (markup in `b2c/page.tsx` 358-382 — `PAY_REGIONS` mapped into a 2-row `.b2c-pay-marquee` of `.b2c-pay-track`s, each card = `<h4>` region name + flex-wrap of `.tag` method chips). The pale-yellow fill came from the shared `.card` glass rule at `sections.css` 2391-2402 (`background: var(--glass-bg)` + `backdrop-filter: blur(20px) saturate(1.2)` + `box-shadow`). **Fix**: appended to the existing `.b2c-pay-card` sizing rule (which previously only set `flex/width/margin`): `background: transparent; backdrop-filter: none; -webkit-backdrop-filter: none; box-shadow: none;` + a new `.b2c-pay-card:hover { background: transparent; box-shadow: none }` to keep it flat on hover. **Cascade**: `.b2c-pay-card` (0,1,0) ties `.card` (0,1,0) on specificity but wins by later source order (2621 > 2391), so the transparent bg overrides the glass fill. The card's **border** is still the `.card` `1px solid var(--glass-border)` from 2397 — left intact (that IS the "只有边线" line the user wants kept). The `.card::after` 1px white top-edge highlight (2416) is harmless on transparent and left alone. Did NOT touch the `.card` base rule (used sitewide), the `.tag` chips, or the marquee animation. CSS-only, no tsc impact. Not screenshotted (autodebug needs user preview on their IP). Revert: delete the 4 appended props + the `:hover` block from `.b2c-pay-card`, restoring the inherited `.card` glass fill. **Followup (2026-06-01, `85ab8ec`)**: user re-flagged the same area (`这里每个国家的支付方式样式展示，浅黄色背景改成只有边线的效果`) — the card was already transparent, so the remaining pale-gold was the payment-method `.tag` chips (M-PESA/PayPal/Stripe/Alipay/…), which globally use `background: var(--primary-dim)` + `color: var(--primary)` (`pages.css` 104). Added a **scoped** `.b2c-pay-card .tag { background: transparent; border: 1px solid rgba(214,158,0,.35); color: var(--primary-text,#8C5F00) }` (AA gold text, NOT #FEBF1D) right after the `.b2c-pay-card:hover` block (~`sections.css` 2635), plus `.b2c-pay-card .tag:hover { box-shadow:none; transform:none }` (kills the global `.tag:hover` lift from `polish.css` 513 inside the sliding marquee) and a dark-theme override (border `rgba(254,191,29,.4)` + text `--primary`). Scoped to `.b2c-pay-card .tag` so the global `.tag` chips elsewhere keep their fill. Revert: delete those 3 blocks.

- 2026-06-01 — **b2b `#revenue` "多元化营收渠道，成就极致盈利能力" — 4-card `grid-4` glass grid → yellow-theme accordion, restyled to match a 2nd ref (solid-gold open card / plain collapsed rows / +- toggle)** (`b2b/page.tsx`: import + replaced the `grid-4` map block with `<ScrollAnimation><RevenueAccordion /></ScrollAnimation>`, deleted unused `revenueIcons`/`revenues`; new client component `b2b/RevenueAccordion.tsx`; `sections.css` `.revenue-acc*` block ~5805-5960 after the modes-bento `@media`). **Two turns**: (1) built the accordion from a blue-FAQ ref (pale-gold collapsed + faint-gold open tint + chevron). (2) user sent a 2nd ref (`卡片颜色和图标样式参考这个`): open item = **solid gold card** `linear-gradient(135deg,#fdcd54,#f9bb31)` + `box-shadow 0 18px 44px rgba(214,158,0,.28)`, 24px radius, **dark** title/icon/desc/toggle (`#1a1505`, desc `rgba(26,21,5,.72)`); collapsed items = **transparent, no border** with **gold** icons (`var(--primary)`) + dark title + grey toggle, gold-tint bg only on hover. **Toggle is +/-** not chevron: `.revenue-acc-toggle` span draws 2 `currentColor` bars via `::before`(horiz)+`::after`(vert); `.is-open` zeroes the vert bar opacity → minus. **Component** (`'use client'`, `useState(0)`→item1 open; `useTranslations()`): maps `item1..item4`, each `.revenue-acc-item`(+`is-open`) > `<button.revenue-acc-head aria-expanded>`(gold 22px icon + title + `.revenue-acc-toggle`) + `.revenue-acc-body>.revenue-acc-body-inner>p`. Click `setOpen(isOpen?-1:i)`. Kept 4 SVG icons (coin/bank/zap/megaphone) inline. **Expand** = CSS grid `0fr→1fr` (no JS); inner `overflow:hidden;min-height:0`. `max-width:1080px`, gap 10px, head pad 24/32px, body p left-pad 72px (aligns under title). `:focus-visible` gold ring (dark on open card). Dark-theme override only flips collapsed title→white + hover tint; gold open card stays gold. Mobile (≤760px) shrinks pad + body left-pad→50px. reduced-motion zeroes transitions incl toggle pseudo-els. Did NOT touch i18n (`b2b.revenue.item{1..4}.{title,desc}`) or section header. `tsc` 0. Not screenshotted (autodebug needs user preview on their IP). Revert: restore `grid-4` map + `revenueIcons`/`revenues` in `b2b/page.tsx`, delete `RevenueAccordion.tsx` + the `.revenue-acc*` CSS.

- 2026-06-01 — **b2b `#architecture` flow diagram — polish pass: shadows off, core gets a thick 10% light-yellow border, connector lines get rounded corners** (`pages.css` `.b2b-flow-*` block; `b2b/page.tsx` swapped the two `<svg className="b2b-flow-wire">` for `<span className="b2b-flow-wire" />`). User: `所以投影效果去掉，中间卡片增加粗边线10%的浅黄色，线条改成圆角`. (1) **Shadows removed**: `box-shadow:none` on `.b2b-flow-node-ic` (rest+hover), `.b2b-flow-tier`, `.b2b-flow-core` (rest+hover), `.b2b-flow-pill:hover`; hover feedback now = transform + border/bg only; trimmed `box-shadow` from those `transition` lists. Kept the tiny `.b2b-flow-tier::before` dot ring (halo, not a drop shadow). (2) **Core border**: `.b2b-flow-core` `1px solid var(--border-subtle)` + inset ring → `4px solid rgba(254,191,29,0.1)` (literal "10% 浅黄色" = alpha .1 → soft cream frame on white; bump alpha not width if user wants it bolder). Hover → border-color `rgba(254,191,29,0.22)`. (3) **Rounded-corner wires**: the stretched `preserveAspectRatio="none"` SVG can't do clean rounded elbows (curves distort under non-uniform scale), so rebuilt as **pure CSS**: `.b2b-flow-wire` (`<span>`, inset:0) paints the full-width horizontal feeder via `linear-gradient(to bottom, transparent, --wire [2px @50%], transparent)`; `::before`/`::after` = two branch corner boxes (`width:18%;height:33.3%`) with `border-top/right` (left rail) or `border-top/left` (right rail) + `border-*-radius:12px` for the rounded elbow (px radius ⇒ no aspect distortion). Vars: `--wire rgba(214,158,0,.42)`, `--wire-w 2px`, `--wire-r 12px`; bus at 18% (near nodes) so branches merge then single feeder to core with the tier pill on it. At the narrow rail width (~65-90px) the 18% box ≈ the radius so the elbow renders as a smooth near-quarter-curve (intended, reference-like). Mobile `.b2b-flow-wire{display:none}` + `.b2b-flow-rail::before` vertical connector unchanged. tsc 0; SSR `/zh/b2b` renders the span. Not screenshotted (autodebug needs user-enabled preview). Revert: restore the `box-shadow` values, core border to `1px solid var(--border-subtle)` + inset, and swap the `<span>` wires back to the SVG paths.

- 2026-06-01 — **b2b `#architecture` "OCPP CPMS 系统平台版 – B2B" — rebuilt the tiered "shelf" diagram as a horizontal left→right FLOW diagram** (`b2b/page.tsx` 145-216 replaced the `.b2b-arch-diagram` JSX; `pages.css` 1463-1827 replaced the whole `.b2b-arch-*` block with new `.b2b-flow-*` styles). User refs: screenshot 1 red-boxed the existing 4-band vertical stack (平台方 / 运营商 / APP·CPMS·ADMIN center / 用户); screenshot 2 was a Quiety-style flow graphic (3 icon nodes on the left elbow-merge into a labeled "Collab" pill on the connecting line → a big rounded center card holding hashtag pills → "Connect" pill → branches out to 3 avatar nodes on the right). Asked `改成这种风格的层级图`. **New mapping** (Operators → Platform core → Users): left = 3 operator nodes (运营商 A/B/N, `Building2` icon); left rail = elbow SVG wire + "运营商" tier pill sitting on the line; center = platform core card (APP·CPMS·ADMIN badge row + "平台方" title + 4 `platformItems` gold pills + `model` caption); right rail = mirrored wire + "用户" tier pill; right = 3 user nodes (用户 A/B/N, `User` icon). **No content lost**: `operatorItems` (4) + `userItems` (3) render as muted secondary chips in a 2nd grid row under each side group; all three tier labels still present (运营商/平台方/用户 ); A/B/N instances kept as the side nodes. **Layout** = CSS grid, 5 cols `minmax(104px,.85fr) minmax(60px,.7fr) minmax(300px,1.9fr) minmax(60px,.7fr) minmax(104px,.85fr)`, 2 rows `240px auto`; core spans `grid-row:1/span 2` so it's the tall hero; rails + node cols live in row1, chips in row2 (`grid-column:1/3` op, `4/6` user). **Wire alignment trick**: node columns use `justify-content:space-around` inside the fixed 240px row1 track → the 3 node centers land at exactly 16.7% / 50% / 83.3%. The rail `<svg viewBox="0 0 100 100" preserveAspectRatio="none">` stretches to the same 240px (align-items:stretch fills the row track) so its path coords `M0 16.7 H14 … M14 16.7 V83.3 M14 50 H100` (left) / mirrored at x=86 (right) line up with the node centers. `vectorEffect="non-scaling-stroke"` keeps stroke width uniform despite the non-uniform x-stretch; elbow paths are H/V-only so the stretch never distorts a diagonal. Wire color `rgba(214,158,0,.42)`. **A11y/contrast**: gold pill / chip / badge text uses `var(--primary-text,#8C5F00)` (the site's AA token) NOT `--primary-dark` (#E6A817, too light on the gold-soft bg). Nodes/tier pills/core are `#fff` + gold border + soft shadow on the white section. **Responsive**: tablet (821-1100px) shrinks tracks + fonts; mobile (≤820px) flips `.b2b-flow` to `display:flex;flex-direction:column` and uses `order:1..7` to stack operators→opChips→leftRail→core→rightRail→userChips→users, hides the SVG wires (`.b2b-flow-wire{display:none}`) and draws a 2px vertical gold connector via `.b2b-flow-rail::before`, tier pills go `position:relative`; node columns become horizontal wrap rows. Dark-theme overrides flip node/tier/core bg to `--dark-card`. Reduced-motion zeroes the fade-up entrance anims (`b2b-flow-fade`, staggered by `--i`). **Verify gap**: tsc 0 + SSR fetch of `/zh/b2b` shows all `.b2b-flow-*` classes rendering — but could NOT screenshot (autodebug needs the user to enable preview on their IP); visual alignment of the wires to node centers is reasoned, not eyeballed. If the wire endpoints look off, the lever is the `240px` row1 height vs node intrinsic height (~72px×3=216) — space-around math assumes equal thirds, so keep node total < 240. Revert: restore the prior `.b2b-arch-diagram` JSX + the `.b2b-arch-*` CSS block (both from `git show HEAD:` for the two files). Did NOT touch i18n (reused all existing `b2b.overview.*` keys unchanged) or any other b2b section.

- 2026-05-30 — **b2c `#dashboard` Smart-Ops image — removed 6 decorative L-bracket / crop-mark elements baked into the PNG** (`public/images/smart-ops-dashboard-4.png`, 1332×1000 RGBA, 188 KB → 187 KB). User screenshot red-boxed FOUR corners of the dashboard preview + four corners of the `#gallery` row; asked why the rounded corners had a black border line. Investigation showed the marks were NOT a CSS rendering issue — the `feature-image-placeholder` in `b2c/page.tsx` 286-307 is a flat `borderRadius:20, overflow:hidden, boxShadow:none` div with no border, the L-marks are part of the artwork itself (white crop-mark brackets at each corner of the floating "Order Total" yellow card, plus a yellow L-frame at the dashboard's top-left, plus a white "+" cross structure flanking the "Business" button). User picked option 1 of three offered (redo PNG / CSS overlay / crop). **Method**: Python + Pillow + numpy. For each of 6 regions, built a pixel-level mask (`is_strict_white >=252` OR `is_yellow` rgb(254,191,29)±) and iteratively replaced each masked pixel with the mean of its 4-neighbour NON-masked pixels until no masked pixels remain (~50 passes for the small bbox cases). For the dash_TL yellow L bracket the arms were too long for the iterative approach to look natural (4 px × 665 px vertical line at x=198-201, y=168-833 + 4 px × 184 px horizontal line at y=190-193, x=178-361 + a square outline at the corner) — handled each arm by a direct cross-axis copy (`arr[y, 198:202] = arr[y, 188]` for the vertical arm, sampling gray page-bg 5px to the left; `arr[190:194, x] = arr[183, x]` for the horizontal arm). The bracket vertical arm also continues BELOW the Business button at x=198-201, y=920-999 — handled by the same direct-copy approach. **Critical pitfall** (cost 2 wasted passes): first iteration used `is_white(>=240)` which classified the dashboard's near-white cream panel bg (RGB 255,253,248) as bracket pixels; the iterative inpaint then sampled the adjacent yellow bracket arm and spread yellow into the cream panel, turning the area around the Business button bright yellow. Fix: tightened the strict-white classifier to `>=252` (cream still has G=253, B=248 — strict white catches only `255,255,255` true-bracket pixels). Lesson: when inpainting near a sharp color boundary, your classifier must be narrow enough to distinguish bracket-color from any near-bg color that the algorithm could mistakenly absorb. **Bbox coords**: card brackets at (342,40)-(410,90) TL, (920,40)-(990,90) TR, (340,230)-(410,310) BL, (920,230)-(995,310) BR; dash_TL square (170,165)-(215,200); business cross handled in three tight rects (60,800)-(200,840) top horizontal, (175,860)-(220,920) verticals, (60,920)-(200,960) bottom horizontal. **Verification**: rendered at displayed 666×500 size — all 6 elements gone, no visible patching seams, image reads naturally. Did NOT touch `b2c/page.tsx` markup or any CSS — the JSX `feature-image-placeholder` chrome is correct as-is. Did NOT regenerate or replace other dashboard mockup PNGs (the file path `smart-ops-dashboard-4.png` is the v4 used by the `Smart Operations Center` row only). Production file size dropped marginally (188475 → 186873 bytes) because the inpaint smoothed out high-frequency detail that compresses better. **Revert path**: `git checkout HEAD~1 -- website/frontend/public/images/smart-ops-dashboard-4.png` once committed; pre-commit backup also saved at `/tmp/_smart_ops_backup_original.png` (ephemeral, /tmp). **Followup** (not done this turn): user mentioned the `#gallery` section below the dashboard had similar corner marks in their original screenshot — those are likely also baked into a different PNG (the `Gallery3D` row uses CSS-rendered phone mockups, NOT a PNG, so the marks user saw there must be in some other artwork; if user surfaces it again, scan the gallery section for image assets and apply the same inpaint flow).

- 2026-05-30 — **b2c `#features` cell hover: dropped the `box-shadow` lift** (`sections.css` 409-414). User: `交互时，卡片无投影`. The 6 cards in "现代化 EV 充电的强大功能" had `.b2c-feature-cell:hover { transform: translateY(-4px); box-shadow: var(--shadow-md) }` — at rest no shadow (border only), on hover a soft drop shadow appeared along with the lift, matching the rest of the site's card pattern. Now the shadow is gone (`box-shadow: none`) and the `translateY(-4px)` lift is the only interaction signal. The icon-tile gold-flip (`.b2c-feature-cell:hover .feature-card-icon { background: var(--primary); border-color: transparent; box-shadow: 0 0 20px rgba(254,191,29,.35) }` at line ~460, set by the prior `a8ad8f6` commit) is unchanged — that small gold glow on the 52px icon tile is intentional (the user's earlier "黄色 only on hover" instruction) and is scoped to the inner icon, not the card itself. Updated the comment block above the hover rule (was `No shadow at rest (border only); shadow appears on hover, matching the other cards across the site.` — that was the old behavior and now wrong) → new comment cites the user request. Did NOT touch the rest-state rule (`background:var(--dark-card); border:1px solid var(--border-subtle); border-radius:var(--radius-lg); box-shadow:none`) or the `transition: transform .25s ease, box-shadow .25s ease` (the transition still exists so future shadow tweaks would animate — cheap to leave). Did NOT touch the universal `.card:hover, .glass-card:hover, .feature-card:hover { box-shadow: ... }` rule in `polish.css` 141-150 — that selector doesn't match `.b2c-feature-cell` (different class name), so no spillover. `tsc` 0. Revert: change `box-shadow: none` back to `box-shadow: var(--shadow-md)` and restore the original comment. Prior session's `#gallery` featured-2nd-phone-collapses-on-row-hover trick + `#features` white-tile-icon-flip (`a8ad8f6`) still in place — untouched this turn.

- 2026-05-30 — **b2c `#gallery` — featured 2nd phone collapses back to row size when ANY phone in the row is hovered** (`polish.css` 612-629). User: `这里的手机界面做交互时，原来放大的缩小成跟其他一样小，只有鼠标悬停的才会放大`. The static-state featured rule `.g3d-phone:nth-child(2) { width: 310px; transform: translateY(-20px); z-index:1; filter: drop-shadow(0 28px 36px rgba(0,0,0,.26)) }` made the 2nd phone hero-sized at rest. With the hover rule `.g3d-phone:hover, :focus-visible { transform: scale(1.35) translateY(-24px) }`, hovering on phones 1/3/4 enlarged them BUT phone 2 also stayed large from the featured rule — two phones large at once, busy. **Fix**: inserted a new block between the featured rule and the individual hover rule: `.gallery-row:hover .g3d-phone:nth-child(2):not(:hover), .gallery-row:focus-within .g3d-phone:nth-child(2):not(:focus-visible) { width:250px; transform:none; z-index:auto; filter:drop-shadow(0 18px 24px rgba(0,0,0,.18)) }`. **How the cascade resolves**: (1) at rest no phone is hovered → only `.g3d-phone:nth-child(2)` matches → 2nd phone featured at 310px; (2) hover phone 1/3/4 → `.gallery-row:hover` matches AND `.g3d-phone:nth-child(2):not(:hover)` matches (2nd phone isn't hovered) → 2nd phone shrinks to 250px/no-transform, hovered phone gets `scale(1.35)`; (3) hover phone 2 → `.gallery-row:hover` matches BUT `:not(:hover)` excludes the 2nd phone (it IS hovered) → row-reset rule doesn't apply → `.g3d-phone:hover` rule applies on the 2nd phone, giving it `scale(1.35) translateY(-24px)` instead of the static featured look. **Specificity math** (for reader sanity): featured rule (0,1,1) < row-reset (0,4,0) < individual `:hover` (0,2,0). Wait — the row-reset has higher specificity than individual hover, but they never compete because of the `:not(:hover)` filter — when individual hover wins, the row-reset selector simply doesn't match. Without `:not(:hover)`, the higher-specificity row-reset would override the individual hover and the hovered 2nd phone would stay small (bug). The `:not()` is load-bearing. Also added `:focus-within`/`:focus-visible` variant for keyboard nav. Source-order remains: featured-2nd → row-reset → individual-hover so the css reads top-to-bottom in the order it fires. Mobile breakpoint (`max-width:760px`) was already a flat grid reset (`.g3d-phone:nth-child(2){width:auto;transform:none}`) — touch devices don't really "hover" so this turn's rule is dormant there. `prefers-reduced-motion` already zeros the transition; the static→hover swap still happens but instantly. `tsc` 0. Revert: delete the new `.gallery-row:hover .g3d-phone:nth-child(2):not(:hover), .gallery-row:focus-within .g3d-phone:nth-child(2):not(:focus-visible)` block entirely (the featured rule + individual-hover rule sit on either side, untouched). Prior session's `#features` white-icon-tile + hover-flip-to-gold (`a8ad8f6`), b2b `#demo` equal heights, and the `#support` bento-grid (`3159866`) all still in place — untouched this turn.

- 2026-05-30 — **b2c `#features` "现代化 EV 充电的强大功能" icon tiles: yellow → white-bordered static, yellow only on hover** (`sections.css` 439-466). User screenshot of the 6-card features grid: `黄色改成白底带边线，无投影，只有鼠标做悬停时，才是现在那样黄色`. The 52px rounded square tile next to each card title was previously `background: var(--primary, #febf1d); border: 0; box-shadow: 0 0 20px rgba(254,191,29,.35)` in ALL states. **Now**: static state is `background: #fff; border: 1px solid rgba(0,0,0,.12); box-shadow: none`. Added a hover/focus rule `.b2c-feature-cell:hover .feature-card-icon, .b2c-feature-cell:focus-within .feature-card-icon { background: var(--primary,#febf1d); border-color: transparent; box-shadow: 0 0 20px rgba(254,191,29,.35) }` so hovering anywhere on the cell (or focusing into it via keyboard) lights the tile back to the prior gold look. Added `transition: background 200ms ease, border-color 200ms ease, box-shadow 200ms ease` for a smooth swap. Icons are PNGs (`/images/b2c-feat-1.png` through `b2c-feat-6.png`, rendered via `next/image` 26×26) — they're black line drawings with transparent bg, so they read fine on both white (static) and yellow (hover); no asset swap needed. The SVG `stroke` override rule at lines 463-469 (`.b2c-feature-cell .feature-card-icon svg path/circle/rect/line { stroke: #181818 }`) is now dead (these icons are PNG not SVG), but left in place — harmless and would re-activate if anyone swaps to inline SVG later. The `[data-theme="dark"] .b2c-feature-cell .feature-card-icon { background: var(--primary); border: 0 }` rule at line 479 left intact — dark-mode users keep the yellow-always look (the dark-mode toggle was removed from the popover earlier this session, so this is mostly dead code; out of scope to also flip dark-mode). `tsc` 0. Revert: restore the prior single-state block (`border: 0; background: var(--primary,#febf1d); box-shadow: 0 0 20px rgba(254,191,29,.35)`) and delete the `:hover, :focus-within` rule and the `transition` line.

- 2026-05-30 — **b2b `#demo` two columns now equal height** (`pages.css` 2570-2577). Followup to the QR-card wrap: user `两个卡片高度要一致`. Root cause: `.b2b-demo-grid` had `align-items: start` (set when the right side was a bare flex row that didn't want to stretch). With both columns now being white cards, the cred card on the left had less content than the QR+CTA card on the right, so it rendered shorter. Fix: changed `align-items: start` → `align-items: stretch` (which is also the grid default but spelled out for intent) AND added `.b2b-demo-grid > div { height: 100% }` so the `ScrollAnimation` wrappers between the grid and the inner cards propagate the cell height down. Both inner cards (`.b2c-demo-card` line 532 + `.b2b-demo-qr-card` line 2657) already had `height: 100%`, so once the wrapper stretches, the cards fill. The cred card now matches the QR card's height — the `.b2c-demo-card-actions` containing the inline 启动 button uses `display: flex` (inherited from the card's `flex-direction: column` + the actions wrapper) so the button stays at its natural size and the gap between creds and button expands. `tsc` 0. Revert: change `align-items: stretch` back to `start` and drop the `.b2b-demo-grid > div { height: 100% }` rule.

- 2026-05-30 — **b2b `#demo` "系统演示" QR side: bare flex row → white card w/ CTA + copy buttons on creds, matching B2C** (`b2b/page.tsx` 5/9/455-499, new `b2b/B2BDemoCredCard.tsx`, `pages.css` 2657-2701, `messages/{en,zh}.json` `b2b.demo.{desc,launchOnline,copy,copied}`). User screenshot red-boxed the existing layout (creds card on left w/ white card chrome + 启动 button at the bottom; right side: two bare QRs floating on the page bg with no card around them, no description text, no launch button below). Asked for 3 things: (a) 右边二维码加白色卡片背景, (b) 加 `亲身体验 GCSS。选择版本探索在线系统。` 文本 + `启动在线演示` 按钮, (c) 用户名密码右边加复制按钮 — and (d) overall match B2C. **i18n**: appended 4 keys to `b2b.demo` in both locale files: `desc`/`launchOnline`/`copy`/`copied`. Kept the existing `launch` ("启动" / "Launch") — that's the inline button INSIDE the creds card, separate from the new `launchOnline` ("启动在线演示" / "Launch live demo") under the QRs. **New client component** `B2BDemoCredCard.tsx` (mirrors `b2c/DemoTabs.tsx` shape exactly): inner `CredRow` component uses `useState<boolean>` + `navigator.clipboard.writeText(value)` with a 1600ms `setCopied(false)` timer for the "已复制 ✓" → "复制 ⧉" toggle. Reuses `.b2c-demo-cred-row` / `.b2c-demo-cred-key` / `.b2c-demo-cred-value` / `.b2c-demo-cred-copy` classes which already exist in `sections.css` 625-676 (no new CSS for the copy button itself). Inline launch button is now an `<a href="https://app.gcss.hk/admin" target="_blank" rel="noopener noreferrer" class="btn btn-primary">` (was a dead `<button type="button">` with no onClick — user request to "match B2C" implicitly fixes the dead button by linking it). Component takes `account` / `password` props so the credentials still live in the server component (page.tsx `DEMO_CREDS.admin`) and stay out of the client bundle. **Page restructure** (`b2b/page.tsx` 455-499): replaced the inline 32-line creds-card markup with `<B2BDemoCredCard account={...} password={...} />`. Wrapped the previously-bare `.b2b-demo-qrs` flex row in a new `.b2b-demo-qr-card` white container that also holds a new `.b2b-demo-qr-cta` block (border-top divider + centered `<p>{t('b2b.demo.desc')}</p>` + gold `<a class="btn btn-primary" href={DEMO_QR_URLS.web}>启动在线演示</a>`). The CTA link points at `DEMO_QR_URLS.web` ('https://app.gcss.hk/admin') matching the admin focus of the demo. **CSS** (`pages.css` 2657-2701, inserted right after the existing `.b2b-demo-qrs` rule): `.b2b-demo-qr-card{background:#fff;border:1px solid var(--border-subtle);border-radius:var(--radius-lg);padding:32px;display:flex;flex-direction:column;gap:24px;height:100%}` mirrors `.b2c-demo-qr-row` chrome exactly (same bg/border/radius/padding tokens). `[data-theme="dark"]` override flips bg/border to the same dark-mode tokens B2C uses. Scoped reset `.b2b-demo-qr-card .b2b-demo-qrs{padding-top:0}` drops the legacy 12px buffer (the card padding handles spacing now). `.b2b-demo-qr-cta{display:flex;flex-direction:column;align-items:center;gap:16px;padding-top:20px;border-top:1px solid var(--border-subtle);text-align:center}` + `p{max-width:360px;font-size:.95rem;line-height:1.6;color:var(--text-secondary)}` puts the desc on its own line above the gold pill button. The 2-col `.b2b-demo-grid` (1fr 1fr) untouched — equal column widths still hold because both sides now have card chrome at matching padding. Did NOT touch the existing creds card CSS (`.b2c-demo-card*` rules in `sections.css`) — reusing those classes was the whole point. Did NOT add new `.b2b-demo-qr-card` styles for the existing `.b2b-demo-qr-block` / `.b2b-demo-qr` rules (those target a different markup pattern that isn't used here — we're using `.b2c-demo-qr` from B2C instead, which was already the case before this turn). `tsc` 0. Revert: in `b2b/page.tsx` swap the `<B2BDemoCredCard>` back to the original inline 32-line creds-card markup AND unwrap the QR `.b2b-demo-qr-card` (delete the wrapping div + the `.b2b-demo-qr-cta` block, leaving just the bare `.b2b-demo-qrs` flex), then drop `B2BDemoCredCard.tsx` + the 4 i18n keys + the `.b2b-demo-qr-card`/`.b2b-demo-qr-cta` CSS block at `pages.css` 2657-2701.

- 2026-05-30 — **b2b `#modes` "多元化运营模式" — 4 equal `.grid-4` glass cards → 2x2 bento layout (warm-grey outer tile + white inner visual panel + gold icon)** (`b2b/page.tsx` 237-251, `sections.css` 5720-5810). User refs: screenshot 1 was the current vertical-ish stack (`grid-4` collapsing to `repeat(2, minmax(0,1fr))` at the 1024px breakpoint, so on desktop it was actually 4 narrow columns but the screenshot was captured below 1024); screenshot 2 was a bento reference (top row: 2 wide cards each with a UI-mockup preview on top + title/desc below; bottom row: 3 small cards same shape). Said `改成这种`. **Markup**: replaced `<div className="grid-4">` + 4× `<div className="card glass-card" style={{ height:'100%' }}>` (each w/ `.card-icon` + h3 + p inline) with `<div className="modes-bento-grid">` containing 4 `<article className="mode-bento-card">` — each split into `.mode-bento-visual` (white inner panel, 16px radius, `min-height:160px`, centers the icon) + `.mode-bento-body` (title + desc, left-aligned). Icon tile inside `.mode-bento-visual` is `.mode-bento-icon` — 64x64 solid-gold (`var(--primary)`) with `border-radius:16px` + `box-shadow:0 8px 20px rgba(254,191,29,.28)` for the elevation visible in the reference. **CSS**: `.modes-bento-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px}` — 2x2 layout. Cards use `background:rgba(243,240,233,.55)` (warm-grey cream tint matching the yellow site theme — mirrors how the reference uses a soft cream tone rather than pure grey to stay tonally with the gold accents) + 24px radius + 20px padding + soft gold hover lift (`translateY(-4px); border-color:rgba(254,191,29,.28); box-shadow:0 14px 32px rgba(214,158,0,.10)`). The 4 SVG icons (`modeIcons[0..3]` — circle-globe, building, link-chain, refresh) are reused unchanged; only the wrapping tile chrome changed. `.modes-bento-grid > div { height:100% }` so the ScrollAnimation wrapper stretches to fill the grid row (without it, the inner card would shrink to content height and rows would be uneven). Mobile (`max-width:760px`) collapses to `1fr`, gap drops to 16px, card padding to 16px, visual `min-height` to 130px. Did NOT touch the adjacent `#revenue` section even though it shares the same `grid-4` + `card glass-card` pattern — user only asked about the modes section. Did NOT modify `.grid-4` base rule (would leak into many other places). `tsc` 0. Revert: restore `<div className="grid-4">{4× .card.glass-card with .card-icon + h3 + p}</div>` in `b2b/page.tsx` 237-247 and delete the `.modes-bento-grid` / `.mode-bento-card` / `.mode-bento-visual` / `.mode-bento-icon` / `.mode-bento-body` block at end of `sections.css`. Note: this turn's commit landed bundled with the parallel `d665c7a` "#demo refactor" commit because the working tree was staged together — the modes-bento diff is fully inside `d665c7a` despite the misleading subject line.

- 2026-05-30 — **b2c `#support` "完善售后，无忧保障" — 4 equal cards → bento-grid layout (tall left + wide top-right + small + dark accent), gold theme** (`b2c/page.tsx` 469-528, `sections.css` 5460-5660+, `messages/{en,zh}.json` `product.support.bento.*`). User refs: screenshot 1 red-boxed the existing 4-card grid; screenshot 2 was a Quiety-style bento layout (Goals tall card, Analytics wide card, Grow small card, "Reduced Turnover 27%" dark green accent stat card). Asked for `布局改成 + 适配黄色主题的色调`. **Markup**: replaced `<div className="grid grid-4">` + 4 identical `.card.glass-card.tilt-card` divs with `<div className="support-bento">` containing 4 `<article>` cards keyed by purpose: `.support-bento-tall` (s1 video tutorials, spans 2 rows on the left), `.support-bento-wide` (s2 docs, spans cols 2-3 on top, `flex-direction:row` so text and doc-preview sit side-by-side), `.support-bento-small` (s3 community, single cell bottom-middle), `.support-bento-accent` (s4 1-on-1 support, dark bg bottom-right). Each card has the original icon + h3 + p plus a decorative element matching its purpose: tall card has a `.support-bento-list` with 4 lesson items + bullet dots (one `--active` highlighted gold); wide card has 2 `.support-bento-doc` rows w/ `API`/`SDK` tag chips + dummy `.support-bento-doc-line` rule bars; small card has a stacked avatar group (`L M W +12` overlapping circles in gold gradients); accent card has a large `1:1` glyph in gold + small-caps label. **CSS**: `.support-bento{display:grid;grid-template-columns:1.15fr 1fr 1fr;grid-template-rows:auto auto;gap:20px}` desktop. Placement via `grid-row: span 2` on tall, `grid-column: span 2` on wide. Cards share `.support-bento-card{background:#fff;border:1px solid rgba(0,0,0,.06);border-radius:24px;padding:32px}` + soft gold hover lift `box-shadow:0 14px 32px rgba(214,158,0,.10);transform:translateY(-3px)`. Three icon-tile tints all in the warm gold family (`--gold` rgba(254,191,29,.18), `--ink` rgba(24,24,24,.06), `--cream` rgba(254,233,168,.55)) — replaces the prior blue/lavender/green/amber set since user asked for yellow-theme adaptation. **Accent card**: `linear-gradient(135deg,#1d1a14,#2a241a)` dark warm-brown bg (NOT pure black — warmer matches the yellow theme), border `rgba(254,191,29,.25)` thin gold rim, headline white, body 72%-white, `1:1` number `font-size:3rem;color:var(--primary)` gold. **Responsive**: `@media(max-width:1024px)` collapses to 2-col with tall+wide both spanning the whole row, small/accent sit side-by-side; `@media(max-width:640px)` to single column, accent card stacks number under text. **i18n**: added `product.support.bento.{lesson1..4,accentLabel}` in both `en.json` (Installation & Setup / Device Onboarding / Billing & Operations / Advanced Features / Dedicated channel) and `zh.json` (安装与配置 / 设备接入 / 计费与运营 / 高级功能 / 专属对接) so the EN site doesn't show Chinese decorative text. Did NOT touch the b2b page's separate `#aftersales` section (different selectors `b2b.aftersales.item1-4`) — only b2c `#support` redesigned this turn. Did NOT remove the original 4 SVG icons — reused the same SVG paths in the new markup so visual continuity with prior session's icon work is preserved. `tsc` 0. Revert path: restore the prior `<div className="grid grid-4">{4× .card.glass-card.tilt-card}</div>` block in `b2c/page.tsx` (snapshot from before this turn lives in `git show <pre-commit>:b2c/page.tsx`), delete the new `.support-bento*` CSS block at the bottom of `sections.css`, drop the new `product.support.bento.*` keys in both JSON files.

- 2026-05-30 — **b2c `#gallery` "产品展示" — phones enlarged + 2nd phone featured by default (no subtitle overlap)** (`polish.css` 574-620 + 690-707). Two-step this turn driven by paired user msgs: (1) `手机界面放大一点点，四个的间距可以小一点` → bumped `.g3d-phone { width: 220px → 250px }` and tightened `.gallery-row { max-width: 1280px → 1180px, gap: 32px → 24px }`. With `justify-content: space-between` the actual gap is computed by container, not by `gap`; the new max-width brings the inter-phone gap from ~123px (old: `1280-32-880`)/3 = ~123) down to ~29px (`1148-1060`)/3 after the featured 2nd kicks in). (2) `静态展示时，默认第二个手机界面放大，当不要挡住副标题的文字内容` → added a new static rule `.g3d-phone:nth-child(2) { width: 310px; transform: translateY(-20px); z-index: 1; filter: drop-shadow(0 28px 36px rgba(0,0,0,.26)) }` placed BETWEEN the base `.g3d-phone` rule and the `:hover, :focus-visible` rule so hover (same specificity 0,2,0) wins on any phone via source-order. The -20px lift stays well within the gallery row's `padding-top: 80px` buffer — even on a 310px wide phone (`height = 310 × 844/390 = 671px`), the top edge lands at 60px from section top, leaving plenty of clearance above for the subtitle "查看 GCSS 在移动端、网页端和管理端的实际运行效果。". Hover state (`scale(1.35) translateY(-24px)`) untouched — when user actively hovers, the 1.35x explosion still happens (transient, user-initiated; user's constraint was about STATIC overlap). Mobile breakpoint (`@media (max-width:760px)`): added `.g3d-phone:nth-child(2) { width:auto; transform:none; z-index:auto; filter:drop-shadow(0 18px 24px rgba(0,0,0,.18)) }` to reset the featured rule inside the 2-col grid (same selector specificity, later source order → wins inside the @media). `Gallery3D.tsx` markup untouched — still 4 plain `.g3d-phone tabIndex=0 role="img"` divs in source order, no `is-active` class anymore (the `3fe4b93` click-to-enlarge state was already reverted to hover/focus before this turn). `tsc` 0. Revert: change `.g3d-phone { width: 250px → 220px }`, `.gallery-row { max-width: 1180px → 1280px, gap: 24px → 32px }`, delete the new `.g3d-phone:nth-child(2)` block AND the mobile reset block.

- 2026-05-30 — **`#models` top-right button radius unified with sitewide `.btn`** (`sections.css` 2683-2702, commit `f7e2001` pushed fork `design-draft-local`). User: `按钮的圆角与其他按钮圆角统一`. The pill from `5425a25`/`102fb48` was using `border-radius: 999px` (full capsule, matched the iconic pill shape but stood apart from every other CTA on the site). Changed to `border-radius: var(--radius)` — the `--radius: 10px` token from `tokens.css` 84, which the base `.btn` rule uses at `components.css` 8. Now the `了解更多` button matches `.btn-primary` / `.btn-secondary` / `.btn-ghost` and all the form/CTA buttons across the site. Other pill props (`position:absolute;top:24px;right:24px;padding:8px 14px;background:#fff;border:1px solid rgba(0,0,0,.12);font-size:.8rem`) and the `svg{width:14px;height:14px}` icon shrink left intact — only the corner-radius changed. Hover rule still flips to solid gold + drop-shadow. Revert: change `border-radius: var(--radius)` back to `999px`.

- 2026-05-30 — **Removed visual shadows from the top nav bar AND the diagram modal close button** (`components.css` 202-210 + `DiagramModal.css` 65/73). User screenshot of an opened `DiagramModal` (B2B2C diagram) red-boxed the band under the nav; said `顶部导航栏和关闭按钮都去掉投影`. **Close button**: `.diagram-modal-close` had `box-shadow: 0 4px 12px rgba(0,0,0,.08)` (base) + `box-shadow: 0 4px 16px rgba(254,191,29,.35)` (hover). Both swapped to `box-shadow: none`. Kept the 1px `rgba(0,0,0,.12)` border, the 44px circle, the gold hover bg, and the `scale(1.08)` hover pop — only the shadow goes. **Nav**: every `.header` / `.header.scrolled` selector across `components.css` already had `box-shadow: none` (set by a prior session whose comment was `顶部导航栏静态展示时，无投影，只有边线`). The visible "shadow band" the user saw was the `.header::after` decorative 1px gradient (`linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent)`) — combined with the header's `backdrop-filter: blur(24px) saturate(180%)` and the modal's white overlay sitting BELOW the nav (the `<main>` element has `position:relative;z-index:1` in `polish.css` ~1141 creating a stacking context that traps the modal's `z-index:10000` overlay below the nav's `z-index:100` — that's why the nav stays visible when the modal opens), it rendered as a soft highlight band reading as a shadow. Dropped the `::after` entirely (`content: none`). Kept the `.header { border-bottom: 1px solid rgba(0,0,0,0.06) }` edge line — earlier session explicitly wanted `只有边线`, so the line stays. Did NOT touch the per-page `.header.scrolled` rules (homepage `body:has(.hero-with-video)`, pricing/demo `body:has(.demo-hero|.pricing-hero)`, sub-nav body) — they already had `box-shadow: none`. **Did NOT** fix the underlying modal-overlay-behind-nav stacking bug — out of scope; modal currently looks fine because the close button is `position:fixed; top:80px` (clears the nav) and the diagram itself doesn't sit under the nav. If user later wants the overlay to actually cover the nav, drop the `z-index:1` on `main` or render the modal as a body-level portal. `tsc` 0. Revert: restore `.header::after { content:''; position:absolute; bottom:0; left:0; right:0; height:1px; background:linear-gradient(90deg,transparent,rgba(255,255,255,0.1),transparent); pointer-events:none }` and reinstate the close-button shadows. Prior session's `#models` pill restyle (commits `1405778`/`102fb48`) + hero rounded-card (`3f093ed`) still in place — untouched this turn.

- 2026-05-30 — **`#models` pill button static state → solid white + 1px border** (`sections.css` 2683-2702, commits `1405778` + `102fb48` pushed fork `design-draft-local`). Two micro-tweaks to the top-right pill from `5425a25`: (a) `1405778` flipped `background: rgba(254,191,29,.12)` (faint gold) → `background: #fff` for a cleaner pill on the busy yellow-illustration card bgs, then (b) `102fb48` added `border: 1px solid rgba(0,0,0,.12)` per follow-up `带边线` so the white pill stays visible on the pale-yellow regions of the new chart/molecule artwork. Hover rule (`.btn-accent:hover, .model-card:hover .btn-accent, .model-card:focus-within .btn-accent` at 2700-2709) still flips to `background: var(--primary)` solid gold + the gold drop-shadow — the new border just sits under the gold fill when hovered (1px black-12 doesn't visibly bleed through `#FEBF1D` at full alpha). All other pill props (`position:absolute;top:24px;right:24px;padding:8px 14px;border-radius:999px;font-size:.8rem;gap:6px`) and the `.model-card .btn-accent svg{width:14px;height:14px}` arrow-icon shrink from `5425a25` left intact. Revert: drop the `border` line and change `background:#fff` back to `rgba(254,191,29,.12)`.

- 2026-05-30 — **Nav bar drop-shadow removed sitewide — `box-shadow: none` on every `.header.scrolled` variant** (`components.css` 106-117 + 135-141). User retried `没有改到，重新改` after two prior attempts targeting only `body:has(.pricing-hero)` did nothing on the actual pricing page. **Root cause**: `.pricing-hero` is not a class that exists anywhere in the codebase (`grep` confirmed only the two CSS selectors reference it — the pricing-page markup uses `section#plans.section.pricing-section.pricing-section--tabs` instead). So `body:has(.pricing-hero) .header` never matched and my "fix" applied to nothing on the page the user was looking at. The shadow the user saw came from the base rule `.header.scrolled { box-shadow: 0 4px 14px rgba(0,0,0,0.05) }` which fires on every page once `scrollY > 20` (logic in `Header.tsx` 58). **Fix**: replaced `box-shadow: 0 4px 14px rgba(0,0,0,0.05)` with `box-shadow: none` in BOTH places where it lived — the base `.header.scrolled` (line ~110) AND the homepage override `body:has(.hero-with-video) .header.scrolled` (line ~139). Used `replace_all:true` on the 4-line block since both occurrences were byte-identical. The dead `body:has(.pricing-hero)` block stays in place but is harmless (no markup matches it; pruning could nick the demo page which DOES have `.demo-hero` — left intact). Border line `rgba(0,0,0,0.06)` stays everywhere. Backdrop blur stays everywhere. Transition `all 0.4s cubic-bezier(0.16,1,0.3,1)` on `.header` (line 104) still animates the colour/border state-changes, just nothing shadow-shaped to animate now. **Lesson for next time**: when a CSS selector "fix" doesn't take effect, FIRST verify the selector actually matches the page (search the JSX for the class name). Don't trust the CSS file's history of references — `.pricing-hero` was a ghost selector that had been added prophylactically. `tsc` 0. Revert: change both `box-shadow: none` back to `box-shadow: 0 4px 14px rgba(0,0,0,0.05)` and drop the new comment block. Prior session's `#models` button → top-right pill (`5425a25`), homepage hero inset rounded card, `#models` BG asset swap (`e5b215c`), settings-popover dark-mode removal (`01f2583`), b2c gallery hover-to-enlarge (`0197200`) all still in place — untouched this turn.

- 2026-05-30 — **Homepage `#models` card "了解更多" button: full-width bottom bar → compact top-right pill** (`sections.css` 2683-2702, commit `5425a25` pushed fork `design-draft-local`). User: `把卡片里面的按钮改小一点，放到卡片的右上角`. Before: `.model-card .btn-accent{margin-top:auto;width:100%;padding:14px 24px;border-radius:var(--radius);background:rgba(254,191,29,.1);color:#1f1f1f;font-weight:700;font-size:.95rem;justify-content:space-between}` — `margin-top:auto` inside the `.model-card{display:flex;flex-direction:column}` pushed the button to the bottom edge, `width:100%` made it a full-width band with the text/arrow split via `space-between`. After: `position:absolute;top:24px;right:24px;margin-top:0;width:auto;padding:8px 14px;border-radius:999px;background:rgba(254,191,29,.12);color:#1f1f1f;font-weight:600;font-size:.8rem;gap:6px;justify-content:center` — pinned to the top-right corner (the parent `.model-card` already had `position:relative` at 2676, so absolute positioning anchors correctly). Added `.model-card .btn-accent svg{width:14px;height:14px}` to scale the arrow icon down from the JSX-hardcoded 18×18 to match the smaller text. Hover rule (lines 2700-2709, `background:var(--primary);color:#1f1f1f;box-shadow:0 6px 20px rgba(254,191,29,.25)`) left intact — fires on `.btn-accent:hover`, `.model-card:hover .btn-accent`, and `.model-card:focus-within .btn-accent` so hovering anywhere on the card still flips the pill to solid gold. Markup (`BusinessModelsSection.tsx`) untouched — both cards still render `.model-icon → h3 → p → button.btn-accent` in source order, the button just renders out-of-flow now and the description `<p>` is the bottom-most flow item. Did NOT touch the card padding (`40px` at line 2673) — the top-right pill at `top:24px;right:24px` fits inside the 40px gutter without overlapping the model-icon at the top-left or the h3 title below. Did NOT add a `padding-right` reservation on `<h3>` — the 24px title sits ~50px below the pill (40px card padding + 32px model-icon + 20px gap), so they don't collide vertically. Revert: restore the prior block (margin-top:auto, width:100%, padding:14px 24px, font-size:.95rem, justify-content:space-between) and delete the new `.model-card .btn-accent svg` rule.

- 2026-05-30 — **Homepage hero (`.hero.hero-centered`): edge-to-edge orange bg → inset rounded card** (`pages-extracted.css` ~2451-2495). User ref: Quiety hero (rounded lavender card with margin around it on the page). Previously the hero painted `/images/hero-bg-8.png` flush from viewport edge to edge with `padding-top: calc(72px + clamp(2px,0.5vh,8px))` to push content below the fixed 72px nav overlay. The comment explicitly bragged about being edge-to-edge (`fills the WHOLE first screen edge-to-edge (no white margin / inset panel)`) — now reversed. **Now**: `margin: 84px 16px 12px` (top = 72px nav clearance + 12px gap, sides = 16px gutter, bottom = 12px gap before trusted-bar) + `border-radius: 32px` + `overflow:hidden` (was already there — clips the bg image to the rounded shape). Removed the 72px buffer from `padding-top` since the margin now handles nav clearance: `padding: clamp(12px,1.5vh,24px) clamp(16px,3vw,32px) clamp(12px,1.5vh,24px)`. **First-fold math**: previously `min-height: 100vh - 180px` (180 = trusted-bar height) with hero pinned at top:0. Now `100vh - 276px` (276 = 84 top margin + 12 bottom margin + 180 trusted-bar) so the marquee still lands flush at `100vh`. `max-height` matches (locks the panel so dashboard overflow can't push the marquee out of the fold). Mobile breakpoint `@media (max-width:600px)`: tighter `margin:80px 8px 8px` + softer `border-radius:20px` + recomputed `min/max-height: calc(100svh - 268px)` (80+8+180). Background image (`hero-bg-8.png`) unchanged — it just gets clipped now. Inner `.hero-centered-inner` (transparent, content cap) untouched. `tsc` 0. Revert: restore the prior block (margin/border-radius/the new `min-height: 276` calc gone, `padding-top` back to `calc(72px + clamp(2px,0.5vh,8px))`, `min/max-height` back to `100vh - 180px`). Prior session's `#models` card bg swap (commit `e5b215c`) still in place — untouched this turn.

- 2026-05-30 — **Swapped the two homepage `#models` "灵活的运营模式" card background images** (`public/images/business-models/b2c-bg.jpg` + `b2b2c-bg.jpg`, commit `e5b215c` pushed fork `design-draft-local`). User pasted two new artwork PNGs (1368×760): a yellow-grid + bar/line-chart composition for the B2C card and a yellow molecular-network with concentric ripple rings for the B2B2C card, plus a screenshot red-boxing both `model-card`s on the homepage. The cards were already wired for background images — `sections.css` 2711-2723 has `#models .model-card.model-card-b2c{background-image:url('/images/business-models/b2c-bg.jpg');background-size:cover;background-position:center;background-repeat:no-repeat}` and the same for `.model-card-b2b2c` pointing at `b2b2c-bg.jpg`. So the change is a pure asset swap: converted both PNGs → JPG via Pillow (`quality=88, optimize=True, progressive=True`) and overwrote the existing paths in `public/images/business-models/` (was 1400×918 photos, now 1368×760 illustrations — `cover` handles the aspect ratio, content stays centered). New file sizes 37.6KB (b2c) + 53.2KB (b2b2c) — well under the prior assets. Markup (`BusinessModelsSection.tsx`) and CSS untouched. Revert: restore the prior JPGs from `git show e5b215c^:website/frontend/public/images/business-models/b2c-bg.jpg` etc.

- 2026-05-30 — **Pricing / demo nav: drop box-shadow in BOTH static AND scrolled states — only border line remains** (`components.css` 143-156). User repeated `顶部导航栏静态展示时，无投影，只有边线` with the same scrolled-state pricing-page screenshot. **First attempt this turn** (commit `d26d796`, since superseded): split the 4-selector group into static (no shadow) + `.scrolled` (shadow) — reasoning: "静态展示时" = static state, so leave the scrolled shadow alone. **User repeated the request with the SAME screenshot showing the scrolled state still has the shadow** — interpretation #1 was wrong. Correct reading: "静态展示" means "show the nav statically/without effects" (interpretation #2), i.e. no dynamic drop-shadow appearing at all. **Final fix**: re-merged the 4-selector group `body:has(.demo-hero) .header, body:has(.demo-hero) .header.scrolled, body:has(.pricing-hero) .header, body:has(.pricing-hero) .header.scrolled` into one block with `box-shadow: none` everywhere. Border line `rgba(0,0,0,0.06)` stays — user said "只有边线". White frost `rgba(255,255,255,0.92)` + `backdrop-filter: blur(24px) saturate(180%)` unchanged. Updated the comment block to: `Demo / pricing page (yellow hero) — keep the header readable on the warm bg by forcing a near-opaque white frost on top, regardless of scroll. No drop-shadow in EITHER state — only the 1px border line separates the nav from the content (user request: "只有边线").` **Lesson** (for future me): when the user repeats the same request with the same screenshot after a "fix", do not assume the prior interpretation was right — re-read the request literally. "只有边线" (only the border line) was the load-bearing phrase — if shadow remains in any state, the user will repeat. Other pages still get the scroll-shadow normally: base `.header.scrolled` (line 105) keeps `box-shadow: 0 4px 14px rgba(0,0,0,0.05)`, and the homepage `body:has(.hero-with-video) .header.scrolled` (line 134) keeps it too — only pricing+demo override is shadow-free. The transition `all 0.4s` on `.header` still animates state changes (now just frost/border/colour, no shadow). `tsc` 0. Revert: re-add `box-shadow: 0 4px 14px rgba(0,0,0,0.05)` to the merged block (which would restore the original always-shadow behaviour, NOT the in-between `d26d796` state). Prior session's settings-popover dark-mode removal (`01f2583`) + b2c gallery hover-to-enlarge (`0197200`) + homepage `#models` BG asset swap (`e5b215c`) still in place — untouched this turn.

- 2026-05-30 — **b2c `#gallery` — equal-size static row, HOVER phone to enlarge** (`Gallery3D.tsx` + `polish.css` ~572-650, commit `0197200` pushed fork `design-draft-local`, supersedes click-based `3fe4b93`). User flipped requirement again: `帮我改成，鼠标光标移动到界面上面就放大`. Dropped `'use client'`/`useState`/`onClick`/`<button>` and the `is-active` class machinery; `Gallery3D.tsx` back to a pure SSR `<div tabIndex={0} role="img">` component. CSS replaced `.is-active` with `:hover, :focus-visible` selectors carrying the same `scale(1.35) translateY(-24px); z-index:2; drop-shadow` payload. Static state unchanged: all 4 phones still `width:220px`. Mobile (`max-width:760px`) `:hover`/`:focus-visible` keeps the softer `scale(1.08) translateY(-6px)`. Earlier troubleshooting note: when an edit doesn't visibly take effect on :8000 dev, the running `next dev`'s file-watcher likely died (container inotify limit); kill the process, `rm -rf .next`, restart `npm run dev` — and the user must hard-refresh (Ctrl+Shift+R) to bust the browser's cached CSS bundle (Turbopack uses content-addressed bundle URLs, so cached bundles persist across server restarts when content is identical). Revert: swap `:hover,:focus-visible` back to `.is-active` and restore the `useState`/`onClick` flow from commit `3fe4b93`. User: `鼠标做点击交互时，手机界面才放大，静态展示时，四个一样大`. Replaces the prior "2nd phone featured + hover lift" state (commit `4e27f53`). **Component**: re-added `'use client'`, `useState<number | null>(null)` tracks the active index, root tag changed from `<div>` to `<button type="button">` per phone (semantic for click-to-toggle + native focus/keyboard handling), `onClick` toggles `(curr === i ? null : i)` so clicking the same phone again resets, `aria-pressed={active === i}`, `aria-label={p.label}`. Default state: no phone is enlarged. **CSS**: dropped the `.g3d-phone:nth-child(2){width:300px;translateY(-32px)}` featured rule AND all the `:hover/:focus-visible{translateY(-24px)}` lift rules. All 4 phones now share `width:220px;flex:0 0 auto;transform-origin:50% 100%` (origin bottom-center so scale-up grows upward, keeping the baseline). Added `<button>` resets: `padding:0;background:transparent;border:none;font:inherit;color:inherit;cursor:pointer` so the `<button>` looks identical to the prior `<div>`. **Active state**: `.g3d-phone.is-active{transform:scale(1.35) translateY(-24px);z-index:2;filter:drop-shadow(0 36px 44px rgba(0,0,0,.32))}` — scale + small lift, on top, shadow deepened. Transition same `transform .45s cubic-bezier(.22,1,.36,1),filter .45s`. Mobile (`max-width:760px`): grid stays, `.g3d-phone.is-active` softer (`scale(1.08) translateY(-6px)`). `prefers-reduced-motion` zeros transition duration. `tsc` 0. Revert: re-apply `:nth-child(2)` + `:hover` blocks, remove `is-active` rule, change `<button>` back to `<div tabIndex={0}>`, drop `useState`/onClick.

- 2026-05-30 — **Removed the dark-mode (主题) toggle from the header settings popover** (`SettingsPopover.tsx` + `Header.tsx`). User screenshot circled the moon-icon toggle inside the gear-icon dropdown (right under the EN/中文 row) — wanted it gone. `SettingsPopover.tsx`: dropped the `useThemeContext` import + `const { toggleTheme } = useThemeContext()` destructure, then deleted the entire `<div className="settings-popover-divider" />` + theme `settings-popover-section` block (was `t('settingsTheme')` label + the moon/sun SVG button calling `toggleTheme()`). The popover now renders only the language section (EN/中文). `Header.tsx`: also removed the matching mobile-nav `<button className="theme-toggle">` (previously at ~296-307) inside `.mobile-nav-settings` so desktop + mobile stay consistent — only `.lang-switcher` remains in that div. Pruned now-unused `useThemeContext` import + `const { toggleTheme } = useThemeContext()` from `Header.tsx` too. Did NOT touch `ThemeProvider` (still wraps the tree — other code may depend on theme state even without a user-facing toggle). Did NOT delete the `.theme-toggle` / `.settings-popover-theme` / `.settings-popover-divider` CSS rules in `pages.css`/`enhancements.css` (dead now but cheap to leave; pruning risks accidentally hitting a co-named selector). Did NOT remove the `nav.settingsTheme` i18n key from `messages/{en,zh}.json` (orphan key is harmless, removing risks churn if a future session wants the toggle back). `tsc` 0. Revert: re-add `import { useThemeContext } from '@/providers/ThemeProvider'` + `const { toggleTheme } = useThemeContext()` in both files; in `SettingsPopover.tsx` paste the theme JSX (divider + section with the moon/sun SVG button) back between the closing `</div></div>` of the lang section and the `)}` of the conditional; in `Header.tsx` paste the mobile `<button className="theme-toggle">` block back after the `.lang-switcher` div inside `.mobile-nav-settings`. Prior session's b2b hero EN-wrap fix (`c7cfb17`) and the b2c `Gallery3D` 4-phone row (`4e27f53`) still in place — untouched this turn.

- 2026-05-30 — **b2b (`/[locale]/b2b`) hero "SaaS Platform Operator Mode" EN title was being clipped — removed `white-space:nowrap` so EN wraps to 2 lines, ZH still fits one line** (`sections.css` 1816-1829). User screenshot showed EN title cut after "Mod-" with the rest hidden under the right-side illustration. Root cause: a prior session forced `.product-hero.particles-bg .hero-title { white-space: nowrap }` (tuned for ZH "SaaS 平台版运营模式" ≈ 510px at 3.5rem, which fits the 560px text column from the `.product-hero.particles-bg .hero-split:has(.hero-video-wrap)` grid rule). EN "SaaS Platform Operator Mode" at the same 3.5rem clamp cap is ~900px — `nowrap` made it overflow the column and visually get covered by the `.hero-video-wrap` illustration in the right column. Fix: deleted the `.product-hero.particles-bg .hero-title { white-space: nowrap }` block entirely; kept `.product-hero.particles-bg .hero-title-tag { display:block; margin-left:0; white-space:nowrap }` so "(B2B2C)" stays intact AND stays on its own line under the title. Did NOT change font size (user's earlier `字号不要改` constraint still holds — base h1 `clamp(2rem,5vw,3.5rem)` untouched). Did NOT widen the 560px text column (the alternative fix) — column was deliberately narrowed so the illustration takes the larger share; wrapping EN is the lighter-touch change. Result: ZH renders identically (CJK is compact enough to fit 560px on one line without nowrap); EN now wraps "SaaS Platform Operator" / "Mode" (or similar) inside the column with the tag on a third line. Mobile (`max-width:900px`) stack rule unchanged. `tsc` 0. Revert: re-add `.product-hero.particles-bg .hero-title { white-space: nowrap }` above the `.hero-title-tag` rule. Prior session's `Gallery3D` work (b2c `#gallery` row, final commit `4e27f53`) still in place — untouched this turn. Started this session as the original `Gallery3D` 3D cluster (4 absolute-positioned slots in back/front pairs, scene-wide parallax tilt, single-phone hover straightens to front). Walked through 4 layouts via user feedback: (a) `9506399` hover-to-feature row (`useState`+`is-active` class, default index 1 scaled+tilted, hover hands spotlight); (b) `429c11c` fully static evenly-distributed row after `静态展示时，对齐分布，无交互` (dropped `useState`/hover entirely, `flex:1 1 0;max-width:220px`); (c) `4bbf065` enlarged + hover lift after `展示界面放大一点，鼠标光标交互时，手机界面向上展示` (re-added `tabIndex`, `max-width:280px`, hover `translateY(-24px)`); (d) `4e27f53` current state mirroring user's 4-phone reference image (`参考这个布局`): bottoms aligned at baseline, 2nd phone enlarged and raised. **Current CSS**: `.gallery-row{display:flex;justify-content:space-between;align-items:flex-end;gap:32px;max-width:1280px;padding:80px 16px 40px}` (top padding leaves room for the raised 2nd phone). `.g3d-phone{flex:0 0 auto;width:220px;cursor:pointer;filter:drop-shadow(0 18px 24px rgba(0,0,0,.18));transition:transform .45s cubic-bezier(.22,1,.36,1),filter .45s}`. **Featured rule** `.g3d-phone:nth-child(2){width:300px;transform:translateY(-32px);filter:drop-shadow(0 30px 38px rgba(0,0,0,.28));z-index:2}` — fixed-width approach (NOT scale transform) so the bezel/screen scale together correctly. Hover: `.g3d-phone:hover/:focus-visible{transform:translateY(-24px);filter:drop-shadow(0 36px 44px rgba(0,0,0,.32))}` and a stacked override `.g3d-phone:nth-child(2):hover{transform:translateY(-56px)}` so the already-raised featured phone lifts further (-32 base + -24 hover delta = -56). `:focus-visible` adds 2px gold outline at offset 6 with 24px radius. **Mobile (`max-width:760px`)**: collapses to 2-col grid, both base + `:nth-child(2)` rules reset (`width:auto;transform:none`), hover lift drops to `-8px` (also applied to nth-child(2)). `prefers-reduced-motion` zeros transition duration only (transform still applies — accessible default for "phone moves on hover"). `Gallery3D.tsx` is a pure SSR component (no `'use client'`, no state) — interaction is 100% CSS. Kept the graphite `.g3d-device` + metallic `::before` rim + `.g3d-screen` + `.g3d-notch` mockup unchanged throughout. `tsc` 0. Revert path: restore the original `Gallery3D` (absolute slots + scene-tilt useState) + the old `.gallery-3d`/`.gallery-3d-stage`/`.g3d-slot-*` CSS block.

- 2026-05-30 — **Pricing `#addons` "可选扩展" cards restyle: editorial-stat layout, yellow icon tile + black line icon, vertical dividers between columns** (`sections.css` 4283-4360, commits `3e23bcb` + `619440d` + `2dd3e6f` pushed fork `design-draft-local`). Three-step this turn: (1) `3e23bcb` shipped the editorial-stat layout (centered, icon on top, big bold price under it, small grey label below) AND flipped the icon to black-bg + yellow-icon with no shadow, mirroring the "12-24 hours / 2 seconds / 40% faster" reference. (2) `619440d` flipped the icon palette back: user wanted yellow background + black icon (the original brand pairing) while keeping the no-shadow flat treatment and the new centered editorial layout. (3) `2dd3e6f` added thin vertical divider lines between cards (user reference image had red guides drawn between the 3 columns) — `.addon-card{position:relative}` + scoped pseudo-element `@media(min-width:901px){.addons-grid .addon-card:not(:nth-child(3n+1))::before{content:'';position:absolute;left:-7px;top:12%;bottom:12%;width:1px;background:rgba(0,0,0,.1);pointer-events:none}}`. `left:-7px` centers the 1px line in the 14px grid gap. Companion `@media(max-width:900px) and (min-width:561px){.addons-grid .addon-card:nth-child(2n)::before{...}}` handles the 2-column tablet breakpoint (every even card gets a left divider). At the 1-col mobile breakpoint (`max-width:560px`) no rule fires, so no dividers stack vertically. Old cards rendered as left-aligned chips with `.addon-card{background:var(--dark-card);border:1px solid var(--border-light);border-radius:var(--radius-md);padding:28px 20px;flex-direction:column;align-items:flex-start;gap:14px;...:hover{translateY(-4px);box-shadow:var(--shadow-md)}}` (from the original block 3818-3858) plus the override at 4283 which only set `flex-direction:column;align-items:flex-start;gap:14px`, and `.addon-card-icon` was a 52px solid-gold tile with `background:var(--primary)`, `color:#181818`, `box-shadow:0 0 20px rgba(254,191,29,.35)` (the gold glow). **Now** (overrides at 4283-4329, originals at 3818-3858 left intact — overrides win by cascade): `.addon-card{background:transparent;border:none;border-radius:0;padding:24px 16px;align-items:center;text-align:center;gap:18px;box-shadow:none;transition:none}` + a fresh `.addon-card:hover{transform:none;box-shadow:none}` that kills the lift. `.addon-card-icon` → **56px, `background:var(--primary,#febf1d)`, `color:#181818`, `box-shadow:none`** (yellow bg, black icon, no glow — the 619440d state). `.addon-card-body{flex-direction:column-reverse;align-items:center;text-align:center;gap:8px}` — DOM order stays label→price (a11y-friendly: what-it-is read before how-much), `column-reverse` flips visual order so price renders directly under the icon as the big stat and label sits below as the description. New scoped overrides `.addon-card .addon-label{font-size:.92rem;font-weight:500;color:var(--text-secondary,#555);line-height:1.45}` and `.addon-card .addon-price{font-size:1.6rem;font-weight:800;color:var(--primary);line-height:1.1;letter-spacing:-.01em}`. Markup unchanged (`pricing/page.tsx` 271-281 still renders `.addons-grid > .addon-card > .addon-card-icon + .addon-card-body > .addon-label + .addon-price`). Revert: restore the original 4283-4329 block from before `3e23bcb`.

  **PUSH POLICY (settled):** push every commit with `git push fork main:design-draft-local`. Local `main` → fork branch `design-draft-local`. NEVER force-push or touch fork `main` (it holds 54 security-pin/contact-page commits that diverged from local at `8a4d15c` on 2026-05-11; local-only design work is the 400+ commits). `origin` = `anindya127/Design-draft` also diverged — don't push there.

  **Older still-relevant flags (do NOT reapply unless asked):** b2c `#features` section 4 has an `AppSlideshow` carousel — a separate phone slideshow in a `4/3` `.feature-image-placeholder:has(.app-slideshow)` card (do not confuse with the `Gallery3D` `#gallery` cluster). homepage hero `.hero.hero-centered .hero-centered-inner` uses `url('/images/hero-grid-bg.png') cover` — a CSS dot-grid was tried then REVERTED (`6ed2f2b`). b2c `.product-hero::before` light-mode dot field IS live (`d692ec4`, gold `rgba(214,158,0,0.55)` 2.5px/24px; pale-yellow on `#F1F2F4` ≈ invisible — use darker gold ≥0.4 alpha for texture on that bg); same selector also styles the b2b hero. Homepage yellow nav bar was reverted (`bcbd0aa`); `logo-globe.png` recoverable via `git show 8fef934`. b2c `.product-hero` is `min-height:calc(100svh - 42px)` flex-column with `.hero-footer-bar{margin-top:auto}` pinning the 3 stats to the fold (b2b shares `.product-hero` — verify if touching). The running `next dev` on :8000 can develop a dead file-watcher (container inotify limit) and serve a stale CSS bundle — if an edit doesn't show, kill it, `rm -rf .next`, restart `npm run dev`.
